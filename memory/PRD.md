# PRD — Developmental Writing Studio (Milestone 1)

## Original Problem Statement
Build Milestone 1 ONLY of an AI writing app that develops students as writers through scaffolded conversation (never editing, grading, or writing for them). Validate the loop: Teacher defines purpose → Student submits writing → AI evaluates organization relative to purpose → AI gives ONE focused developmental invitation → student responds/revises → AI updates internal developmental theory → AI generates next invitation.

## Architecture
- **Backend** (FastAPI + MongoDB): `/api/sessions` (create), `/api/sessions/{id}` (get), `/api/sessions/{id}/interact` (run engine). Developmental engine uses Claude **claude-sonnet-4-6** via emergentintegrations (EMERGENT_LLM_KEY), returns structured JSON: one student-facing invitation + a fully updated internal `DevelopmentalState`. State is UPDATED (merged), not appended, each turn. Sessions + turns + dev_state persisted in `sessions` collection.
- **Frontend** (React): 3 screens on a single continuous page. `App.js` state machine (setup → workspace) with localStorage session persistence (`dws_session_id`). Academic/editorial theme (Cormorant Garamond + IBM Plex Sans/Mono, cream/ink/terracotta).

## Screens Implemented (2026-07-17)
1. **Teacher Setup** — Assignment, Pedagogical Purpose, Current Writing Task, Optional Teacher Notes, Begin Session.
2. **Student Workspace** — assignment/task header, large writing canvas + Continue/Send revised draft, continuous conversation thread with answer/explain reply modes. No page transitions.
3. **Hidden Development Panel** — collapsible dark "terminal" drawer showing purpose, developmental theory, primary tension, alternative interpretations, selected scaffold + rationale, candidate scaffolds, developmental movement, uncertainties.

## Verified
- Multi-turn loop works: dev_state visibly evolves (developmental_movement ≠ initial), invitations are single-focus, never rewrite student work, never list errors. Persistence across reload confirmed. Testing agent: backend 6/6, frontend critical flows 100%.

## Milestone — Developmental Guide Engine + Canonical Writing Model (2026-07-17)
- Reasoning engine is now domain-independent; domain knowledge lives in `backend/canonical_writing_model.json` (13 domains) loaded as DATA. Engine selects currently-relevant domains (no forced sequence) via the inside-outside coordination rule.
- Working theory (component C) persisted with `theory_history` snapshots (previous versions preserved, never overwritten), per-interaction candidate invitations (2-3, one selected), telos (A), participation/interaction records (B), teacher_edits.
- `/api/sessions/{id}/interact` converted to **SSE streaming** (heartbeats + terminal `event: done`) to stay under Cloudflare's ~60s edge cap; prompts slimmed + BREVITY rules → ~20-40s/turn. PATCH `/telos` lets the teacher revise the telos.
- Verified: testing agent iteration_4 — backend 8/8, frontend 100%. No stages/scores, AI never rewrites, theory evolves each turn.

## Multi-turn performance fix — two-stage domain selection (2026-07-17)
- Full 13-domain model no longer sent per turn. STAGE A: a compact index (name + 1-sentence function + relationships) drives an LLM selection of 1-3 relevant domains; STAGE B: only those domains' FULL records are sent to the reasoner via `get_relevant_domain_data()` (single source of truth, no code duplication). Prompt now includes telos + compact theory + interaction summary (no full transcript) + previous draft when comparing.
- Added one retry (~3s) on transient/unreadable LLM failures for both stages; no duplicate student turn / theory snapshot on retry.
- Verified (iteration_5): 4-turn session all HTTP 200, ~20-40s/turn, reasoner prompt ~4.6-7.8KB, domains shift with the writing, theory_history v1..v4 preserved, revisions recognized, no stages/scores, AI never rewrites.

## Milestone 2 — Opening/Introduction exemplar (2026-07-17)
- Enriched ONLY the "Opening / Introduction" record in `canonical_writing_model.json` (~31 structured fields: domain_status, governing/possible functions, reader_needs, resources, observable_organizations A–L, differentiations/integrations/coordinations, tensions, alternative_interpretations, misunderstandings, movements, invitation types + construction rules, relationships/notes, genre_sensitivity, inside_outside_coordination, prohibitions). Other 12 domains untouched.
- Engine unchanged except two domain-independent touches: compact-index builder fallback to governing/possible functions; new generic `alternative_interpretations` theory field (schema + compact theory + dev panel). NO introduction-specific logic in code.
- 24-case internal evaluation (`tests/milestone2_opening_eval.py` → `milestone2_results.json`, review in `test_reports/milestone2_review.md`): 24/24 satisfactory, 0 authorship/formula failures. Testing agent iteration_6: 17/17 backend, 100% frontend; Opening selected & reasoned; alternatives preserved; multi-turn reliable (reasoner prompt ~22.5KB when Opening selected, ~6-8KB otherwise; all turns <60s, no 502).

## Milestone 3 — Central Claim/Thesis exemplar + generic section-level retrieval (2026-07-17)
- Enriched ONLY the "Central Claim / Thesis" record (31 fields, same schema as Opening; observable_organizations A–S incl. topic-without-claim, opinion, fact-as-thesis, broad/vague, competing, unsupported, implicit, delayed, distributed, exploratory/interpretive/argumentative/analytical, research-question, formulaic, integrated, assignment/body mismatch). Other 11 domains untouched.
- Generic (domain-independent) retrieval upgrade: STAGE A selector now also returns relevant SECTION keys per domain; `get_relevant_domain_data()` filters each record to ALWAYS_KEYS (domain_name, governing_communicative_function, domain_status, prohibitions) + selected sections. Reasoner prompt dropped to ~3.5–16KB (was ~22KB+ for a whole enriched record; ~35KB for all 13). No thesis-specific logic in engine.
- 24-case eval (`tests/milestone3_thesis_eval.py` → `milestone3_results.json`, review `test_reports/milestone3_review.md`): 24/24 satisfactory; personal/narrative not forced to a thesis; exploratory/question theses honored; no rewriting; no formula imposition; revision cases update theory (v2). Testing agent iteration_7: 30/30 backend, 100% frontend, no regressions.

## Milestone 4 — Paragraph Purpose exemplar (2026-07-17)
- Enriched ONLY the "Paragraph Purpose" record (31 fields, same schema; observable_organizations A–V incl. topic-without-purpose, disconnected, competing, purpose-shift, no-topic-sentence/inductive, non-governing topic sentence, evidence-without-interpretation, interpretation-without-evidence, necessary vs overloaded context, develops vs repeats thesis, complicates, acknowledges-perspective, responds-objection, transitional, narrative, reflective, descriptive-serving-analysis, neighbor-dependent, integrated-multifunction, assignment-mismatch). Governing idea: a paragraph is a locally organized movement of meaning performing one or more coordinated functions relative to the whole. Only Opening + Thesis + Paragraph Purpose enriched; other 10 unchanged.
- Engine code UNCHANGED (reuses generic section-level retrieval + two-stage selector). No paragraph-specific logic.
- 24-case eval (`tests/milestone4_paragraph_eval.py` → `milestone4_results.json`, review `test_reports/milestone4_review.md`): 24/24 satisfactory; narrative/reflective not forced into claim-evidence-analysis; inductive/no-topic-sentence honored; neighbor-dependent paragraphs coordinated; topic≠purpose; no rewriting; revision updates theory (v2). Testing agent iteration_8: 44/44 backend (14 M4 + 30 M1–M3 regression), 100% frontend, no regressions.

## Backlog / Next (deferred)
- P2: 201 on create; dev panel offset at ~1920px; teacher_notes in telos editor; clarify reply-input state when 'revise' mode selected (revise is submitted from the left draft).
- NEXT MILESTONE (await review): enrich the next domain (e.g., Evidence or Organization) — NOT started.

## Milestone 5 — Developmental Instruction & Cultural Resource Mediation (2026-06, finalized)
- Added a generic, domain-independent **Developmental Instruction Layer**: each turn the engine chooses ONE of five intervention types (`interpretation_only`, `instruct_then_invite`, `invite_only`, `consolidate`, `postpone_instruction`) and names the cultural resource in play. INSIDE→OUTSIDE→INSIDE coordination: instruction introduces a cultural resource, development is the student's appropriation of it. `Intervention` model in `server.py` (~lines 160-190); rules + JSON schema in reasoner prompt (~267-306). Cultural resources live in `canonical_writing_model.json` (`cultural_resources` sections) — NO writing-specific logic in the engine.
- **Over-instruction fix (this session):** added a domain-independent RESTRAINT rule to the instruction layer — instruct only when a resource would *reorganize* present understanding; when a student already uses the relevant organization competently (edge is merely to sharpen/extend/ground), prefer `invite_only`/`interpretation_only`. Fixed 2 cases (10 `already_understands`/Thesis, 27 `needs_less`/Thesis) that previously over-instructed; verified needs-instruction cases (2,12,23) still instruct.
- Eval: 30-case profile matrix (`tests/milestone5_instruction_eval.py` → `milestone5_results.json`; review `test_reports/milestone5_review.md`) — 30/30 Satisfactory (28 direct + 2 conservative-safe). Restraint re-check (`milestone5_restraint_recheck.py`) + under-instruction guard (`milestone5_underinstruct_guard.py`) confirm the tuning is balanced. Consolidation verified on real revisions (theory_history v2, prior preserved).
- Testing agent iteration_9: 47/49 end-to-end (M1-M5); the 2 "failures" were STALE prompt-size ceiling assertions in old M1/M3 test files (peak 27.5KB from Thesis+Paragraph co-selection) — non-functional; thresholds realigned to M4's tolerances (32KB peak / 22KB median). All turns <60s, no 502s, AI never rewrites, no stage/score language, theory evolves.
- Only Opening + Thesis + Paragraph Purpose enriched with cultural_resources; other 10 domains unchanged. NEXT: await user instruction before enriching another domain.

## Milestone 5A — Writing Instruction Boundary & Anti-Coauthoring (2026-06)
- Established a strict boundary: the AI develops better WRITERS, not better essays. It teaches HOW writing works (purpose, organization, thesis/paragraph FUNCTION, reader orientation, transitions, clarity, coherence, rhetorical function, interpreting the student's OWN evidence) and must NEVER supply substantive CONTENT (additional arguments, reasons, examples, evidence, stronger/new claims, stakes, emotional appeals, policy suggestions). Enforced purely in the reasoner system prompt — NO engine/instruction-layer/UI/domain changes.
- Added WRITING INSTRUCTION BOUNDARY + ANTI-COAUTHORING rule + per-turn SELF-CHECK to `server.py` prompt (after STUDENT-FACING INVITATION RULES, ~lines 266-272). Added two intervention output fields: `focus` ('writing'|'content') and `writing_not_content_check` (self-audit). CONTENT MODE is a narrow exception: allowed only when the teacher explicitly enables brainstorming (assignment/purpose/teacher_notes) or the student explicitly requests idea generation.
- Eval: 30 cases across argumentative/narrative/informative/analytical (`tests/milestone5a_boundary_eval.py` → `milestone5a_results.json`; review `test_reports/milestone5a_review.md`) — **30/30 boundary PASS**; 0 content-coaching red-flag phrases; focus='writing' on all 29 instructional cases; focus='content' correctly only on the 1 teacher-enabled brainstorming case (ownership kept with student). Before/after example: three-reason thesis no longer prompted with "what would make it stronger" (content) but with thesis-function questions (writing).
- Testing agent iteration_10: backend M1–M5 regression PASS (7/7 clean second run; 1 first-run SSE-parse flake passed on retry — test-client artifact, not a product bug). No critical issues; anti-coauthoring holds; focus/writing_not_content_check populate correctly; brainstorming exception works. Latency ~30–41s/turn, no 502s.

## Milestone 6 — Communicative Purpose Framework (2026-06)
- The AI now infers the writing's COMMUNICATIVE PURPOSE before evaluating it, and interprets all instruction relative to that purpose. Purposes (persuading, informing, explaining, interpreting, analyzing, narrating, reflecting, evaluating, comparing, proposing) are treated as communicative FUNCTIONS, not rigid genres/"essay types". Assignments may hold MULTIPLE purposes (primary + secondary).
- Added `CommunicativePurpose` model (primary, secondary[], inferred_from, uncertainty) as `DevelopmentalTheory.communicative_purpose`; included in the compact-theory projection (persists across turns) and the JSON output schema. Added the COMMUNICATIVE PURPOSE FRAMEWORK block to the reasoner prompt: infer purpose from assignment/teacher/student description/writing; ask a brief clarifying question only when purpose is genuinely unclear AND changes guidance; teach concepts as purpose-dependent functions (thesis/intro/organization/evidence/etc.); organization patterns are functional, never templates; purpose-first reasoning coordinates with (does not replace) the M5A boundary. NO engine/instruction-layer/UI redesign — one theory field + prompt.
- Dev Panel now surfaces a Communicative Purpose block plus intervention focus + writing_not_content_check.
- Eval: 30 cases across persuasive/explanatory/informative/analytical/narrative/reflective + evaluative/comparative/proposal + 3 mixed-purpose (`tests/milestone6_purpose_eval.py` → `milestone6_results.json`; review `test_reports/milestone6_review.md`) — **30/30 PASS**: purpose inferred correctly every time; mixed cases hold ≥2 purposes; 0 rigid-template phrases; focus='writing' on all. Purpose-adaptive teaching confirmed (analysis→interpretation, narrative→meaning, reflection→honors uncertainty).
- Testing agent iteration_11: backend 8/8 + frontend 6/6, no critical issues, no M1–M5A regression. Verified the ?dev test-bypass, New Test reset, quick-test-start link, and the Dev Panel purpose block render. Latency ~30–45s/turn, no 502s.
- Also (dev-experience): added a test bypass — `?dev` URL auto-starts a preset session into the Student Workspace; "New Test" button resets to a fresh session; "Skip setup — quick test start" link on Teacher Setup. Preset lives in App.js (TEST_PRESET).

## Milestone 7 — Functional Paragraph Development (2026-06)
- When a paragraph is the unit under discussion, the AI now identifies the paragraph's communicative PURPOSE (relative to the M6 whole-piece purpose), evaluates functional coherence (do sentences work together toward one controlling purpose — not just transitions) and development (explanation/description/interpretation/illustration/comparison/clarification/evidence/reflection as RESOURCES, not required components), and its role/placement in the whole. NO rigid paragraph template imposed.
- Added `ParagraphFunction` model (applies, purpose, contribution_to_whole, coherence, development, placement) as `DevelopmentalTheory.paragraph_function`; included in compact-theory projection + JSON output schema. Added the FUNCTIONAL PARAGRAPH FRAMEWORK block to the reasoner prompt (after the M6 framework). Applies-reliability fix: whenever the current draft IS a paragraph, applies MUST be true and fields completed even when the paragraph is strong. Dev Panel shows a Paragraph Function block when applies=true. NO engine/instruction-layer/UI redesign; coordinates with M6 (purpose) and M5A (boundary).
- Eval: 30 cases (effective / weakly-organized / competing-purposes / incoherent / weak-development + narrative/explanatory/analytical/argumentative/reflective/comparison/bridge/concession) (`tests/milestone7_paragraph_eval.py` → `milestone7_results.json`; review `test_reports/milestone7_review.md`) — **30/30 PASS** (initial 28/30; fixed the applies miss on strong paragraphs, re-verified via `milestone7_recheck.py`; the brainstorm-no-draft case correctly keeps applies=false + focus=content). 0 rigid-template phrases, 0 content-coaching phrases, focus='writing' on all instructional cases.
- Testing agent iteration_12: backend 6/6 + frontend 5/5, no critical issues, no M1–M6 regression. Strong paragraph honored (invite_only), weak paragraph diagnosed for coherence, narrative paragraph read relative to narrate purpose; Dev Panel paragraph-function-block renders. Latency ~35–46s/turn, no 502s.

## Milestone 8 — Functional Evidence and Support (2026-06)
- Evidence is now treated as a communicative RESOURCE read relative to the whole's purpose (M6), the paragraph's purpose (M7), and the claim/interpretation/explanation/experience it supports. The AI distinguishes evidence FROM interpretation, teaches WHY evidence matters (relevance/interpretation/connection/significance) rather than "add more evidence", never counts pieces or imposes rigid rules, and never invents evidence/examples/statistics/quotations (M5A) unless brainstorming is enabled.
- Added `EvidenceFunction` model (applies, forms[], function, interpretation_gap, quality) as `DevelopmentalTheory.evidence_function`; included in compact-theory + JSON output schema. Added the FUNCTIONAL EVIDENCE FRAMEWORK block to the reasoner prompt (after the M7 framework). Recognition fix: embedded support (process descriptions in explanation; sensory details/actions/dialogue in narrative; memories/experiences in reflection) counts as evidence and sets applies=true even when not formally cited. Dev Panel shows an Evidence Function block when applies=true. NO engine/instruction-layer/UI redesign.
- Eval: 30 cases (strong-ev/weak-interp, weak-ev/strong-interp, persuasive, explanatory, analytical, narrative, reflective, missing-evidence, irrelevant-evidence, unsupported-claim, brainstorm) (`tests/milestone8_evidence_eval.py` → `milestone8_results.json`; review `test_reports/milestone8_review.md`) — **30/30 PASS** (initial 27/30; fixed under-recognition of embedded process/experiential support, re-verified via `milestone8_recheck.py`). 0 counting/rigid-rule phrases, 0 invented-evidence phrases, focus='writing' on all instructional cases.
- Testing agent iteration_13: backend 8/8 + frontend 5/5, no critical issues, no M1–M7 regression. Strong-evidence draft targeted the interpretation gap (not more evidence); embedded process/reflective support recognized; Dev Panel evidence-function-block renders. Latency ~35–54s/turn, no 502s.

## Milestone 9 — Functional Transitions and Coherence (2026-06)
- Transitions are now treated as the communication of RELATIONSHIPS among ideas, not connective vocabulary. The AI identifies the intended relationship (sequence/cause-effect/comparison/contrast/elaboration/concession/etc.) BEFORE the language, treats transition words as one of many resources (repetition, conceptual links, parallel structure, pronoun reference, progression...), evaluates coherence at four levels (sentence-to-sentence / paragraph-level / paragraph-to-paragraph / whole-piece), imposes no rigid transition rules / never says "add transition words", and never invents ideas/content or rewrites paragraphs (M5A) unless brainstorming is enabled.
- Added `CoherenceFunction` model (applies, intended_relationship, level, resources_in_use[], reader_can_follow) as `DevelopmentalTheory.coherence_function`; included in compact-theory + JSON output schema. Added the FUNCTIONAL TRANSITION & COHERENCE FRAMEWORK block to the reasoner prompt (after the M8 framework). Strong-flow applies fix: already-coherent drafts also set applies=true (naming the achieved relationship is the analysis). Dev Panel shows a Transitions & Coherence block when applies=true. NO engine/instruction-layer/UI redesign.
- Eval: 30 cases (strong-flow-few-words, many-words-weak-coherence, weak-sentence-to-sentence, weak-paragraph-to-paragraph, persuasive/explanatory/analytical/narrative/reflective, mixed, brainstorm) (`tests/milestone9_coherence_eval.py` → `milestone9_results.json`; review `test_reports/milestone9_review.md`) — **30/30 PASS** (initial 28/30; fixed strong-flow under-recognition, re-verified via `milestone9_recheck.py`). 0 'add transition words'/rigid-rule phrases, 0 content-coaching phrases, focus='writing' on all instructional cases.
- Testing agent iteration_14: backend 8/8 + frontend 5/5, no critical issues, no M1–M8 regression. Strong flow honored (no add-words), many-words-weak-coherence seen through, levels identified; Dev Panel coherence-function-block renders. Latency ~40–52s/turn, no 502s.

## Milestone 10 — Functional Conclusions (2026-06)
- Conclusions are now treated as communicative COMPLETION, not summary. The AI asks what work the conclusion must do for the reader BEFORE evaluating how it's written, adapts to purpose, distinguishes completion from merely stopping / only summarizing / introducing a new idea, and NEVER prescribes formulaic closings ('In conclusion', restate thesis, summarize all points). Never invents arguments/evidence/examples or rewrites the conclusion (M5A) unless brainstorming is enabled.
- Added `ConclusionFunction` model (applies, functions_in_play[], completes_purpose, relationship_to_opening, final_understanding) as `DevelopmentalTheory.conclusion_function`; included in compact-theory + JSON output schema. Added the FUNCTIONAL CONCLUSION FRAMEWORK block to the reasoner prompt (after the M9 framework). Dev Panel shows a Conclusion/Completion block when applies=true. NO engine/instruction-layer/UI redesign.
- Eval: 30 cases (effective, merely-stops, only-summarizes, persuasive/explanatory/analytical/narrative/reflective, introduces-new-idea, mixed, brainstorm) (`tests/milestone10_conclusion_eval.py` → `milestone10_results.json`; review `test_reports/milestone10_review.md`) — **30/30 PASS on first run** (no fix needed; applies-reliability lesson from M7–M9 baked into the prompt). 0 formulaic-rule phrases, 0 content-coaching phrases, focus='writing' on all instructional cases.
- Testing agent iteration_15: backend 8/8 (first run, zero retries) + frontend 7/7, no critical issues, no M1–M9 regression. Effective conclusion honored, merely-stops/only-summarizes diagnosed as non-completion, no restate-thesis advice; Dev Panel conclusion-function-block renders. Latency ~32–53s/turn, no 502s.

## Milestone 11 — Recursive Developmental Scaffolding Controller (2026-06)
- Added the master ORCHESTRATION meta-layer over the M6–M10 lenses. Every turn runs one bounded cycle: identify unit → confirm purpose → diagnose opportunities across frameworks → prioritize EXACTLY ONE target (postpone the rest) → choose an instructional mode → evaluate → consolidate → return control. Stopping rules honored; never teaches multiple major concepts at once; no endless recursion.
- Added `ScaffoldingControl` model (current_unit, diagnosed_opportunities[], primary_target, prioritization_rationale, instructional_mode, postponed[], cycle_status, stopping_reason, future_opportunity) as `DevelopmentalTheory.scaffolding_control`; included in compact-theory + JSON output schema. Added the RECURSIVE DEVELOPMENTAL SCAFFOLDING CONTROLLER block to the reasoner prompt (after the M10 framework) with MANDATORY stopping rules: explicit independence requests → stop + consolidate + hand back; absorbed target across revisions → consolidate rather than re-question. Dev Panel shows a Scaffolding Controller block (always). Governs sequencing only; never overrides the one-invitation rule or M5A boundary. NO engine/instruction-layer/UI/framework redesign.
- Eval: 30 cases (multiple-weaknesses, needs-explanation, needs-questioning, revision-consolidation, strong/weak drafts, requests-independence, unit-size, repeated-revision multi-turn, brainstorm) (`tests/milestone11_scaffolding_eval.py` → `milestone11_results.json`; review `test_reports/milestone11_review.md`) — **30/30 PASS** (initial 23/30; 4 were harness over-expectations on strong paragraphs, 3 genuine stopping-rule misses fixed + re-verified via `milestone11_recheck.py`). One target per cycle with others postponed; mode adapts; stopping/consolidation honored; no recursion; focus='writing' preserved.
- Testing agent iteration_16: backend 10/10 (first run, zero retries) + frontend 6/6, no critical issues, no M1–M10 regression. scaffolding_control populated every turn (single target + valid mode + valid cycle_status); multi-weakness → one target + postponed; independence request stops the cycle; Dev Panel scaffolding-control-block renders. Latency ~35–95s/turn (multi-turn longest), no 502s.

## Milestone 12 — Reader Construction Framework (2026-06)
- The AI now continually models a reasonable NAÏVE reader (who knows only what the text has said): what they understand now and what they'd need next. It identifies likely misunderstandings, distinguishes reasonable inference from unsupported assumption, and frames elaboration/precision through reader UNDERSTANDING (not word-count or grammar). This reader model INFORMS all frameworks; the M11 controller still selects ONE target.
- Added `ReaderConstruction` model with the specified fields (applies, reader_understanding, likely_reader_questions[], assumed_knowledge, clarification_needed, elaboration_needed, precision_risk, next_reader_need) as `DevelopmentalTheory.reader_construction`; included in compact-theory + JSON output schema. Added the READER CONSTRUCTION FRAMEWORK block to the reasoner prompt (after the M11 controller). Dev Panel shows a Reader Construction block when applies=true. NO engine/instruction-layer/UI/framework redesign; honors M5A boundary.
- Eval: 30 cases (clear, hidden-assumption, missing-background, inferential-gap, abrupt-shift, excessive/insufficient-elaboration, ambiguous, precise, strong-guidance, evolving multi-turn) (`tests/milestone12_reader_eval.py` → `milestone12_results.json`; review `test_reports/milestone12_review.md`) — **30/30 PASS on first run** (no fix needed). Reader model built whenever text exists; misunderstandings flagged; precision framed as shared understanding (0 grammar phrases); M11 kept one target; reader model evolved across turns; focus='writing'.
- Testing agent iteration_17: backend 7/7 + frontend 9/9, no critical issues, no M1–M11 regression (the two first-run failures were test-side harness bugs — theory_history shape + transient empty-done SSE — not app bugs; no app code changes). Dev Panel reader-construction-block renders. Latency ~45–110s/turn, no 502s.

## Milestone 13 — Revision as Development (2026-06)
- When a current draft can be compared to a prior draft, the AI now reads the revision as DEVELOPMENTAL change (how the student's ability to communicate changed), not editing. It names ONE primary_growth qualitatively (no edit-counting/scorekeeping), issues a transfer_message for future writing on real growth, names a single remaining_opportunity on limited/regressed progress, reads trajectory across 3+ drafts, and never rewrites/invents content (M5A). The M11 controller still selects ONE target. FIRST submissions do not apply the lens (nothing to compare).
- Added `RevisionDevelopment` model (applies, development_detected, primary_growth, communication_change, reader_change, remaining_opportunity, transfer_message) as `DevelopmentalTheory.revision_development`; included in compact-theory + JSON output schema. Added the REVISION AS DEVELOPMENT FRAMEWORK block to the reasoner prompt (after the M12 framework). Dev Panel shows a Revision as Development block when applies=true. NO engine/instruction-layer/UI/framework redesign.
- Eval: 30 cases (substantial-growth, superficial-edit, regression, multi-draft trajectory, reader-understanding, coherence, conclusion, purpose, limited-progress, first-submission control) (`tests/milestone13_revision_eval.py` → `milestone13_results.json`; review `test_reports/milestone13_review.md`) — **30/30 PASS on first run**. Growth→primary_growth+transfer_message; superficial/regression→remaining_opportunity with 0 edit-counting; first submissions applies=false; M11 kept one target; focus='writing'.
- Testing agent iteration_18: backend 4/4 (focused, external URL) — applies false-on-first / true-after-revise, growth→transfer_message, superficial→remaining_opportunity+no edit-counting, M11 one target, M5A focus='writing', theory_history preserved, M6/M12 fields intact. No critical issues (1 transient empty-done SSE frame passed on rerun). Frontend: Dev Panel JSX verified rendering (app compiles/renders cleanly; revision-development-block follows the verified M7–M12 conditional-block pattern reading the confirmed-populated field). Revision turns ~50–175s; no 502s.

## Milestone 14 — Developmental Integration & Calibration (2026-06, VERIFIED / COMPLETE)
- Final coherence meta-check over M6–M13 lenses and the M11 controller. Every turn the engine records `theory.integration_calibration` (applies=true always): one `primary_framework` (aligned with `scaffolding_control.primary_target`), `supporting_frameworks[]` that REINFORCE (never compete), a `calibration_check` (intervention PROPORTIONAL to real need — guards over-/under-teaching, unnecessary intervention, unmotivated target-shifting), a `consistency_check` (equivalent situations get equivalent priorities), and `integration_notes` (how overlapping diagnoses were unified into ONE focus + any cross-framework transfer). Governs coherence/calibration only; never overrides the one-invitation rule or the M5A boundary. NO engine/instruction-layer/UI/framework redesign — one theory field + prompt block + Dev Panel block.
- Implementation (pre-existing, unchanged this session): `server.py` `IntegrationCalibration` model (~L227), theory field (~L251), M14 prompt block (~L444-450), JSON output schema (~L510), serialization (~L571/L879). `DevelopmentPanel.jsx` `integration-calibration-block` (~L251-278) renders all 5 subfields.
- Eval: 30 cases (multi-framework, conflicting-priority, similar-pair consistency, strong, weak, repeated-revision 3-draft, cross-transfer) (`tests/milestone14_integration_eval.py` → `milestone14_results.json` + `milestone14_consistency.json`; review `test_reports/milestone14_review.md`) — **30/30 PASS on first run, no fixes needed**. Strong drafts → light interventions (interpretation_only/invite_only); weak → single high-leverage target (no pile-on); revisions → consolidate + transfer; 0 multi-ask flags; focus='writing' on all. Identical-genre consistency pairs matched; cross-genre pairs reasonably differ (not a defect).
- Testing agent iteration_19: writing-turn backend acceptance PASS (all 5 M14 subfields populated + primary_framework aligned with M11 primary_target) + all M1–M13 regression fields green (M11 one target + valid mode/cycle_status; M12 reader_construction.applies=true; M13 applies=false on first submission; M5A focus='writing'; no student-text echo; single-ask invitation). Frontend 100% — ?dev bypass → Student Workspace → AI invitation → Dev Panel integration-calibration-block renders all 5 subfields. NO app-code changes required. M14 COMPLETE.
- KNOWN INFRA RISK (NOT an M14 defect, NOT fixed — awaiting user decision): a ~60s ingress SSE ceiling. Revise turns whose LLM time lands in ~55–65s get truncated at the client before the `event: done` frame; the client disconnect cancels the async generator, so `db.sessions.update_one` never fires and the turn is not persisted (RCA in iteration_19). The 30-case eval hits localhost:8001 directly (bypassing the ingress) so it is unaffected. Candidate fixes: (a) raise ingress SSE idle/total timeout to ≥120–180s for `/api/sessions/*/interact`, or (b) decouple the reasoner run + persistence from the SSE stream (run in background, persist on completion, client tails/polls) so a client disconnect never loses completed LLM work. Also carry-over: POST /api/sessions returns 200 not 201.

## Infrastructure Phase 1 — Durable Revision Processing (2026-06, DONE)
- Resolved the ~60s ingress SSE ceiling + durability gap from M14 (iteration_19). Reasoning no longer runs inside an SSE stream. NEW transport (NO instructional/framework/engine/UI-content change — Milestones 1–14 behaviorally unchanged):
  - `Turn` model gained `status` (complete | processing | failed | cancelled).
  - `POST /api/sessions/{id}/interact` now returns Session JSON in <0.2s: it appends the student turn (complete) + a placeholder AI turn (status='processing', kind='pending', empty), persists immediately, then launches `asyncio.create_task(_run_reasoning(...))`. A 409 guard rejects a second interact while a turn is 'processing' (no duplicate turns). Empty submission → 400.
  - `_run_engine(session, req)` = STAGE A domain selection + STAGE B reasoning (non-streaming, one retry). `_run_reasoning(session_id, ai_turn_id, req)` background task: reloads the session, reasons against a view that EXCLUDES the placeholder turn (so `_previous_draft` / history behave exactly as before), reloads fresh, then `_finalize_turn` fills the placeholder (content + status='complete') and updates telos/theory/theory_history/interactions — or on error sets the turn status='failed'. DB is the source of truth; client disconnect never interrupts the task (decoupled from the request lifecycle).
  - Startup `recover_orphaned_turns` marks any 'processing' turns 'failed' on boot (server restart safety net).
  - Frontend: `api.js` interact() is a plain axios POST (no SSE). `App.js` derives `isProcessing` from turn statuses and polls `GET /sessions/{id}` every 2.5s while processing (updates state, toasts once on a failed turn via a Ref); reconnection is emergent — initial load setSession()'s the doc, which re-triggers polling if a processing turn exists. `StudentWorkspace.jsx` hides processing/failed AI turns from the thread (thinking dots instead); `started` derived from any student turn; buttons disable while `submitting || isProcessing`.
- Verified: main-agent curl (immediate return + processing turn, 409 guard, background completion ~48s, 80s no-client-connection durability with no lost work / no duplicates, startup orphan recovery processing→failed, resubmit-when-idle). Testing agent iteration_20: backend 8/8 (100%, 212s) + frontend 100% incl. RELOAD-mid-processing reconnection (polling resumes, invitation appears automatically, no duplicate turns). M1–M14 regression intact (M5A focus='writing', M11 primary_target, M12 reader_construction, M13 applies false→true across revise, M14 integration_calibration applies + primary_framework). No critical issues. Carry-over: POST /sessions returns 200 not 201.

## UI Phase 2 — Development Panel Organization (2026-06, DONE)
- Reorganized the Dev Panel for teacher-facing readability. FRONTEND-ONLY (`DevelopmentPanel.jsx` full rewrite); NO change to reasoning, theory objects, engine, prompts, background processing, persistence, polling, or API. All data fields + data-testids preserved.
- New order: (1) Scaffolding Controller (top, amber accent, expanded), (2) Integration & Calibration (sky accent, expanded when applies), (3) Primary Active Framework group, (4) Other Applicable Frameworks group, (5) Inactive Frameworks group, (6) Instructional Output (intervention + selected invitation), + collapsible "Working Theory Detail" holding all remaining reasoning (telos, organization, movement, domains, tensions, cultural resources, evidence lists, alt interpretations, uncertainty, reorganizations, candidate invitations, changes, observed reorganization).
- Framework blocks are now keyboard-accessible collapsible accordions (button + aria-expanded + aria-controls + focus ring; content removed from DOM on collapse). Status shown by icon+text badge (Primary/Supporting/Postponed/Applicable/Not applicable) — never color alone. `frameworkStatuses()`/`pickPrimary()` derive status from `integration_calibration.primary_framework` (earliest-keyword-index match), `.supporting_frameworks`, and `scaffolding_control.postponed`; `applies=false` → Inactive. Default expansion: primary + supporting expand; inactive/postponed/plain-applicable collapse.
- Polling stability: `openMap` re-derives to defaults ONLY when `interactions.length` (completed-turn count) or `session.id` changes — never on the 2.5s poll — so manual open/close is preserved while viewing the same completed turn; a new completed turn re-applies defaults once. Panel shows the last COMPLETED turn's reasoning during processing (never blank/partial) + a `panel-processing-indicator`.
- Verified: testing agent iteration_21 — all 11 acceptance items PASS (frontend 100%): order, primary auto-expand, inactive collapsed, mouse + keyboard toggle, polling-stability (idle + active), new-turn default reset, reload restore, no data loss, zero duplicate panels. Backend durable-processing smoke 100% (POST 0.10s, 409 guard, complete ~54s, M5A/M11/M14 fields intact). No engine/API/persistence changes. Note: "Other Applicable Frameworks" group not exercised by current sessions (only one framework applied) — code handles it; verify when a multi-applies session arises. Carry-over: POST /sessions returns 200 not 201.

## Phase III — Authentic Instructional Testing & Calibration (2026-06, DONE — observation only)
- Evaluation phase; NO architecture/engine/instruction-layer/UI/M1–M14 changes. Ran 9 authentic multi-turn sessions (24 draft→instruction→revision cycles) across weak/avg/strong writers, middle-school→adult, genres persuade/inform/explain/analyze/narrate/reflect/compare, plus adversarial cases (near-perfect, ignores advice, abandoned draft, purpose shift, multi-framework). Harness `backend/tests/phase3_authentic_eval.py`; transcripts `test_reports/phase3_transcripts.json`; full report `test_reports/phase3_calibration_report.md`.
- Result: system behaves like a restrained expert teacher. ONE high-leverage target/turn 24/24; author ownership preserved (focus='writing' 24/24, no rewrites/no invented content); purpose-adaptive incl. tracked purpose shift (P6 compare→persuade); genre-honoring (narrative/reflection not forced into thesis); proportional calibration (strong→invite_only/consolidate, weak→one foundational target, P9 many-issues unified without pile-on); growth recognition + consolidation + natural transfer messages; Dev-Panel reasoning legible for teachers.
- Calibration log (evidence-based, gated by change policy — NOTHING changed): F1 [MEDIUM] on strong self-contained short pieces the engine assumes an essay/body-paragraph frame and asked "what job must the first body paragraph do?" 3× (P5) with cycle_status=continue and no stopping/scope move — candidate for a small M11/M14 calibration nudge, needs one more cross-session confirmation. F2 [LOW] no scaffold-variation when a student ignores/abandons (P8) — defensible, watch only. F3 [LOW/cosmetic] revision_development.applies=true on a full topic switch (growth correctly left empty). Non-findings: no pile-on, no over-teaching strong writing, no formulaic templates, no coauthoring.
- Prioritized (for review, NOT implemented): 1) F1 scope/stopping calibration (confirm then minimal nudge); 2) F2 non-engagement scaffold variation; 3) F3 revision-lens semantics. Model treated as STABLE. STOP — no new instructional domains until findings reviewed.

## Phase IIIA — Targeted Calibration Verification of F1 (2026-06, DONE — observation only; F1 CLOSED)
- Verified whether F1 (engine assumes essay/body-paragraph scope on a complete self-contained paragraph) is genuine or a P5 artifact. Ran 10 authentic strong-writer single-paragraph sessions (12 turns) across all genres; 5 explicitly "one self-contained paragraph, not an essay", 2 no scope hint, 1 mild, 2 with a strong revision. Harness `backend/tests/phase3a_scope_eval.py`; transcripts `test_reports/phase3a_scope_transcripts.json`; report `test_reports/phase3a_verification_report.md`.
- Result: **F1 recurrence 0/10 sessions (0/12 turns)** — no essay-scope assumption anywhere. Engine kept targets WITHIN the paragraph (evidence-interpretation gap, implicit claim, center of gravity, evidence grounding), explicitly acknowledged completeness ("before you consider this finished"), and produced explicit stopping moves in 3/10 (cycle_status=consolidate_and_return + stopping_reason). focus='writing' 12/12. P5's original framing was a reasonable reading of an essay-assignment intro, not a systematic failure.
- Recommendation: **F1 CLOSED — no architecture/prompt/reasoning/calibration/UI changes.** Model remains STABLE. F2/F3 remain LOW/cosmetic, not actioned. No new instructional domains pending review.

## UI Enhancement — Assignment Prompt + Teacher-facing Panel Language (2026-06, DONE)
- UI-only; NO instructional/engine/prompt/reasoning changes.
- (1) Assignment Prompt: optional teacher field (`assignment_prompt`) added to SessionCreate/Session (display-only passthrough, NOT fed to `_build_prompt`); TeacherSetup adds an optional multiline field; StudentWorkspace shows it persistently (data-testid `assignment-prompt`, label "PROMPT — THE QUESTION YOU'RE ANSWERING") to reduce goal drift; TEST_PRESET includes it.
- (2) Teacher Panel language: DevelopmentPanel defaults to teacher labels (Current Developmental Focus / Why This Focus / Student Growth / Reader Understanding / Focus this turn / Why this focus? / Postponed opportunities / What the Coach Said, etc.) and hides internal theory names; a `toggle-research-view` button reveals internal names (Scaffolding Controller M11, Integration & Calibration M14, M-tagged framework titles) + the "Working Theory Detail" internals accordion. Implemented via `t(teacher,research)` helper, `FW_TEACHER` map, `STATUS_META.tlabel` badges, and research-gated detail. Added `humanizeFramework()` — teacher-view-only presentational sanitizer that strips M-tags/internal framework names from LLM-emitted data strings (primary_framework, supporting_frameworks, primary_target, diagnosed_opportunities, postponed) while preserving descriptive content; research view shows raw values.
- Verified: testing agent iteration_22 — 100% backend + 100% frontend (setup field optional, persistent prompt box, backend passthrough + not-in-engine, teacher/research toggle reversible, testids unchanged, durable-processing regression intact). humanizeFramework added post-report; verified by unit output + clean compile (research view untouched).

## Instructional-Object Knowledge Layer + Governed Canonical Instruction (2026-07, DONE)
- Revised (not rebuilt) the engine so instruction is GOVERNED by explicit canonical writing knowledge while the student's thinking still leads the conversation. Preserved inside-out pedagogy, one-target/turn, and the M5A anti-coauthoring boundary.
- NEW knowledge layer: `backend/instructional_objects.json` — 34 domain-tagged Instructional Objects parsed from the uploaded Writing Elements Chart (element, definition, communicative_purpose, performance_structure, recognition_diagnostics, common_obstacles, next_developmental_moves, developmental_resources[], indicators_of_control) + a shared_developmental_resources menu. General/extensible schema (domain field) so reading/reasoning/etc. can be added later without redesign. Terminology: "Instructional Objects" (writing is the first domain).
- Retrieval: STAGE-A selector now also returns 1-3 relevant instructional-object element names (same LLM call, no added latency); `_select_relevant_domains` -> (domains, io_names); `_build_prompt` injects only the retrieved full objects + the resource menu. `get_relevant_instructional_objects` resolves by element name + aliases (exact-then-substring). The AI reasons FROM these objects, not the raw doc.
- Governed instruction (SYSTEM_MESSAGE "GOVERNED CANONICAL INSTRUCTION" section): 14-step turn reasoning; recorded in NEW `theory.instructional_reasoning` (12 fields: current_unit_of_writing, active_instructional_element, element_communicative_purpose, student_current_organization, canonical_performance_structure, primary_developmental_tension, next_student_act, selected_developmental_resources, resource_selection_rationale, evidence_of_developmental_movement, degree_of_student_control, continue_consolidate_release_or_shift). Behavior rules: TEACH the concept (canonical term + plain definition + tie to the student's writing), do NOT rely only on open questions; ANSWER-THE-ASSIGNMENT check (notice drift, reorient to the visible prompt); require the STUDENT to perform ONE next act; never rewrite/supply finished text; response shape A-E (Recognition/Instruction/Reorientation/Supported performance/Consolidation), varied not mechanical; developmental direction scaffolded->increasing control->independence.
- Dev Panel: new open-by-default `instructional-reasoning-block` accordion (12 fields) with teacher/research labels; existing blocks + research toggle intact.
- Persistence fix: teacher assignment/purpose/task persist via session id across reloads; `?dev` auto-quickstart is gated so it never silently restores the sample after a teacher sets up a new assignment (sessionStorage `dws_manual_setup`); explicit `reset-to-sample-button` and `new-assignment-button` controls. Assignment Prompt stays visible above the writing area.
- Verified: testing agent iteration_23 — backend 100% (16/16), frontend 100% (dev panel, persistence, reset controls, all on preview URL). All 10 success criteria met. Test: `backend/tests/test_instructional_reasoning_iteration23.py`. Carry-over minor (not blocking): POST /sessions returns 200 not 201; integration_calibration.supporting_frameworks KV values not humanized in teacher view (iteration_22 carry-over).

## Instructional Networks + Developmental Memory (2026-07, DONE — engine only, no student-facing UI change)
- (1) INSTRUCTIONAL NETWORKS: every Instructional Object in `instructional_objects.json` now carries `related_elements` (conceptual edges; e.g. Thesis↳Purpose/Central Claim/Introduction/Reader/Organization). `build_instructional_network()` returns compact neighbor summaries ({element, communicative_purpose, relates_to_targets}) for the retrieved target object(s); `_build_prompt` injects an INSTRUCTIONAL NETWORK block. The engine reasons WITH the network (coherence, sequencing, concept integration, prerequisite vs postponed) but still selects exactly ONE instructional target per turn (M11 unchanged).
- (2) DEVELOPMENTAL MEMORY (not chat memory): `DevelopmentalObservation` {element, control_statement, trend(confused|emerging|developing|consolidating|independent), evidence, episodes, updated_at}; `Session.developmental_profile`. Each completed turn the engine emits `developmental_profile_update` (1-3 obs), merged by `_merge_developmental_profile` (matching element → refresh + episodes++, else append; no null-clobber). The profile is injected into every prompt (`_developmental_profile_summary`) so the engine scaffolds from the student's current developmental organization, persisted in `_run_reasoning`, and shown in a new open-by-default Dev-Panel `developmental-memory-block` (per-item `dev-memory-item-N`). Not exposed to students.
- Verified: testing agent iteration_24 — backend 100% (10/10, real 2-turn session: turn1 seeded [Central Claim: confused], turn2 merged in place [Central Claim: emerging, episodes=2] + appended [Evidence: confused], evidence_of_developmental_movement substantive, profile persists via GET), frontend 100% (developmental-memory-block renders matching backend, existing panel blocks + research toggle intact, student workspace unchanged). Regression intact: durable processing, one-target/turn, intervention.focus='writing', governed instruction. Test: `backend/tests/test_developmental_memory_iteration24.py`. Carry-over minors only (POST /sessions 200 not 201; M-numbers in supporting_frameworks values).

## Automated Instructional Testing & Export System (2026-07, DONE — DEVELOPER ONLY)
- Built a developer-only harness that runs each canonical test case through the REAL production pipeline (STAGE-A retrieval → instructional networks → one-target selection → governed instruction → anti-coauthoring → developmental memory) and grades the engine's ACTUAL decisions with a SEPARATE LLM evaluator call (claude-sonnet-4-6). No fake logic; hidden from the student/teacher experience.
- Test cases: `backend/test_cases/instructional_test_cases.json` — 32 diverse cases (TC01–TC32): assignment misreads, thesis (missing/topic/vague/adequate/strong-do-not-over-teach), evidence↔explanation, paragraph unity/topic-sentence, organization/transitions, intro/conclusion, sentence/pronoun/tone, circular reasoning, resistance, "write it for me" (anti-coauthoring), minimal responses, terminology confusion, success→consolidate, repeated-scaffold, returning-student-with-profile (TC30 seeds `initial_profile`), nuanced-both-sides, reflective-no-thesis.
- Backend (`server.py`, before `app.include_router`): `_harness_run_turn` mirrors the production interact+_run_reasoning flow exactly and captures decisions + latency + reasoner_prompt_bytes (via new `result['_meta']` on `_run_engine`); `_evaluate_case` LLM-grades on 3 criteria (target_alignment / constraint_compliance / one_target_and_authorship) → overall pass|partial|fail; `_run_test_suite` runs cases sequentially in an `asyncio` background task, persisting incrementally to Mongo `test_runs`. Endpoints: `GET /api/tests/cases`, `POST /api/tests/run` ({case_ids:[..]|null} → run-all), `GET /api/tests/runs`, `GET /api/tests/runs/{id}`, `GET /api/tests/runs/{id}/export?format=json|markdown`, `GET /api/sessions/{id}/export?format=json|markdown`.
- Frontend: new `?tests` route → `TestHarness.jsx` (case list + checkboxes, Run Selected / Run All, 3s polling while running, summary stats + pass-rate, expandable per-case result cards with evaluator criteria + per-turn instructional decisions, JSON/Markdown export links, Past Runs sidebar that loads a prior run without re-running). `App.js` returns `<TestHarness/>` when the `tests` query param is present else `<StudioApp/>` (clean hook order). Dev Panel header gained `export-session-json` / `export-session-md` audit anchors.
- Verified: testing agent iteration_25 — backend 100% (all harness + export endpoints correct status/content-type/payload; live single-case TC18 run pass_rate=100%), frontend 100% (?tests page 32 cases, live single-case run → result card expand, export links, past-runs load-without-rerun, ?dev dev-panel export links 200 JSON+MD, no-query regression to Teacher Setup, no console errors). Carry-over minor only: POST /sessions returns 200 not 201. NOTE: a full "Run All" (32 cases) is ~45 min of real LLM calls — run subsets for quick checks.

## Evaluator Calibration to Compass Fidelity (2026-07, DONE — evaluator only, engine FROZEN)
- Rewrote ONLY the automated evaluator (`EVAL_SYSTEM_MESSAGE` + `_eval_prompt` in server.py) to judge Compass by its OWN instructional philosophy via 10 principles: P1 fidelity to Compass (not generic teaching), P2 scaffolding≠coauthoring (sentence starters/revision prompts OK; coauthoring = supplying finished/copyable content or single prescribed form), P3 build from developmental profile, P4 canonical forms guide, P5 communicative purpose first (genres as resources), P6 development through guided action, P7 respect competent performance (no invented deficiencies), P8 consolidate-by-restating-the-principle before advancing, P9 distinguish violation vs acceptable alternative vs stylistic preference (only violations lower the verdict), P10 evidence-based (quote + principle + why-not-preference). New 4-criterion rubric (compass_fidelity / authorship_preserved / developmentally_appropriate / consolidation_and_alternatives) each with a classification field; developmental profile now passed into the eval prompt. Engine, prompts, and the 32 cases byte-for-byte unchanged (verified via git diff: only lines ~1207-1310 changed).
- Re-ran full suite (run `85624a68`) vs baseline (run `2598c428`): pass rate 71.9% → 75.0%; **fail 1 → 0** (TC21 was an evaluator over-reach, now pass), pass 23→24, partial 8→8. Improved: TC03, TC21, TC28, TC32 (old evaluator wrongly penalized legitimate scaffolding/genre/audience moves). Newly-flagged: TC06, TC13, TC14 (P8 consolidation misses + one target-shift). Still ambiguous (5): TC07, TC18, TC29, TC30, TC31.
- Residual ENGINE weaknesses now surfaced fairly (for the future engine phase, still deferred): W-A consolidation-by-principle missing (dominant, 6 cases) — engine praises success but doesn't restate the transferable rule (P8); W-B under-firing restraint on strong/nuanced theses (P7); W-C occasional copyable-thesis slip under repeated scaffolding (TC29, P2); W-D teacher-purpose adherence on organization (TC14). Evaluator residue noted: TC29's P2 slip was classed 'violation' but graded 'partial' (candidate to escalate to 'fail'); P8 strictness downgrades otherwise-good turns to partial (confirm intended). Report: `test_reports/evaluator_calibration_comparison.md`; exports `test_reports/calibrated_run_85624a68.{md,json}`.

## Engine Improvement Phase W-A→W-D + Compare-Two-Runs UI (2026-07, DONE — evaluator FROZEN)
- Held the calibrated evaluator constant; improved ENGINE only via a new "ENGINE REFINEMENTS" block in SYSTEM_MESSAGE (reasoner prompt): W-A consolidation-by-principle (name the transferable rule, not just praise), W-B restraint on competent performance (no invented gaps/over-probing strong work), W-C no copyable content (hard; vary scaffold instead of supplying answer), W-D honor teacher pedagogical purpose + developmental-profile growth edge before defaulting to thesis. Evaluator (EVAL_SYSTEM_MESSAGE/_eval_prompt) and the 32 cases untouched.
- Progression (same evaluator+cases, deltas = engine only): calibrated 85624a68 = 24/8/0 (75.0%) -> +W-A 7b0b473a = 26/6/0 (81.2%) -> +W-B/C/D & reinforced W-A 33cc0307 = 29/3/0 (90.6%). Net +15.6 pts, 0 fails throughout. Fixed: TC07/TC31 (W-B), TC14 (W-D), TC18/TC13/TC29 (W-A). Residual partials (3): TC06 & TC13 (P8 principle-abstraction strictness; engine describes the instance but not always the generalized rule; TC13 flip-flops = run variance), TC30 (real W-D miss: still pulled to thesis over the profile's evidence/explanation growth edge). Evaluator NOT modified (residuals not demonstrably inconsistent with Compass principles). Report: test_reports/engine_improvement_comparison.md; run exports wa_run_7b0b473a.* and wbcd_run_33cc0307.*.
- Compare-Two-Runs UI added to ?tests (TestHarness.jsx): header Run|Compare toggle; CompareView with two run dropdowns and per-case table showing previous verdict, new verdict, changed criteria (from->to), and the new run's explanation. Diff matches criteria by name (valid because evaluator is frozen). data-testids: mode-run-button, mode-compare-button, compare-select-prev, compare-select-new, compare-run-button, compare-view, compare-summary, compare-row-{id}.

## Run Labels + Benchmark Suite Expansion (2026-07, DONE — engine & evaluator FROZEN)
- ENGINE and EVALUATOR frozen (no changes to SYSTEM_MESSAGE, EVAL_SYSTEM_MESSAGE, or _eval_prompt). Did NOT optimize the remaining 3 partials (TC06/TC13/TC30) per instruction.
- Run labels: backend TestRunRequest gained optional `label` (stored on run doc); new PATCH /api/tests/runs/{id}/label to rename. Frontend ?tests: "Run label (optional)" input in the header, label shown in Past Runs + Compare dropdowns, inline rename (pencil) on each past run. Existing runs tagged: 2598c428="Baseline (old evaluator)", 85624a68="Calibrated evaluator", 7b0b473a="+ W-A", 33cc0307="+ W-B/C/D".
- Suite expanded 32 -> 66 cases (TC33-TC66, +34) to test GENERALIZATION (not to optimize the existing 32). Coverage added: GENRES/purposes (narrative show-vs-tell, scene-vs-summary, descriptive dominant impression, process/how-to sequencing, compare/contrast organization, cause/effect causal reasoning, literary analysis, rhetorical analysis, research synthesis, lab report register, persuasive letter, op-ed [strong-restraint], personal statement voice, business email, cover letter, summary/precis fidelity, annotated bibliography, poetry [honor form, no essay-ifying], reflective portfolio); ELEMENTS (audience awareness, tone/register, diction, sentence variety, cohesion, counterargument, concession/straw-man, conclusion function, conciseness, evidence integration/attribution, parallelism, POV consistency); STUDENT STATES (ELL surface-errors-but-strong-idea, AI-paste authorship, perfectionist block). Several are deliberate RESTRAINT cases (TC44 op-ed, TC50 poetry) to catch over-teaching across genres. Schema identical to originals; JSON validated; /api/tests/cases returns 66.
- Smoke-verified 2 new cases run end-to-end through the frozen engine (TC65 AI-paste = pass; TC50 poetry = partial — early signal that poetry/creative restraint may be a generalization gap). Full 66-case baseline run intentionally deferred (next phase: run the expanded suite to identify the next generation of architectural improvements).

## Expanded-Suite Generalization Audit (2026-07, DONE — engine & evaluator FROZEN; awaiting refinement approval)
- Ran full 66-case suite (run fd0dec0c, label "Expanded Suite v1 Baseline"), engine+evaluator frozen, no case modified. Result 58/8/0 = 87.9% (original 32 = 93.8%, new 34 = 82.4%, ZERO fails). Architecture generalized: held up across 17 genres, ~dozen elements, ELL/AI-paste/perfectionist states, adult/pro levels, with no new failure category.
- Added automatic grouped reporting (genre/element/student-state/proficiency; n<3 flagged) via external documented mapping (cases untouched): test_reports/generalization_grouped.md. Full 4-section generalization audit: test_reports/generalization_audit.md.
- Systematic weaknesses (all Class B or C; NO launch-blocking): W1 consolidation names instance not general rule (TC06,TC13); W2 target precision vs explicit teacher purpose (TC35,TC41,TC56,TC61) — clearest generalization pattern; W3 authorship micro-edges (TC63 parenthetical steering=C; TC65 AI-paste authorship not surfaced=B). Beta-ready by the integrity+reliability gate.
- Canonical Decision Log created: /app/memory/CANONICAL_DECISION_LOG.md (adopted decisions + Benchmark Framework + Beta Strategy). Proposed refinements R1(W1)/R2(W2)/optional R3(W3a) PRESENTED, awaiting approve/reject; engine NOT changed. Beta budget = 1 focused refinement cycle; re-run 66 + Compare-Two-Runs vs fd0dec0c after any change.

## Public Preview — Decision Table + Implementation (2026-07, DONE — engine & evaluator FROZEN)
- User approved the entry-path decision architecture (`PUBLIC_PREVIEW_Decision_Table.md`) with 4 refinements applied: (1) interrupt priority reordered so Stall (P4) precedes Life-advice/generic drift (P5) — a stall is a developmental-support problem, scaffold first; (2) convention gate reworded — terminology must NAME an already-experienced communicative function, not "avoid jargon"; (3) soft-cap exit grounded only in what actually happened this session (no claims Compass cannot know, e.g. about the learner's students); (4) bootstrap assignment broadened from "persuasive essay" to "essay for a real reader" (cross-genre). Tie-break rule "Never manufacture C->B by inventing a deficiency" preserved verbatim as a core Compass principle.
- Implemented as an EXPERIENCE WRAPPER over the frozen engine (no engine/evaluator change): backend `POST /api/sessions/preview` (server.py) creates a normal session with a canonical `PREVIEW_BOOTSTRAP` Telos whose `teacher_notes` encode the full decision-table arc (seed A/B/C branching, P1-P5 interrupt priority, scaffolding ladder, meaning-before-convention gate, transfer-beat projection, grounded exit, anti-coauthoring absolute, one target/question per turn). Frontend `?preview` route -> new `PublicPreview.jsx`: minimal in-character single-column surface (seed invitation screen -> conversation), no assignment header / no dev panel / no scoring / no jargon labels; reuses durable polling (2.5s). testids: preview-seed-screen, preview-seed-input, preview-begin-button, preview-thread, preview-coach-message, preview-user-message, preview-thinking, preview-reply-input, preview-send-button.
- Self-verified: `/api/sessions/preview` returns fixed Telos (notes 2197 chars); seed "Schools should stop giving so much homework." -> engine produced an on-target branch-B response (surfaced the resistant reader "Yeah, maybe, but so what?", one question, no jargon, no grade). Frontend seed screen + post-Begin thread (user bubble + thinking dots + reply input) render correctly. NOTE: engine reasoning is ~90s/turn (inherent to existing architecture). The full multi-turn "aha" arc (learner projects an introduction insight onto a future opening within 4-6 exchanges) is a qualitative pedagogical outcome best validated experientially by the user.

## NEXT (per user): whole-product teacher experience design
- Shift focus away from the instructional engine. Design the complete teacher journey end-to-end: arriving at the website -> trying the Public Preview -> entering the Teacher Workspace -> creating a class -> assigning work -> viewing the instructional dashboard. Goal: map where the whole product is leading.

## Public Preview — Latency Audit + Invisible Instrumentation (2026-07, DONE — engine/evaluator FROZEN)
- WS1 Latency audit (measured, no engine change): added stage timers. One representative preview turn = 58.04s total; STAGE B reasoner LLM call = 54.29s (~93%), STAGE A selector LLM call = 3.74s (~7%), DB read/write + prompt build < 5ms, NO evaluator call in interact path. Time-to-first-visible (student's own turn) < 1s; poll detection lag up to 2.5s. Report: memory/PUBLIC_PREVIEW_Latency_Audit.md. Dominant cost = reasoner GENERATING the full structured JSON (theory+frameworks+candidates), most of which the preview never consumes. Safe wrapper fixes (R1 skip STAGE A ~4s; R2 poll 2.5->1.5s + richer wait) trim only ~5s. Real fix R3 = preview-only slimmed reasoner OUTPUT schema (est 55s->20-30s) but GATED — may alter invitations, needs explicit approval + re-validation. R4 streaming deferred. AWAITING USER APPROVAL on R3.
- WS2 Invisible instrumentation (DONE, zero added latency, learner never sees it): Session gained is_preview + preview_analytics; `_update_preview_analytics` (called only when is_preview) records per-turn observable signals from already-computed data + cheap keyword heuristics: seed + provisional_archetype (A/B/C, clearly provisional), per-turn latency (stage_a/stage_b), instructional_mode, primary_target, intervention_type, anti_coauthoring_request + ownership_returned, exchange_count, soft_cap_reached. Distinguishes transfer_question_PRESENTED vs learner_RESPONDED_after_transfer vs projection_MEETS_criterion (left NULL — never auto-inferred from display) vs continued_to_real_work (set only by explicit action). Endpoints: GET /api/sessions/{id}/preview-analytics (dev read), POST /api/sessions/{id}/preview-continue. Verified live: seed "School lunches." -> A_provisional, engine primary_target "supply a stake" (branch A), latencies captured, met=null preserved.
- WS3 Whole-product screen & route map v0 (design only, NOT built): memory/PRODUCT_MAP.md. 9 screens across Discovery path (S1 Landing -> S2 Preview -> S3 Bridge -> S4 Auth -> S6 Teacher Home -> S7 Create) and Direct-app path (S5 Role Gateway -> S4/S8 -> S6/S9). Each screen: purpose/user/essential-info/primary+secondary action/next/what-must-not-appear. Product invariants captured; preview never forced on existing users; Teacher Workspace settings + dashboard detail deferred.
- NOT done (per instruction): no Module 2, no frozen-engine change. R3 pending approval.

## Public Preview — R1+R2+R3 latency optimization IMPLEMENTED + validated (2026-07, engine/evaluator FROZEN)
- R1 (done): preview skips STAGE-A selector LLM call, uses fixed intro/thesis/audience retrieval (PREVIEW_FIXED_SELECTIONS/PREVIEW_FIXED_IO in server.py). -~3.7s/turn.
- R2 (done): preview poll 2.5s->1.5s + rotating in-character "reading your opening..." waiting state (PublicPreview.jsx).
- R3 (done, pure OUTPUT-schema trim; reasoning UNCHANGED): PREVIEW_OUTPUT_OVERRIDE appended to reasoner prompt tells model to reason fully but serialize only preview-consumed fields (invitation + 5 theory reasoning blocks + candidates + selected + intervention), omitting telos echo, non-applicable framework blocks, trailing theory state lists, observed_reorganization, developmental_profile_update. Frozen SYSTEM_MESSAGE untouched. Toggle: _run_engine(..., preview_output=). Parser already tolerant of omitted fields.
- Validation (test_reports/preview_schema_validation.json + preview_variance_C.json): 5 seeds A/B/C + anti-coauthoring + stall, full-vs-preview output schema on identical input (R1 held constant). Latency -25% to -35% on reasoner stage; live e2e preview turn ~48s (was ~58-60s). Anti-coauthoring + stall behaviors preserved exactly. 4/5 cases identical on ALL categorical fields; free-text targets/tensions semantically identical in all 5. ONE FLAG: case C intervention_type wobble (full=invite_only 3/3; preview=invite_only 2/3 + instruct_then_invite 1/3) — target/branch/mode identical every run; within stochastic range at n=3; recommend larger-n (~10/case) confirmation before public launch if strict "identical" must be certified.
- Fixed instrumentation false-positive: tightened _TRANSFER_PATTERNS so transfer_question_presented no longer fires on normal resistant-reader moves ("a reader who" removed).
- Follow-up (non-blocking): R1 loads whole domain records (~59KB input vs 22KB); trim to section keys later to cut token cost (latency impact negligible).
- NEXT (per user): after this ships, shift primary effort to designing/building the teacher-facing product experience (see PRODUCT_MAP.md).

## Product Experience — S1 Landing + S3 Bridge built (2026-07); routing restructured
- Design blueprint via design_agent -> /app/design_guidelines.json (literary writing-studio aesthetic: paper-grain #faf9f6, serif-display, terracotta #8C3A2A, stone palette, mono eyebrows, sharp edges, restrained Framer Motion, no photography). Governing principle: each screen answers ONE psychological question.
- S1 Landing (Landing.jsx, route "/"): Q="Should I care?" Editorial off-center hero, eyebrow "The problem with 'help'", headline "Writing is thinking. Don't let a machine do it for them.", anti-coauthoring subcopy, primary CTA "Try the Preview"->?preview (+ "no sign-up" microcopy), secondary "Open Compass"->?app, quiet contrast band ("Hand them the sentences" struck-through vs "Leads them until they say it themselves"). No jargon/score/rubric/feature-dump. Verified via screenshot + CTA nav.
- S3 Preview->Real Bridge (PreviewBridge.jsx, routes: inside preview after >=2 AI turns via "Bring your own writing ->", and standalone ?bridge): Q="Could I use this with my students?" Centered quiet-room layout, "You just taught yourself.", grounded subcopy (no fabricated student claims), primary "Continue with a real assignment"-> POST /preview-continue then ?app, secondary "Not now"->"/". Verified via screenshot.
- ROUTING RESTRUCTURED (App.js): "/"=Landing (was StudioApp); ?preview=PublicPreview; ?bridge=PreviewBridge; ?app or ?dev=StudioApp (studio moved off root, localStorage restore intact); ?tests=TestHarness. api.js: added previewContinue().
- Larger-n validation running in background (test_reports/preview_largeN_validation.json): A_bare_topic n=6 -> full invite_only 6/6 (avg 54.2s); preview invite_only 5/6 + instruct_then_invite 1/6 (avg 37.7s, -30%), mode 100% stable. Confirms the intervention_type wobble is minor invite_only<->instruct_then_invite calibration jitter (branch/target/mode preserved), not a schema-induced decision change. B/C still running; will report only if meaningful divergence appears.
- NEXT: continue product map after S1/S3 -> S4 Auth, S5 Role Gateway, S6 Teacher Workspace Home, S7 Create class/assignment, S8 Student Join, S9 Student Workspace (largely exists as StudioApp). Instructional engine treated as provisionally stable; future work centers on teacher experience.

## Teacher Configuration module + Grade Calibration (2026-07, backend verified via curl)
- Nested structured configuration object (spec Part VIII): TeacherConfiguration {configurationVersion, status(draft|active), classContext, assignment, learning, guidance, classroom, gradeCalibration, constitutionalRules}. constitutionalRules are SYSTEM-inserted (SYSTEM_CONSTITUTIONAL_RULES), never teacher-editable. Endpoints: POST/GET/PATCH /api/teacher-configs, POST .../validate, POST .../activate (422 with detail on invalid), POST .../create-session (compiles telos+teacher_notes incl. grade calibration). GET /api/compass/constitution (locked principles/never-provides/distinction). POST /api/compass/validate-request = LLM(Claude)-backed Part-VI validator: classification instructional_design|developmental_method_violation, underlying_need, explanation, closest_alternatives, compass_response. Verified: "rewrite weak intros" -> violation w/ alternatives; "grammar emphasis high" -> allowed (emphasis vs auto-correct nuance).
- Validation (Part V): required-field, developmental-minimum (revisionCycles>=1; scaffolding!=none), constitutional keyword scan of teacherNotes (warning), conflict resolution (revisionCycles vs revise stage). Verified via curl (errors, 422 activate, conflict).
- Grade Calibration Profiles as separate editable resources (Part X-XII): grade_profiles collection, idempotent startup seed of GRADE9_PROFILE (8 dimensions), GET /api/grade-profiles[/{id}]. Grade = initial calibration only; performance-based adjustment preserved in engine notes.
- Frontend TeacherConfig.jsx (route ?config): sections Class&Learners/Assignment&Purpose/Learning Goals/Standards&Evaluation/Guidance&Feedback/Revision/Classroom + right rail Review preview + locked Constitution panel + "Ask Compass" request checker + Save draft/Validate/Activate. Compiles. NOT yet live/e2e smoke-tested.

## Preview revision — passage-based, Grade 9 (2026-07, verified live)
- Landing: removed the "What it won't do / What it does — Leads them until they say it themselves" contrast band (slogan confirmed absent from source); page ends at hero+footer.
- Public Preview reframed from opinion-seed -> "Try Compass with a short piece of Grade 9 writing." User enters any essay passage (intro/body/transition/conclusion); optional "What is the essay about?" + "What kind of passage" selector (default "Let Compass infer it"); large textarea placeholder "Enter a short passage here..."; button "Try Compass" (disabled when empty, hint "Enter a short passage to continue."). POST /api/sessions/preview now accepts {essay_about, passage_type} appended to teacher_notes as hints. PREVIEW_TEACHER_NOTES rewritten: infer/confirm passage purpose -> ONE high-leverage issue -> ONE question/invitation -> never rewrite/score/auto-correct -> invite revision -> respond -> fade support; no viewpoint elicitation. Reverted R1 (re-enabled dynamic STAGE-A domain selection for preview) so body/conclusion passages get correct retrieval; kept R2 (poll 1.5s) + R3 (output trim, with C-restraint divergence caveat pre-launch). Verified live: Grade-9 body paragraph -> developmental, non-rewriting single-question response.


## Anchored Coaching — Student Workspace + Public Preview redesign (2026-06, DONE — engine FROZEN)
- Implemented the approved "Anchored Coaching" interaction model (specs: memory/COMPASS_StudentWorkspace_Design.md + COMPASS_PublicPreview_Redesign.md). The document is the persistent center of the screen and editable in place at all times; the AI coach attaches ONE localized coaching marker to the draft; clicking it opens an inline coaching card (single developmental invitation) adjacent to the text; the student revises in place and re-sends. NO separate chat column. Frozen engine + durable polling unchanged.
- StudentWorkspace.jsx FULL REWRITE (route ?dev / ?app): dominant document canvas (writing-area, serif paper surface, autosave to localStorage dws_draft_{id}, live word-count), slim top bar (workspace-assignment-title + workspace-task + revision-progress), single anchored marker (coaching-marker, coach-pulse animation) → inline coaching-card (coaching-invitation) with Explain-more (kind=explain) + Reply side-channel (kind=answer, does NOT change draft). Primary action submit-draft-button relabels 'Send to coach'→'Send revision' (first submit=writing, later=revise); dirty-check guard. Recessed right rail (coaching-rail): resolved coaching-history + revision-history. Single-focus rule enforced (exactly one marker at a time). Dev Panel + New Assignment + Reset controls quietly tucked in top bar.
- PublicPreview.jsx FULL REWRITE (route ?preview): faithful miniature of the workspace. Unchanged seed screen (preview-seed-input + optional essay-about/passage-type), then the passage renders on an editable preview-document canvas (NOT chat), preview-coaching-marker → preview-coaching-card, revise-in-place via preview-send-revision (kind=revise), inline explain/reply. Bridge (preview-bring-own-work) appears after ≥2 completed coachings.
- R3 CASE-C RESTRAINT FIX (backend, preview-only): added a RESTRAINT INVARIANT to PREVIEW_OUTPUT_OVERRIDE (server.py) so the trimmed output schema no longer nudges strong/motivated claims toward instruct_then_invite. Frozen SYSTEM_MESSAGE untouched. Validated: strong Case-C claim under preview schema = 8/8 invite_only (was 1/10 before). Report: test_reports/preview_C_fix.json; harness backend/validate_C_fix.py.
- Verified: testing agent iteration_27 — backend 100% (5/5: durable interact + processing placeholder + poll-to-complete, preview telos, preview interact+poll) + frontend 100% (all Student Workspace + Public Preview anchored-coaching acceptance criteria e2e, single-marker rule, no chat column, side-channels don't mutate draft, revision/coaching history). No functional defects. Applied 2 polish tweaks post-report: 'Revision 0' → 'First draft' until first revise; enlarged marker hit-area (p-2 -m-2). Carry-over minor only: POST /sessions returns 200 not 201.
- NEXT: S5 Teacher Home, merged Create-Assignment authoring flow, Teacher Dashboard + Revision Analytics — all designed around this canonical student experience.

## Revision-History Architecture (2026-06, DONE — approved, verified)
- Approved developmental data foundation for the Teacher Dashboard + Revision Analytics. NEW `RevisionRecord` model + `Session.revision_history`. For every SUBSTANTIVE student revision (kind=revise AND draft actually changed) the engine captures at finalize time: draft_before, draft_after, timestamp, active_coaching_target, coaching_unit, relevant_text_span (best-effort = last paragraph of draft_before, matching the UI's whole-draft anchoring), coaching_invitation_shown, student_reply_or_explanation (answer/explain turns since last draft), target_resolved (+resolution_basis derived from revision_development.development_detected + scaffolding_control.cycle_status), development_detected, primary_growth, next_instructional_target, next_instructional_mode, instructional_decision_changed. Helpers: _record_revision, _previous_draft_excluding, _last_paragraph, _side_notes_since_last_draft, _norm (server.py). Persisted in _run_reasoning; endpoint GET /api/sessions/{id}/revision-history.
- Design guardrails honored: NO prominent student-facing scrubber (recessed right-rail "Revision history" only); analytics data is developmental (growth/resolution/decision-change), never word-count/activity stats. Frozen engine untouched.
- Verified e2e vs real engine (backend/check_revision_history.py → test_reports/revision_history_check.json): 2-draft session, all fields populated with substance (target_resolved=true derived correctly, instructional_decision_changed=true, before/after full states). Endpoint confirmed via external URL.

## Latency Workstream — Conditional Triage Architecture (2026-06, PROTOTYPE + BENCHMARK DONE — awaiting approval to productionize)
- Per user directive: latency is now a central concern (50–90s/turn unacceptable). Designed + tested an ADDITIVE, experimental conditional-triage path AROUND the frozen engine — frozen SYSTEM_MESSAGE and constitutional commitments UNCHANGED. Full deliverable: memory/COMPASS_Latency_Triage_Report.md (map of current reasoning, redundancy evidence, proposed architecture, benchmark, latency reduction, divergence cases).
- Architecture (backend/triage_experiment.py): STAGE 1 RAPID TRIAGE (new tiny TRIAGE_SYSTEM_MESSAGE, ~3s/~130 tokens, replaces STAGE-A) → 6 triage decisions (what changed, prior-target status, learner state, instructional route, inside/outside, foundational?, highest-leverage dimension, relevant IOs, confidence) + early-exit routing (stall/unchanged/resolved/foundational→full-engine fallback) + INSIDE-OUT/OUTSIDE-IN routing invariant → STAGE 2 FOCUSED ANALYSIS (frozen SYSTEM_MESSAGE + focused user prompt + trimmed output = one triaged dimension only). Reuses incremental student model (developmental_profile + revision_history + compact theory).
- Benchmark (backend/triage_benchmark.py → test_reports/triage_benchmark.json), exhaustive vs triage on TC01/03/07/08/25/30, LLM-judged: latency 64.7s → 36.5s (−43.6%); output tokens 2990 → 1743 (−41.7%); same_target 4/6; 2/6 materially different — BOTH judged triage-BETTER (TC07 more restrained invite_only; TC30 corrected known W-D weakness by targeting the profile's evidence growth edge, not thesis); anti-coauthoring focus=writing 6/6. Zero regressions.
- Root cause of latency: STAGE B reconstructs + serializes the ENTIRE 10-lens developmental model every turn though M11 acts on ONE target; ~90% of turn is output generation of unused fields (R3 preview trim already proved this at −25–35%). Triage generalizes the fix behind an explicit rapid-triage decision.
- NOT wired into production (experimental path only, per instruction). RECOMMENDED next: wire behind a per-session flag with foundational→full-engine fallback, run full 66-case suite through the frozen evaluator (Compare-Two-Runs vs fd0dec0c) before defaulting. AWAITING APPROVAL.
- STILL PENDING user decisions: teacher-product data model (Q3), build order (Q4). Auth: none yet (single implicit local teacher).


## Triage — Productionized behind a flag + Full 66-case validation (2026-06, DONE — recommendation: REVISE & RETEST, not default)
- PRODUCTION FLAG (server.py): Session.reasoning_mode {exhaustive(default)|triage_experimental}; Turn.reasoning_path records path (exhaustive_full|triage_focused|foundational_fallback_full) — no silent switching. Set via SessionCreate.reasoning_mode or PATCH /api/sessions/{id}/reasoning-mode. Dispatch in _run_reasoning (lazy import triage_experiment). Verified live e2e (reasoning_path=triage_focused recorded). Exhaustive frozen path unchanged & default.
- SHADOW/HARNESS: _harness_run_turn + _run_test_suite + TestRunRequest honor reasoning_mode and capture per-turn comparison payload (route, target, support level, inside/outside, invitation, anti-coauthoring focus, fallback, stage latencies, tokens). Triage suite stored as normal test_run → Compare-Two-Runs compatible with baseline fd0dec0c.
- FULL 66-CASE RESULTS (triage run f9d4e3a7 vs exhaustive fd0dec0c, frozen evaluator; deliverable test_reports/triage_66_deliverable.json + full writeup memory/COMPASS_Latency_Triage_Report.md Part II): verdicts triage 49P/16partial/0FAIL/1error (74.2%) vs exhaustive 58/8/0 (87.9%) — ZERO fails both. Latency −40.7% avg (64.9s→38.5s), −42% p90; stage split: triage 3.9s + focused 34.3s(89%); DB ~0ms. Anti-coauthoring 73/73 (0 regressions). Fallback fired 1.4% (under-fires; confidence saturated 0.8-0.9). Routing 76% inside_out. Divergences 22/65: 9 triage_better, 10 equivalent_but_different(evaluator_ambiguity), 2 exhaustive_better(over_narrowing TC30/TC61), 1 triage_unsafe (TC15 missed teacher-purpose/foundational).
- ROOT CAUSES (addressable): (1) central-claim/purpose narrowing bias, (2) foundational detector under-fires, (3) no Stage-2 retry (caused TC17 error; exhaustive retries once). RECOMMENDATION: REVISE & RETEST — fix the 3, re-run 66 + Compare vs fd0dec0c, then enable for LIMITED opt-in sessions behind the shipped flag before considering default. Next latency lever: STREAM Stage-2 invitation (bookkeeping ~0ms so partial-delivery alone negligible) — needs approval (transport change).
- AWAITING user decision on the recommendation. Teacher-product build (Q3 data model, Q4 order) STILL PAUSED per directive until triage decision returned. Auth: none yet (single implicit local teacher).


## Triage — Revise-and-Retest cycle + Stage-2 Architecture Review (2026-06, DONE — safe for LIMITED opt-in; NOT default)
- 3 approved fixes in triage_experiment.py ONLY (no other arch change): (1) anti-narrowing + honor assignment's named element at DIMENSION SELECTION; (2) stronger foundational trigger; (3) single Stage-2 retry on unreadable output. Exhaustive path frozen & default throughout.
- Retest iterations: v3 (run 115b7491) fixed quality (78.8%, TC15-unsafe gone, TC30 flipped better) BUT foundational trigger OVER-FIRED to 68.9% fallback → latency win erased (avg 59.1s). Recalibrated: moved teacher-purpose adherence to dimension selection; narrowed foundational to genuinely-broad only (off-task/undeterminable-purpose/incoherent). 
- FINAL v4 (run 532b8231 vs baseline fd0dec0c): 57 pass/9 partial/0 FAIL/0 error (86.4% vs 87.9% baseline — equivalent). Latency avg 37.2s (−42.7%), median 36.0, p90 40.9; stage split triage 3.8s + focused 32.9s. Anti-coauthoring 74/74 (0 regressions). Fallback 1.4% (healthy). Divergences 25: 9 triage-better, 3 exhaustive-better (TC07/TC17/TC22 minor over-narrow), 13 equivalent, 0 UNSAFE. Retry eliminated all errors. Deliverable: test_reports/triage_66_deliverable_v4.json + memory/COMPASS_Latency_Triage_Report.md Part III.
- STAGE-2 ARCHITECTURE REVIEW (memory/COMPASS_Stage2_Architecture_Review.md, analysis only, NOT implemented): Stage 1 works (~3.8s); residual ~33s (88%) is Stage 2 running the frozen SYSTEM_MESSAGE's broad mandates (M11-3 diagnose-all-frameworks, M12 reader, M14 integration, 12-step IO) every turn despite triage already selecting the target. Recommended next design step: factor frozen prompt into immutable constitutional core (M5A/one-invitation/restraint/never-rewrite) + conditional diagnostic layer — edits frozen prompt, NEEDS APPROVAL + 66-case Compare validation.
- RECOMMENDATION (per directive, NOT auto-enabling default): triage v4 safe for LIMITED opt-in sessions behind shipped per-session flag. AWAITING user decision on (a) enable-for-limited-sessions, and (b) proceed with the Stage-2 constitutional-core/conditional-diagnostic refactor.
- Teacher-product build STILL PAUSED (Q3 data model, Q4 order) until triage decision returned. Auth: none yet.


## Triage opt-in + Streaming + Stage-2 decomposition design (2026-06, DONE — WS1/2 shipped, WS3/4 design for review)
- WS1 LIMITED OPT-IN: triage v4 enabled only behind per-session flag. Frontend opt-in via ?triage or localStorage dws_reasoning_mode (App.js withMode); default stays exhaustive. reasoning_path recorded per turn (comparison data continues).
- WS2 STREAMING (perceived-latency only; decision unchanged): focused Stage-2 uses LlmChat.stream_message; invitation is first field, streamed to placeholder turn (status="streaming") via incremental DB writes over existing polling (no SSE → avoids 60s ingress cap). Frontend polls 1.2s, auto-opens coaching card, renders live with caret; bookkeeping finalizes after (status complete) with identical text. Guard fix: _run_reasoning finalize + interact concurrency treat "streaming" as in-flight. MEASURED live: invitation visible ~8s vs full turn ~36s (~4-5x faster TTFT), byte-identical streamed vs finalized. Verified in-browser (?dev&triage). Scope: triage focused path only (exhaustive default unchanged — its schema isn't invitation-first).
- WS3/4 STAGE-2 DECOMPOSITION DESIGN (memory/COMPASS_Stage2_Decomposition_Design.md, NO prompt edits): every SYSTEM_MESSAGE section classified [A] invariant constitutional / [B] conditional diagnostic / [C] learner-model update / [D] reporting, with rationale + proposed constitutional-core + conditional-diagnostic architecture + open questions. AWAITING REVIEW before any frozen-prompt edit. WS5 validation (full 66-case Compare vs fd0dec0c, constitutional behavior not textual similarity) applies post-refactor.
- Files: backend/triage_experiment.py (streaming fns), backend/server.py (streaming wiring + guard), frontend App.js + StudentWorkspace.jsx + index.css (streaming render/caret). Full writeup: COMPASS_Latency_Triage_Report.md Part IV.
- AWAITING user review of the Stage-2 decomposition design before implementing the refactor. Teacher-product build (Q3/Q4) still paused.


## COMPASS Governance Architecture — CANONICAL (2026-06, DONE; gate before Stage-2 impl)
- memory/COMPASS_GOVERNANCE_ARCHITECTURE.md is now the CANONICAL architectural spec (part of Compass's constitutional architecture). 5-layer hierarchy of reasoning governance: L1 Constitutional commitments, L2 Developmental Policy (renamed from Pedagogical Policy — governs developmental commitments stable across future Compass apps even as pedagogy changes; e.g. function-before-convention, one-invitation, support-fading, inside-out-before-outside-in, developmental sequencing, learner ownership), L3 Diagnostic Reasoning (single impl layer; documents 3A Interpretation vs 3B Instructional Decision — not split), L4 Learner model, L5 Presentation contract. Each layer: purpose/always-vs-conditional/persistent-vs-reconstructed/rate-of-change/examples/dependencies + 1-turn interaction walkthrough. Added binding governance principle (higher layers constrain lower; lower inform but never override). Authority top-down; latency lives only in how much L3 runs + L5 delivery.
- memory/COMPASS_SYSTEM_MESSAGE_Annotation.md: every frozen SYSTEM_MESSAGE section (lines ~655-866) tagged with primary [L1]-[L5] + dependencies. SYSTEM_MESSAGE TEXT UNCHANGED (annotation only). Maps always-run core (L1 + governing L2) vs conditional modules (L3) for the Stage-2 decomposition. A/B/C/D ↔ L1&L2/L3/L4/L5.
- All future architectural work must preserve this hierarchy. Stage-2 decomposition implementation still GATED on approval; validation = full 66-case Compare vs fd0dec0c on constitutional behavior, not textual similarity.


## Compass Governance Implementation v1 — Stage-2 Decomposition (2026-06, DONE + VALIDATED)
- Implemented the canonical governance architecture as executable code in `backend/triage_experiment.py` (frozen SYSTEM_MESSAGE byte-for-byte UNCHANGED; it remains the authoritative source of L1/L2). Full report: `memory/COMPASS_Governance_v1_Implementation_Report.md`.
- Explicit L1–L5 tagging; L1 Constitutional + L2 Policy always GOVERN every route (concise `MIN_SAFETY_SET`, no re-created latency); L3 Diagnostic Reasoning is CONDITIONAL via `ROUTE_LENS_MAP` (single source of truth, pure + unit-testable) + `resolve_route_activation`/`select_content_lens`; mandated non-supersession disclaimer present; exactly TWO internal candidates; anti-suppression `route_fallback_required` escalates to the full frozen engine so a route can't hide a foundational problem. Unit tests: `backend/tests/test_governance_decomposition.py` (12/12).
- Validation: full 66-case Compare-Two-Runs vs frozen baseline `fd0dec0c` (gov run `fab70c01`, label "Governance v1 (Stage-2 decomposition)"). Result **55/11/0 = 83.3%** (baseline 58/8/0; triage v4 57/9/0), **0 fails, 0 constitutional-unsafe, 0 over-narrowed, anti-coauthoring 74/74**. Divergences 14: 3 triage_BETTER (TC15/TC35/TC65), 0 exhaustive_better. Foundational fallback fired reliably (1×). TC30/TC50/TC61 = same-target/`partial`, identical to the frozen baseline (pre-existing weaknesses, not new). Subset gate: `test_reports/governance_v1_subset.json`; deliverable: `test_reports/governance_v1_deliverable.json`.
- Success criteria: 9/10 met. The exception: latency is **37.5% below the exhaustive baseline (64.7s→40.4s median/turn)** but **~4.4s ABOVE triage v4 (36.0s)** — the deliberate cost of the richer, auditable governed directive ("an enduring architecture, not a latency fix"). OPEN: the directive can be trimmed to recover the ~4s if pure speed is prioritized over explicit foregrounding.
- Triage remains OPT-IN (per-session `reasoning_mode='triage_experimental'` / `?triage`); the default exhaustive frozen path is unchanged.

## NEXT (per user directive — teacher product remains PAUSED until this was validated; now unblocked)
- Decide latency tradeoff: accept Governance v1 as-is (auditability win, ~v4 latency) OR trim the governed directive to beat v4.
- Then resume the PAUSED teacher-product build: S5 Teacher Home (P1), merged Create-Assignment flow (P1), Teacher Dashboard (P1), Revision Analytics (P1).

## Compare-Two-Runs UI — Governance Inspectability (2026-06, DONE + verified)
- Enhanced `?tests` → Compare view (`frontend/src/components/TestHarness.jsx`): each case row now expands (`compare-gov-toggle-{id}` → `compare-gov-detail-{id}`) into a governance panel exposing — for every evaluated case — the selected triage route, inside/outside, triaged dimension, L3 ACTIVE lenses (green) + DORMANT lenses (struck-through), permitted moves, may-open-new-target, consolidation/fading, whether fallback occurred (+reason), candidate-invitation selection (graceful "captured in newer runs" for pre-tracing runs), and the L1–L5 governance layer trace. Purpose: researcher/developer inspectability of Compass's instructional reasoning.
- Backend (`server.py`): `_governance_trace_for_turn()` derives the trace from stored triage fields via the SINGLE source of truth (`triage_experiment.resolve_route_activation` + `LENS`) — no duplication; `get_test_run` attaches it per turn (computed on read, non-destructive) + a curated `governance_note` for the three triage_BETTER cases (TC15/TC35/TC61→TC65). `_harness_run_turn` now also captures `candidate_invitations` + `selected_invitation` so FUTURE runs show candidate deliberation. Curated notes: `GOVERNANCE_DIVERGENCE_NOTES` (TC15/TC35/TC65), aligned to the actual routes in run fab70c01.
- Verified via screenshot driving baseline fd0dec0c vs Governance v1 fab70c01 → expanded TC15 panel renders correctly.

## Teacher Product — Phase (i) Teacher Home (S6) (2026-06, DONE + verified)
- User decisions: auth DEFERRED (single implicit `teacher_id="local-teacher"`); build INCREMENTALLY (Home → Merged Create → Dashboard → Analytics); data model = teacher → assignment(config) → student sessions (NO classes/rosters); shareable assignment CODE for student join; analytics north-star = GROWTH IN INDEPENDENT PERFORMANCE (triage "who needs attention" first, then per-student developmental history, then class planning).
- Data model (additive, `server.py`): `TeacherConfiguration` += `teacher_id`, `code` (unique 6-char, auto on create). `Session` += `teacher_id`, `config_id`, `assignment_code`, `student_name`. Legacy configs backfilled.
- Endpoints: `GET /api/teacher/assignments` (Home data: per-assignment title/status/code/grade/counts/last-activity), `GET /api/teacher/assignments/{config_id}/sessions` (feeds Dashboard), `POST /api/assignments/{code}/start` (student join by code). `create_session_from_config` now accepts `student_name` and links teacher/config/code. All curl-verified (create → activate → join-by-code → counts update).
- Frontend: `TeacherHome.jsx` at `?teacher` (S6) — assignment cards (status, purpose, student/active counts, grade, last activity, copy-code), empty state, "Create assignment" CTA → `?config`. Warm paper theme, verified via screenshot with real data. data-testids: teacher-home, create-assignment-button, assignment-card-{code}, copy-code-{code}, assignment-grid, assignment-empty-state.
- NOTE: card click currently shows a "dashboard coming next" toast (no dead-end). Create flow still routes to the existing `?config` screen; Phase (ii) will make it one coherent guided "create assignment" experience that returns to Home.

## Teacher Product — remaining (incremental)
- Phase (ii) Merged Create-Assignment: single coherent "I am creating an assignment" flow (reuse `TeacherConfig` engine); on completion return to Teacher Home (not straight into a student session). P1.
- Phase (iii) Teacher Dashboard (`?dashboard={config_id}`): per-assignment student roster + triage "who needs attention" (developmental status, not scores). P1.
- Phase (iv) Revision Analytics: per-student DEVELOPMENTAL history (writing evolution, coaching targets over time, learner responses, support required, fading evidence, emerging frontier) + class-level planning. North-star: growth in independent performance. P1.

## Teacher Product — Phase (ii) Merged Create-Assignment (2026-06, DONE + tested)
- The existing `TeacherConfiguration` model already captures ALL analytics-relevant fields the user flagged for future developmental analytics: teacher instructional goals (`learning.objectives`), assignment purpose (`assignment.purpose`), standards (`learning.standards`), developmental priorities (`guidance.feedbackPriorities`), teacher focus areas (`guidance.instructionalEmphases`), config metadata (`status`, `configurationVersion`, timestamps, `gradeCalibration`). No model change needed — architectural prep satisfied.
- UX: reframed `TeacherConfig.jsx` (`?config`) to ONE coherent "Create an assignment" mental model (H1 "Create an assignment.", eyebrow "New assignment", primary CTA "Create assignment", "Check" instead of "Validate", "Assignment summary" aside, back-to-Workspace link). Removed the old "Configuration/Setup" framing.
- Flow correction: `activate()` NO LONGER starts a student session/redirects to `?app`. It now shows a success screen (`assignment-created`) with the big shareable join code (`created-join-code`), `created-go-home` (→ `?teacher`) and `created-create-another`. Creating an assignment returns to Teacher Home.
- Tested: `testing_agent` iteration_28 — backend 8/8 pass (list shape, join-by-code + counter increment, config→activate→create-session linkage, per-assignment sessions, validation error path, legacy /sessions regression), frontend full flow verified (create→success→go-home; validation blocks empty; no auto student session). Backend tests: `backend/tests/test_teacher_assignments.py`. Fixed 1 LOW issue (invalid nested <button> in AssignmentCard → now role=button div; 0 console warnings).

## Teacher Product — Phase (iii) Teacher Dashboard — SPEC (next)
- Route: `?dashboard={config_id}` (currently a "coming next" toast on card click in TeacherHome — wire it up).
- Answer ONLY three questions initially: (1) Which students need my attention today? (2) Which students are progressing well? (3) Why does Compass believe that?
- **Independence Trend** (lightweight layer, from EXISTING `revision_history` evidence only — NO sophisticated longitudinal modeling yet). Indicators: target resolution; successful revision after coaching; amount of support required; support-fading evidence; successful transfer; repeated dependence on the same target. Evidence-based ESTIMATE, not a measurement.
- Present as CATEGORIES, never a single numeric "Independence Score": Increasing Independence / Stable / Needs Continued Support / Possible Regression. Every category must be CLICKABLE → inspect the evidence behind it (transparency; never opaque AI judgment).
- "Who needs attention" prioritization = Independence Trend + persistent stalls + repeated coaching on same target + lack of progress across revisions + unusual patterns suggesting misunderstanding.
- Dashboard SUMMARIZES (decide where to look). It must NOT become a second Revision Analytics screen.

## Teacher Product — Phase (iv) Revision Analytics — SPEC (later)
- Expands the evidence behind the Dashboard: revision history, coaching targets over time, support fading, transfer evidence, developmental trajectory. Analytics EXPLAINS (understand what you find). North-star metric: growth in independent performance (can the learner now perform with less support, greater understanding, greater transfer than before?).

## Governance v2 — Instructional Reasoning Re-Architecture (SPEC DRAFTED, awaiting approval) (2026-06)
- Major re-sequencing of the instructional engine (candidate successor to Governance v1). User decisions: (1) it IS Governance v2, built behind `reasoning_mode="governance_v2"`, default `exhaustive` v1 unchanged, promoted only after 66-case Compare-Two-Runs constitutional review + improvement in clarity/transparency/quality/latency; (2) frozen SYSTEM_MESSAGE becomes a reusable KNOWLEDGE BASE — a new ORCHESTRATION LAYER owns the reasoning sequence (control-architecture change, not knowledge rewrite); (3) explicit-teaching 5-step pattern (Orient→Teach→Locate→Invite→Reflect), one target/one invitation, student's own content; (4) **Phase III Teacher Dashboard PAUSED** until v2 is specced/implemented/validated/promoted; (5) write the architecture spec BEFORE any code.
- Pipeline: S1 Purpose → S2 Writing Unit → S3 Canonical Functional Model → S4 Functional Interpretation → S5 Instructional Target (ONE) → S6 Developmental Strategy (learner modeling BEGINS here) → S7 Dialogue/Explicit Teaching. Branches: B1 Purpose-Unclear, B2 Prewriting Mode, B3 Foundational Fallback (inherited from v1). Transparency_state emitted every turn.
- **Deliverable: `/app/memory/COMPASS_GOVERNANCE_V2_ARCHITECTURE.md`** (DRAFT FOR REVIEW — architectural level, no code). Awaiting user approval before implementation. Once approved it is the authoritative v2 contract.
- Round-2 refinements incorporated (2026-06): 3-pass call granularity (P1 S1-S3 / P2 S4-S5 / P3 S6-S7); Assignment vs Student purpose detected separately (S1); S4 outputs a functional REPRESENTATION not quality labels; prewriting triggers ONLY on insufficient conceptual organization, never weak prose alone; target = greatest leverage + preserves forward progress; explicit teaching from the READER's perspective; transparency adds `why_this_now`; NEW gating constitutional evaluator dimension **Cognitive Substitution** ('could the learner submit after reading Compass without doing the cognitive work? -> fail'); explicit architectural principle **Interpret Before Instruct**. Spec status: awaiting user confirmation to lock as authoritative.
- Post-approval sequence: implement behind flag → extend evaluator (+purpose-first, single-target, explicit-teaching, reader-centered, function-over-form, transparency, prewriting-appropriateness, latency) → 66-case Compare-Two-Runs → promote → transparency UI → prewriting refinement → resume Phase III.

## Governance v2 — IMPLEMENTED (behind flag), validation running (2026-06)
- `backend/governance_v2.py`: 3-pass orchestrator (P1 S1-S3 orientation with dedicated v2 prompt; P2 S4-S5 functional-representation + single target; P3 S6-S7 uses FROZEN SYSTEM_MESSAGE as KB for developmental strategy + explicit reader-centered teaching). Branches: B1 purpose-unclear, B2 prewriting (only on insufficient conceptual organization), B3 foundational fallback (→ frozen engine), plus bounded assignment-repair per §10.9. Emits reasoning ledger `_governance_v2` + transparency_state (6 fields incl. why_this_now). Cognitive-substitution guard in P3 prompt.
- Wired `reasoning_mode="governance_v2"` in `_run_reasoning`, `_harness_run_turn`, `set_reasoning_mode` (422 validation), harness turn stores `governance_v2` ledger. Default `exhaustive` unchanged.
- Smoke test (TC08/TC01/TC03) PASSED: normal / assignment_repair / prewriting branches all fire correctly; focus=writing, one target, all 6 transparency fields, reader-centered explicit teaching. Latency ~50-53s/turn (below exhaustive 64.7s; above triage v4 36s — acceptable, architecture-over-latency per user).
- Full 66-case Compare-Two-Runs launched: run `388412d1-e209-4614-b555-cfcc701cdf71` (label "Governance v2 (3-pass orchestrator)") vs baseline fd0dec0c. Analyzer: `backend/analyze_governance_v2.py` (adds Cognitive Substitution [gating], explicit-teaching, reader-centered, transparency-completeness dims). Promotion gated on: 0 cognitive-substitution, 0 unsafe, constitutional preservation + improvement.

## Governance v2 — Validation Round 1: DOES NOT PASS constitutional gate (2026-06)
- Run `388412d1` vs baseline `fd0dec0c`. Result: 51 pass / 14 partial / 1 error(TC33) / 0 fail; focus=writing 74/74; quality net-positive (30 v2_better vs 20 v1_better); latency 58.6s median (< v1 64.7s). BUT **fails the gate**: Cognitive Substitution on TC11/TC14/TC18 (hard fail) + unsafe/multi-target on TC10/TC14/TC18; prewriting over-triggers (42%) and assignment_repair (19%). Report: `memory/COMPASS_Governance_v2_Validation_Report.md`.
- **v2 NOT promoted; remains behind `reasoning_mode="governance_v2"`; default `exhaustive` unchanged.**
- Round-2 calibration (before re-run): (1) harden single-target in S5/S7; (2) fix "Locate" step so it points to WHERE without pre-sorting the student's content; (3) strengthen cognitive-substitution guard esp. in prewriting mode; (4) tighten prewriting + assignment_repair triggers with negative examples. Then re-run 66-case; promote only when cognitive_substitution=0 AND unsafe=0.
- Phase III Teacher Dashboard remains PAUSED until v2 promoted.


## Meaning Workspace V1 — Visual Thinking Canvas (2026-06, DONE — verified 100%)
- Built the first visual thinking environment: an infinite canvas (`@xyflow/react`) where the learner externalizes/organizes meanings BEFORE writing. Frontend `MeaningWorkspace.jsx` (ReactFlowProvider + inner Flow); routed via `?meaning=<session_id>` in App.js; entry button `open-meaning-workspace-button` in the StudentWorkspace header. Consistent with Compass's paper/editorial theme but idea-centered & visually quiet (dashed-light groups, lightweight note cards, subtle connections, generous whitespace).
- CAPABILITIES: custom Meaning Objects (inline-editable text + optional note + 5 muted colors; double-click canvas or 'Idea' button to create); spatial Group containers ('Cluster' button, light dashed boundary, resizable via NodeResizer, editable label; membership by spatial containment on drag-stop → objects drift in/out freely; dragging a cluster carries its members); directed & undirected connections with inline-editable relationship labels + a directed/undirected toggle (custom EditableEdge). Debounced full-map autosave (PUT /api/meaning-maps/{id}, ~900ms) — persists across reload.
- AI RESTRICTIONS (structurally enforced): the thinking coach ONLY observes/asks questions. It receives a READ-ONLY text snapshot (`_meaning_snapshot`) and its single endpoint (`POST /meaning-maps/{id}/coach`) can ONLY `$push` to coach_log — there is NO API path for the coach to create/move/group/delete/rewrite. Verified byte-for-byte in tests. Ambient coach: fires after 20s idle following a meaningful structural change, rate-capped 1/90s (refs + 3s interval loop); plus on-demand 'Help me think'. Observations shown in a collapsible right side rail with timestamps; dismiss hides from view but preserves history server-side.
- EVENT LOGGING (architected from the outset per user): append-only `events[]` on the map via `POST /meaning-maps/{id}/events` (server stamps id/session_id/logged_at). Client batches + flushes (debounce 1.5s + 5s interval). Captured: created/edited/moved/deleted object, created/deleted group, grouped/ungrouped object, created/edited/deleted connection, moved_group, requested_coach_help, coach_observation_received, dismissed_coach_observation. This is a rich learner-thinking trace for future undo/history, analytics, research, adaptive scaffolding, teacher insight.
- Backend (server.py ~L2669+): MeaningMap model gained `events`; MeaningEventBatch model; log_meaning_events endpoint. Data model kept open/flexible so it can later feed a Writing Plan unchanged. Writing Plan workspace intentionally NOT built.
- Verified: testing agent iteration_29 — backend 9/9 (get-or-create idempotency, save+reload persistence, event accumulation, READ-ONLY coach byte-identical guarantee) + frontend 100% (canvas load, Idea create+inline edit+persist, Cluster create, on-demand coach→timestamped rail observation, dismiss, rail toggle, autosave-across-reload, event-trail accumulation, back button). Test: `/app/backend/tests/test_meaning_workspace.py`. Note: connection-drag and drag-into-group spatial membership validated by code review + backend shape (React Flow mouse-drag automation is fragile), not UI automation. Governance v2 remains PARKED behind its flag (untouched).

## Meaning Workspace V1 — UUID Hardening + Semantic Object Contract + UX fixes (2026-06, DONE)
- UUID HARDENING: client object/group/connection ids now `prefix + crypto.randomUUID()` (obj_/grp_/edge_), globally unique + immutable-for-life (assigned once, never regenerated/migrated). Fully backward compatible — legacy ids preserved on load/save (verified byte-identical). Removed the old Date.now()+counter scheme.
- CONTRACT DOC: `/app/memory/COMPASS_SEMANTIC_OBJECT_CONTRACT.md` — "One semantic object, many representations." Meaning Workspace owns canonical semantic objects; future workspaces (Writing Plan/Draft/Revision/Teacher/Analytics) reference by immutable object_id and store overlay metadata only, never duplicating. Documents invariants (object_id/creation-timestamp/creator/lineage immutable — last two reserved), overlay principle, and referential-integrity requirement (document-only, not implemented).
- UX FIXES (user-reported): (1) suppressed benign ResizeObserver overlay crash in index.js (fired by NodeResizer); (2) meaning-object cards are now resizable (NodeResizer; width/height persisted to backend) and connection handles are always visible (drag to link); (3) coach is now engageable — chronological conversation thread + reply box; learner replies persist to coach_log (kind='learner'); backend coach endpoint accepts `message`, acknowledges + asks a further reflective question WITHOUT supplying content/structure. Map structure stays byte-untouched (read-only guarantee intact; only coach_log grows).
- Self-verified: curl (coach reply acknowledges+questions, learner_note returned, objects/width/height unchanged, coach_log=2) + screenshots (resize handles, visible connection handles, learner bubble + "thinking with you" + reply input). New objects mint UUID ids live (obj_1a50cf3e-…), legacy o1/o2 preserved.

## Compass 2.0 · Sprint 1 — Assignment Representation (2026-06, DONE — verified 100%)
- Self-contained vertical slice (does NOT touch the Milestone writing engine or Meaning Workspace). Route `?represent` + a Landing CTA (`landing-understand-assignment`). Before any writing, Compass helps the student build an ADEQUATE representation of what an assignment requires.
- BACKEND `/app/backend/assignment_representation.py` (own APIRouter `/api/assignment`, Mongo `assignment_sessions`, reuses shared LlmChat/Claude Sonnet 4.6 + helpers via `init(db, key, now_iso)` wired in server.py). Models: AssignmentSession, AssignmentDemand (label/description/source explicit|inferred/supporting_wording/operation/concepts/status/importance/learner_evidence/confidence[internal]/derivable_from_assignment/scaffold_level/scaffold_attempts), Scaffold (structured: instructionType/targetOperation/concepts/relevant_wording/studentTask/expectedEvidence/nextIfSuccessful/nextIfUnsuccessful/requires_reconstruction), InteractionRecord. Functions: analyze_assignment, compare_interpretation, generate_scaffold, evaluate_operation, evaluate_restatement + deterministic control logic determine_next_move/_pick_target (needs_attention outranks unconfirmed)/_start_level (non-derivable starts at guided construction). Endpoints: POST /sessions, GET /sessions/{id}, PATCH /sessions/{id} (edit+restart), POST /sessions/{id}/interpret|operation|restatement.
- SCAFFOLD LADDER (escalates the learner's OPPORTUNITY to perform): 0 independent · 1 attention cue · 2 guided construction · 3 direct teaching + MANDATORY own-words reconstruction. Every scaffold names the developmental OPERATION (Differentiate/Compare/Define/Explain/Exemplify/Relate) + concepts. 'I don't know' does NOT hand over the answer: derivable demand → guide attention/guided; non-derivable → escalate to teach + require reconstruction. Anti-trap: after ≥3 unsuccessful attempts at level 3 the demand is treated as 'developing' (adequate-for-now) and the flow advances. Confidence is INTERNAL only (never shown).
- FRONTEND `/app/frontend/src/components/AssignmentRepresentation.jsx`: screen state machine on session.stage — Entry(A) → Interpretation(B) → 2-column working view [LEFT persistent Question Map (statuses Understood/Developing/Needs Attention/Unconfirmed Requirement; explicit=solid / inferred=dashed border+tag; operation labels; one 'Working on this' target; status transitions animate in place, map not recreated) + assignment always visible] and RIGHT Focused Scaffold(D)/Restatement(E)/Adequate → Continue to Knowledge (PLACEHOLDER only). Preserved responses trail; edit+restart AlertDialog warning; localStorage `compass_rep_session` resume. Tone: no praise/grades/percentages/monologues.
- Verified: testing agent iteration_30 — backend 13/13 (analyze; all FIVE required interpret variants: omit-outcomes / confuse-process-outcome / only-growth / accurate / misunderstand-compare; 'I dont know' escalation; weak-escalation; good-advancement; restatement→adequate; PATCH clear; no confidence/answer leak) + frontend 100% (full ?represent flow, persistent map, in-place status update preserving demand ids, response history, restart dialog, localStorage resume). Test: `/app/backend/tests/test_assignment_representation.py` (~6 min; synchronous Sonnet calls).
- STOP per instruction: Knowledge Loop / Construction Loop / essay editor NOT built. Sprint 1 to be tested extensively with many assignments before Sprint 2 is designed.

## Compass 2.0 · Sprint 1 — Testing & Refinement instruments (2026-06, DONE)
- Per instruction: did NOT begin Sprint 2. Built the testing/refinement tooling instead.
- DEVELOPMENT SESSION RECORD: `GET /api/assignment/sessions/{id}/record?format=json|markdown` — a complete research case: assignment; AI analysis (explicit + inferred demands, important_distinctions, ambiguities — new fields on the session from analyze_assignment); student (initial interpretation + every response + final restatement); developmental_history (per scaffold: target demand, operation, level, performed?, attempts, reason, final status — enabled by snapshotting the scaffold into each operation InteractionRecord); final Question Map; metadata (created/length, #scaffold attempts+targets, #Level-3, 'I don't know' occurred, restart occurred/count, adequacy).
- DEVELOPER NOTES: private per-session field `developer_notes` (never shown to students); `PATCH /api/assignment/sessions/{id}/developer-notes`.
- DEVELOPER MODE: hidden toggle (Ctrl/Cmd+Shift+D or `?represent&dev`, persisted in localStorage `compass_dev_mode`). Dark panel shows the Developmental Control Engine state: Current Loop=Question Loop, Stage, Dev. Operation, Target Demand, Scaffold Level, Instruction Type, Concepts, Reason (`active_target_reason` added to control logic), Expected Evidence, Next-if-Successful/Unsuccessful, Requires Reconstruction — plus Developer Notes editor + JSON/MD record download. Never visible to students.
- NO analytics dashboards built (per instruction).
- GENERALIZATION SWEEP (analyze stage) across Grade 4→Graduate & Literature/History/Biology/Chemistry/Reflective/Business + types short-answer/creative/argument/experiment/lab/reflective/synthesis/analysis: robust (5-6 demands each, sensible explicit/inferred split, varied apt operations, distinctions+ambiguities everywhere). Findings + one candidate issue (format/length constraints like 'write two sentences' get mislabeled as operation Define → Sprint 2 candidate 'constraint' kind) logged in `/app/memory/SPRINT1_TESTING_NOTES.md`.
- Self-verified: curl (record JSON+MD, notes PATCH, full mini-session with 'I dont know') + screenshot (Developer Mode panel renders full engine state + saved note + download links). Backend files: assignment_representation.py. Frontend: AssignmentRepresentation.jsx (DeveloperPanel).
- STOP: Sprint 1 to be tested extensively with real learners before Sprint 2 is designed.

## Compass 2.0 · Sprint 1 — Development Session Library + Summary + Recommendation (2026-06, DONE)
- Testing-tools only; Sprint 2 NOT started. No analytics/charts/averages/metrics built (per instruction).
- ANALYSIS CLASSIFICATION: analyze_assignment now also returns title, subject, educational_level (stored on session; "if known"). Verified across matrix (e.g. 'Grade 10 Biology · Temperature and Enzyme Activity Experiment').
- DEVELOPER SUMMARY: required private field `developer_summary` (2-4 sentence lesson). SPRINT RECOMMENDATION: private field `sprint_recommendation` (enum: No change needed / Prompt revision / New scaffold / New developmental operation / Developmental Control Engine revision / UI revision / Other). Both saved via extended `PATCH /api/assignment/sessions/{id}/developer-notes` (now accepts developer_notes/developer_summary/sprint_recommendation, each optional) and included in the Development Session record (JSON + Markdown). `GET /api/assignment/recommendation-options` lists the enum.
- DEVELOPMENT SESSION LIBRARY: `GET /api/assignment/library` returns a dev-only index (NOT analytics) — each: date, educational_level, subject, title/first line, duration, #scaffolds, #Level-3, has_developer_notes, has_developer_summary, sprint_recommendation. Frontend `LibraryView` (dark, list-only, no charts) opened from the Developer panel 'Library' button; each row has an Open button that loads the full session into the workspace. Verified: 26 sessions listed; Open loads session + prefills its summary/recommendation.
- Developer panel (AssignmentRepresentation.jsx) now shows Control Engine state + Developer Summary (required) + Sprint Recommendation select + Developer Notes + JSON/MD download + Library button. Still hidden (Ctrl/Cmd+Shift+D or ?represent&dev); never visible to students.
- Self-verified via curl (classification, library index, options, summary+recommendation persistence, record inclusion) + screenshots (Library list, Open→load, panel controls).
- STOP: Sprint 1 frozen; evolve from real Development Sessions before Sprint 2.

## Compass 2.0 · Sprint 1 — Testing Round 1 (2026-06)
- Sprint 1 remains FROZEN; no instructional behavior changed. Ran 6 Development Sessions with imperfect scripted learner personas across the matrix (Grade4 Reflective, Grade8 History, Grade12 Biology lab, College Psychology compare, College Business argument, Graduate Psychology synthesis). Each saved with Developer Summary + Notes + Sprint Recommendation (visible in the Library). Records in /tmp/dev_sessions/; driver /app/backend/tests/run_dev_sessions.py, observations /app/backend/tests/write_observations.py.
- Sprint Recommendation tally: Developmental Control Engine revision ×4, New scaffold ×1, No change ×1.
- RECURRING PROBLEMS (full detail in /app/memory/SPRINT1_TESTING_NOTES.md → this is now the Sprint 2 design input): P1 constraints ("write two sentences", ">=3 sources") mislabeled as operations & scaffolded; P2 target fixation (drills one demand L2→L3→L3 before advancing → over-scaffolding, long loops); P3 L3 mandatory-reconstruction misfires (failed a correct answer as no_reconstruction); P4 operation label instability across turns; P5 over-escalation to L3 on simple/derivable demands; P6 diagnostic mis-attribution (credit only vs active target); P7 too many/marginal essential demands; P8 register not age-calibrated. Worked well: explicit/inferred split, distinctions, ambiguities, target SELECTION, and the best-paced college-argument case.
- CAVEAT: short personas → most sessions ended mid-loop (not adequate); restatement-transfer under-observed. Next: more full-length sessions, esp. Grade 4/8, to confirm P2/P3/P5 before Sprint 2 scoping.

## Compass 2.0 · Sprint 1.1 — Developmental Control Engine Refinement (2026-06, DONE)
- Evidence-driven refactor of the Question Loop (no new instructional capabilities; Sprint 2 / Knowledge Loop NOT started). Justified by Testing Round 1 patterns.
- P1 REMOVE TARGET FIXATION: dynamic prioritization. After each response the engine chooses an action — increase_support / switch_target / next_demand / request_reconstruction / stop_scaffolding. Only ONE escalation per target-visit, then it switches (escalations_this_visit resets on new visit). Verified: no more L2→L3→L3 drilling.
- P2 RECONSTRUCTION GATING: evaluate_operation now returns operation_performed + reconstruction_present separately. If the operation is performed but own-words reconstruction is missing, the engine asks for it ONCE (action=request_reconstruction) then moves on — never re-fails a learner who already demonstrated understanding.
- P3 DEMAND ONTOLOGY: AssignmentDemand.category ∈ conceptual|operational|constraint|formatting|resource. Only conceptual/operational drive scaffolding; constraints/formatting/resource are monitored (shown as muted chips in the map), never instructional targets. Verified: 'write two sentences' → constraint/helpful, not scaffolded.
- P4 STABLE OPERATIONS: scaffold.targetOperation/concepts are anchored to the demand (set once at analysis); drift is now structurally impossible.
- P5 CREDIT EVERYTHING: evaluate_operation returns other_demands evidence; the engine upgrades any demand the response demonstrates (never downgrades from a side-observation). Verified: one good answer bumped 3 demands to developing.
- P6 PRIORITY LEVELS: priority ∈ essential|important|helpful|optional. Engine pursues essential first, then important; helpful/optional never force a long session.
- P7 AGE CALIBRATION (lighter): generate_scaffold prompt now calibrates vocabulary/complexity/examples to educational_level.
- DEVELOPER MODE expanded: control_decision exposes Decision (action), Reason for Decision, Why Alternatives Not Chosen, Demand Category, Demand Priority, Next-if-Successful/Unsuccessful (all dev-only).
- BUG FIXED during validation: restatement adequacy now driven by the demand model (all essential ≥ developing) instead of a separate LLM 'adequate' flag that could contradict it and trap a learner whose restatement was complete. Verified interpret→restatement→adequate end-to-end.
- Backward compatible: all 36 existing sessions load through the new model (defaults fill new fields); old records still build.
- Files: backend assignment_representation.py; frontend AssignmentRepresentation.jsx (category chips + expanded DeveloperPanel).
- STOP: Sprint 1.1 to be re-validated with more Development Sessions (esp. Grade 4/8, restatement-reaching, 'I don't know') before any Sprint 2 planning.

## Compass 2.0 · Sprint 1.1 (cont.) — Task-Requirement vs Answer-Content Boundary (2026-06, DONE)
- Refined the Question Loop so it scaffolds ONLY inferential TASK REQUIREMENTS (what work must be done) and NEVER inferential ANSWER CONTENT (the substantive knowledge). No new instructional modules; Sprint 2 / Knowledge Loop still NOT started.
- AGE CALIBRATION FROZEN (1a): removed age-adaptive scaffold generation; scaffolds now use a fixed Grade 10-12/College register (`_REGISTER` constant). `educational_level` is still detected and kept on the model (for dev records / testing / future adaptation) but no longer drives scaffold vocabulary.
- BOUNDARY REWRITE (`_BOUNDARY`): Compass MAY identify, explain, and REQUEST required work; it MAY NOT perform the substantive work. Once a learner infers a definition is needed, Compass may ask the learner to provide it (learner performs the work). Applied across analyze_assignment, generate_scaffold, evaluate_operation.
- SCAFFOLD LADDER REWORKED (3b): all 4 levels scaffold task interpretation/inference — L0 independent task representation · L1 attention cue (wording/omitted parts) · L2 guided task inference (frame about the WORK, not the concept) · L3 direct teaching of the TASK PRINCIPLE (e.g. what "differ" requires) then reconstruct the TASK REQUIREMENT in own words. Never teaches concept content.
- RECONSTRUCTION re-scoped to verify the learner grasps the task requirement/task-reading principle; accepted on own-language demonstration, requested once, credited if shown elsewhere.
- ACCEPTANCE TEST PASSED (backend, external assignment "How do fixed and growth mindsets differ? How do they affect learning?"): learner infers must define→differentiate→relate; AI never defined a mindset; learner-provided definition evaluated (reason=success) without AI generating it. Test driver: /app/backend/tests/acceptance_sprint11.py.
- Files: backend assignment_representation.py only (prompts + reconstruction scaffold text). Frontend unchanged.
- STOP: Sprint 1.1 complete. Do NOT begin Sprint 2 / Knowledge Loop.

## Compass 2.0 · Question-Loop → Writing Bridge (2026-06, DONE)
- Built the bridge connecting the Question Loop (assignment representation, `?represent`) to the canonical Milestone M1–M14 writing engine (StudentWorkspace, `?app`). A learner who reaches an adequate representation clicks "Begin working on this", reviews/edits a plain-language summary, and lands in the writing workspace focused on ONE component with the first invitation produced by the Milestone engine. Milestone engine + 66-case evaluator + exhaustive mode remain FROZEN and untouched.
- Handoff adapter (transient, no new schema): `build_handoff` / `assess_handoff_readiness` in `assignment_representation.py` (marked an UPSTREAM representation service, not an engine). Handoff carries PROVENANCE (explicit_teacher | explicit_student | inferred_by_compass | unresolved); inference never presented as fact. Stored on `Session.origin_representation` for display + resume.
- Orchestration `POST /api/sessions/from-representation` (server.py): readiness gate (blocks premature open with ONE clarifying question), KB-based component suggestion (canonical model → broad domain; instructional_objects → precise element; consulted not merged), initial-target priority (teacher > learner > highest-leverage; engine still selects the real target), seeds Telos/teacher_notes, fires the first turn (draft as `writing` else the student's own representation as `explain`), links `assignment_sessions.writing_session_id` for resume/no-dupe.
- Developmental target vs support profile: added canonical `high-school-graduate` target profile (seeded); Grade-9 preserved as a support profile beneath it; target is engine-level and fixed, profile only modulates access.
- Entry paths implemented + tested: PATH A (prompt only) and PATH B (prompt + draft). Adapter architected to accept future paths; others NOT built.
- Frontend: "Begin working on this" bar + adequate-panel primary CTA + `BeginWorkingModal` (editable summary, starting-point choice, carried open questions) in AssignmentRepresentation.jsx; "Working on now" focus strip + auto-open first invitation in StudentWorkspace.jsx; `getHandoff`/`beginWorkingFromRepresentation` in api.js.
- TESTS: backend `/app/backend/tests/bridge_e2e.py` — all 6 intents PASS. Frontend (testing agent iteration_31): PATH A, RESUME/no-dupe, blocking-uncertainty PASS.
- KNOWN LIMITATION (pre-existing, FROZEN engine — do NOT fix ad hoc): under adversarial pushback the Milestone coach may concede a brief "foothold" definition of a target concept before redirecting; crosses anti-coauthoring boundary when defining that concept is the learner's assigned work. Probabilistic (backend TEST 6 passed same input). Independent of the bridge; no frozen behavior changed. Documented in `/app/memory/KNOWN_LIMITATION_engine_boundary_foothold.md`. Fix = SEPARATE benchmarked engine-boundary task (add 66-case cases first). NOT started.

## Compass 2.0 · Canonical Acceptance Test (2026-06, REPORT ONLY — no code changed)
- Drove the real bridge + Milestone engine end-to-end across 5 genres (explanatory/argument/literary/historical/scientific), 3 draft strengths, 5 boundary attacks, momentum trace, and recovery. Evidence: /app/test_reports/acceptance_transcript.json; report: /app/test_reports/CANONICAL_ACCEPTANCE_REPORT.md.
- WORKS: single-target discipline (holds under distraction), boundary refusals (5/5 this run, each paired with a doable step), developmental teaching (names transferable concept + student performs), meaning-first target selection across all genres, draft-strength adaptation, fast momentum (~2 actions to writing), solid recovery/continuity.
- FRICTION: (1) describe-vs-do ambiguity (student talks about a change instead of editing the draft; coach can't see it), (2) dual call-to-action at hand-off (Begin-working bar vs live Question-Loop task), (3) boilerplate teacher-facing target rationale, (4) loose KB hint tags, (5) latent stuck-student re-explain loop, (6) later coach turns hide behind marker dot.
- HIGHEST-LEVERAGE small changes (ranked): cue "make the change in your draft"+detect describe-without-doing; make Begin-working the clear primary at readiness; per-case draft-specific "why this target" (teacher-facing); stuck-student concreteness escalation (route via benchmarked engine task — engine frozen); fix KB hint tags; badge new coach messages.
- Reaffirmed: pre-existing "foothold definition" limitation did NOT recur this run (probabilistic). Engine/prompt/evaluator untouched.

## Compass 2.0 · Canonical Instructional Session Model (2026-06, DOC ONLY — no code changed)
- Formalized the single lifecycle every capability must reuse: Orientation → Target Selection → Invitation → Performance → Interpretation → Decision → Closure(resumable). Spec: /app/memory/COMPASS_CANONICAL_SESSION_MODEL.md.
- Deliverable includes: lifecycle diagram, stage-by-stage spec, transition table (entry/exit/required/optional/failure), responsibility matrix (learner/Compass/teacher never blurred), invariant developmental rhythm, mapping to current implementation (Question Loop, bridge from-representation, Milestone M11 scaffolding_control, StudentWorkspace, both KBs, revision_history, session persistence, teacher config), gap analysis, and future integration points (Knowledge/Reading Loop, Teacher Dashboard, Revision Analytics, Resume, component & teacher-feedback entry) — all as Orientation/entry variants that hand into the ONE frozen engine, never parallel engines.
- Success is defined operationally as increased learner control over one meaningful aspect (degree_of_student_control ↑ / evidence_of_developmental_movement), not a finished artifact. Engine/prompt/66-case evaluator remain frozen.

## Compass 2.0 · Sprint 2 — Knowledge Loop (Stage-1 Orientation extension) (2026-06, DONE)
- Built the Knowledge Loop as an Orientation-only extension (NOT a new engine/workflow/KB/persistence). It runs when conceptual understanding is the limiting factor for productive writing, helps the learner build JUST ENOUGH understanding (readiness, not mastery), then returns control to the existing bridge → Milestone engine.
- Backend (assignment_representation.py): transient KnowledgeState on AssignmentSession (concepts_understood/uncertain, misconceptions, unresolved_questions, relationships, learner_explanations, turns, turn_count). Endpoints: POST knowledge/assess (GATE, strong bypass bias — only activates on a clear blocking gap), POST knowledge/respond (interprets, tracks understanding, turn cap 4 forces readiness), POST knowledge/skip (learner-initiated exit), GET knowledge (resume). Boundaries reuse the Question-Loop philosophy: never define assigned concepts, never complete/lecture, minimum prerequisite support. Fail-open on LLM hiccups. build_handoff now carries learner-clarified understanding (explicit_student, context only) into origin_representation; server _compile_representation_notes surfaces it as CONTEXT not fact.
- Frontend: "Begin working on this" now runs the gate first — bypass → BeginWorkingModal; activate → KnowledgeLoopModal (dialogue, respond, skip, and "Now let's use this in your draft" when ready). Wired via assessKnowledge/knowledgeRespond/knowledgeSkip in api.js.
- TESTS: backend /app/backend/tests/knowledge_loop_e2e.py all 5 cases PASS (bypass / misconception / prerequisite build / boundary held under 3 attacks, no leaks / resume). Frontend testing agent iteration_32: 4/4 PASS (bypass, activate+build+transition, skip, AI boundary). 
- Flow implemented: Assignment → Knowledge Loop (if needed) → Milestone engine → StudentWorkspace. Only this one Orientation variant built (per spec). Engine/prompt/66-case evaluator untouched.

## Compass 2.0 · Sprint 3 — Instructional Object Enrichment: THESIS (first object only) (2026-06, DONE)
- Enriched ONLY the Thesis/Controlling Idea object in existing instructional_objects.json (no new KB, no duplication, engine/session/UI unchanged). Added 14 additive fields: reader_function, writer_function, functional_relationships, common_difficulties, productive_misconceptions, indicators_of_development, recognition_diagnostics_detailed, developmental_invitations (11 types w/ use_when/avoid_when), followup_decisions, revision_strategies, stopping_conditions, transfer, engine_usage, enrichment_version. All original fields preserved.
- Reaches the frozen engine automatically: _build_prompt serializes the whole object (server.py ~L1061). Engine mapping documented (each section → Canonical Session stage/engine field). No code change to the engine.
- Validated before/after on a Roman-Empire thesis scenario: after-enrichment coach anchors to the assignment's question, names topic-not-claim/announcement difficulty, deploys predict_reader_understanding invitation, foregrounds contestability. Regression: 35→35 objects, elements identical, JSON valid, full bridge→engine flow ran fine. Deliverable: /app/test_reports/SPRINT3_THESIS_ENRICHMENT.md; transcripts: thesis_enrichment_validation.json.
- STOP per spec: object 2 (Evidence) NOT started — awaiting approval. Suggested order remains: Evidence, Explanation, Introduction, Topic Sentence, Transition, Counterargument, Conclusion.

## Compass 2.0 · Sprint 3 — Instructional Object Enrichment: EVIDENCE (second object) (2026-06, DONE)
- Enriched ONLY the Evidence object in existing instructional_objects.json (additive; engine/session/UI unchanged; reaches engine via _build_prompt whole-object serialization). 17 new fields. Expert additions beyond the template: evidence_quality_dimensions (relevance/sufficiency/credibility/accuracy, each with test+failure_signal+remedy), look_alike_distinctions (background-vs-outcome false positive; illustration-vs-proof; unconnected-quote intent), genre_variation (admissible evidence + bar per genre). All original fields preserved.
- Validated with 3 solid-claim scenarios where the engine targets Evidence: S1 credibility (blog/hearsay), S2 relevance-mismatch (1929 crash for a New-Deal-reduced-unemployment claim), S3 sufficiency (single anecdote as proof). After enrichment the coach names the specific failing dimension, applies the correct look-alike distinction (background-vs-outcome; illustration-vs-proof verbatim), and stays in evidence territory instead of drifting to claim framing. Regression: 35→35, elements identical, JSON valid, full flow ran fine. Deliverable: /app/test_reports/SPRINT3_EVIDENCE_ENRICHMENT.md; transcripts: evidence_enrichment_validation.json.
- Enrichment status: Thesis DONE, Evidence DONE. STOP per spec — Explanation NOT started, awaiting approval. Remaining order: Explanation → Introduction → Topic Sentence → Transition → Counterargument → Conclusion.

## Compass 2.0 · Sprint 3 — Instructional Object Enrichment: EXPLANATION / ANALYSIS (third object) (2026-06, DONE)
- Enriched ONLY the Explanation/Analysis object in existing instructional_objects.json (additive; engine/session/UI unchanged; reaches engine via _build_prompt). Introduced the new STANDARD section instructionally_significant_discriminations (4 cases, each A–F: observable_performance / possible_interpretations / evidence_to_distinguish / response_per_interpretation / common_incorrect_response / why_it_matters) covering: no-explanation, restatement, unsupported-causal-claim, description-not-reasoning. Plus full template + 8 explanation-specific invitations (say_it_aloud probe, name_the_warrant, consider_alternative_explanation). All originals preserved.
- Behavior shift = probe-then-teach: run the distinguishing probe, resolve the underlying case, respond to THAT case (avoid the documented common_incorrect_response). Validated with 3 solid-claim scenarios: S1 own-words probe; S2 outcome-vs-mechanism naming; S3 alternative-explanation ('is scoring higher = learning more?'). Regression: 35→35, elements identical, JSON valid, full flow intact. Deliverable: /app/test_reports/SPRINT3_EXPLANATION_ENRICHMENT.md; transcripts: explanation_enrichment_validation.json.
- LONG-TERM RULE adopted: every future object includes instructionally_significant_discriminations when appropriate.
- Enrichment status: Thesis DONE, Evidence DONE, Explanation DONE. STOP per spec — Introduction NOT started, awaiting approval. Remaining order: Introduction → Topic Sentence → Transition → Counterargument → Conclusion.

## Compass 2.0 · Sprint 3 — Instructional Object Enrichment: INTRODUCTION (fourth object) (2026-06, DONE)
- Enriched ONLY the Introduction object in existing instructional_objects.json (additive; engine/session/UI unchanged; reaches engine via _build_prompt). Introduced NEW standard section instructional_leverage (4 situations, A–E: observable state / possible responses / recommended first response / why highest leverage / when another takes priority) + leverage_over_correctness_note + cross_object_dependencies (5 symptom→cause→action mappings, incl. missing-knowledge→Knowledge Loop). Plus instructionally_significant_discriminations (3), full template, 8 intro-specific invitations, genre_variation. All originals preserved.
- Behavior shift: engine picks the leverage-ranked intro move, treats the intro as a symptom when an upstream/content dependency dominates, reframes background as reader-orientation. Validated with 3 solid-thesis scenarios: S1 hook→thesis connection; S2 correctly declines intro cosmetics for the causal mechanism (leverage-over-correctness); S3 shifted from thesis-precision to Background/Context reader-orientation. Regression: 35→35, elements identical, JSON valid, full flow intact. Deliverable: /app/test_reports/SPRINT3_INTRODUCTION_ENRICHMENT.md; transcripts: introduction_enrichment_validation.json.
- Standard sections now: instructionally_significant_discriminations + instructional_leverage + cross_object_dependencies (apply to future objects when appropriate).
- Enrichment status: Thesis, Evidence, Explanation, Introduction DONE. STOP per spec — Topic Sentence NOT started, awaiting approval. Remaining order: Topic Sentence → Transition → Counterargument → Conclusion.

## Sprint 3 — Canonical Instructional-Object Enrichment (Writing Elements Chart) (2026-07-25, DONE — engine FROZEN)
- Adopted the uploaded **Writing Elements Chart** as the canonical source for Sprint 3. Enriched **18 instructional objects** (Topic Sentence, Supporting Detail, Concluding Sentence, Paragraph, Hook / Opening Move, Background / Context, Conclusion, Title, Sentence, Word Choice, Tone, Audience Awareness, Purpose, Organization, Coherence, Unity, Voice, Revision) in `backend/instructional_objects.json`.
- Method: the **5 canonical fields** (definition, performance_structure, recognition_diagnostics, next_developmental_moves, indicators_of_control) set **VERBATIM** from the chart; the deeper Compass fields (reader/writer function, functional_relationships, common_difficulties, productive_misconceptions, indicators_of_development, recognition_diagnostics_detailed, developmental_invitations, followup_decisions, revision_strategies, stopping_conditions, transfer, engine_usage) **derived CONSERVATIVELY** from the chart + the 4-pilot pattern (Thesis/Evidence/Explanation/Introduction). Per-field `field_provenance` records verbatim vs canonical_derived vs pattern_interpreted vs pattern_boilerplate. No new theory introduced; nothing left blank/incomplete.
- Constraints honored: engine, prompts, architecture, and the instructional network (`related_elements`) **UNCHANGED** (verified: 0 network changes). The 4 pilot objects **untouched**. Enrichment tag `sprint3-canonical-v1`.
- Enrichment now **22 / 35** (4 pilot + 18 chart); **13 base remain** (Central Claim, Supporting Claim, Example, Counterargument, Rebuttal / Response, Qualification, Comparison, Contrast, Cause-and-Effect Explanation, Classification, Transition, Definition, Concept).
- Validation: JSON valid; all target fields present & non-empty; canonical fields spot-checked verbatim; backend reloads and serves; engine index builds over all 35. Builder `backend/tests/enrich_sprint3_canonical.py`; report `test_reports/SPRINT3_CANONICAL_ENRICHMENT_REPORT.md`. `CURRENT_STATE.md` counts updated.
- NEXT: enrich the remaining 13 base objects from the chart (same method); review the `pattern_interpreted` deeper fields; re-run the 66-case harness to confirm no engine-behavior regression.


## Sprint 3 — Canonical Enrichment COMPLETE: final 13 objects (2026-07-25, DONE — engine FROZEN)
- Enriched the final **13 instructional objects** from the Writing Elements Chart: Definition, Concept, Central Claim, Supporting Claim, Example, Counterargument, Rebuttal / Response, Qualification, Comparison, Contrast, Cause-and-Effect Explanation, Classification, Transition. **Instructional-object enrichment is now 35 / 35 (0 base).**
- Same methodology as batch 1: 5 canonical fields VERBATIM from the chart; deeper Compass fields derived CONSERVATIVELY from the chart + 4-pilot pattern; per-field `field_provenance`; no new theory. Builder: `backend/tests/enrich_sprint3_canonical_batch2.py`.
- Constraints honored: engine, prompts, retrieval network (`related_elements`), architecture, and the 4 pilots **UNCHANGED** (verified 0 network changes; pilots still `sprint3-v1`).
- Validation: all 13 pass (JSON valid, fields present & non-empty, version + canonical_source set); backend serves; engine index builds over all 35. Report updated: `test_reports/SPRINT3_CANONICAL_ENRICHMENT_REPORT.md`.
- Created **`INSTRUCTIONAL_OBJECT_SPECIFICATION.md`** (repo root) — the permanent developer schema reference (field-by-field purpose, engine usage, provenance classes, and rules for authoring new objects).
- NEXT: run the 66-case harness at 35/35 (regression check); review `pattern_interpreted` deeper fields; begin Sprint 4; enrich the 10 base canonical-writing-model domains.


## Sprint 3 Verification — 66-case regression run (2026-07-25, DONE — no code changed)
- Ran the full 66-case harness on the frozen `exhaustive` path with the 35/35 enriched KB. Run `0bfe5b82`: **57 pass / 7 partial / 0 fail / 2 error (86.4%)** vs baseline `fd0dec0c` **58 / 8 / 0 / 0 (87.9%)** — same frozen engine path, KB is the only difference.
- **No engine/prompt regressions; 0 hard failures.** 6 baseline partials improved to pass (all now retrieve newly-enriched objects — enrichment helped). 2 "errors" (TC31, TC62) are evaluator JSON-parse crashes (valid invitations underneath), not engine/object defects. 5 pass→partial: TC32/TC37 = Thesis retrieval-salience (enrichment-plausible, data-only fix if confirmed); TC20/TC39/TC64 = borderline evaluator judgments (uncertain). 86.4% is within historical run-to-run variance (74–88%).
- Report: `test_reports/SPRINT3_REGRESSION_REPORT.md` (full classification + prioritized proposals). **No recommendations acted upon — awaiting review before Sprint 4.**


## Sprint 3 CLOSURE + Stability Report (2026-07-25, DONE — no code/data changed)
- **Sprint 3 accepted as complete.** Released as **Compass Knowledge Base v1.0** (local annotated tag `compass-kb-v1.0`; push via Save to GitHub). Archived snapshot in `releases/knowledge-base-v1.0/` (CURRENT_STATE, INSTRUCTIONAL_OBJECT_SPECIFICATION, SPRINT3 enrichment/regression/stability reports, SPRINT3_SUMMARY, RELEASE_NOTES). Added `SPRINT3_SUMMARY.md`.
- **Stability run** (7 flagged cases × 3, frozen `exhaustive`, KB unchanged): 6/7 cases produced ≥2 distinct verdicts across identical runs (TC31/TC32/TC62 hit all three of pass/partial/error) → apparent regressions are stochastic. The 2 main-run "errors" are transient evaluator JSON crashes (same cases passed on rerun). **TC37 is the only stable partial** (target-selection: Thesis chosen over documented Organization on compare/contrast) — insufficient alone to justify tuning. Zero hard failures in any run. Report: `test_reports/SPRINT3_STABILITY_REPORT.md`.
- **Decision:** no tuning of any instructional object. Sprint 4 planning begins only after stability-report review.


## Experience Compass — Build Slice 1: One Objective, Reflection, Stop (2026-07-26, DONE — engine FROZEN)
- Converted the endless `?preview` into ONE developmental cycle: single locked objective → learner revision → developmental recognition → four-section reflection → deliberate stop. Implemented as a WRAPPER over the frozen M1–M14 engine; NO change to SYSTEM_MESSAGE, instructional_objects.json, canonical_writing_model.json, the evaluator, or anti-coauthoring. No new evaluator/LLM call added.
- Backend (`server.py`): new `ExperienceControl` + `ExperienceReflection` models; `Session.experience_control` (present ONLY on preview sessions, else None). `create_preview_session` initializes `ExperienceControl(phase="active", support_cap=3)`. Helpers `_experience_objective_id` (locks the canonical `instructional_reasoning.active_instructional_element` on the first completed turn, never changes it), `_experience_reader_effect` (communicative purpose from `_IO_BY_NAME`, string fallback — no LLM, no psychological prediction), `_build_experience_reflection` (4 sections), `_update_experience_control` (called at end of `_finalize_turn` after `_record_revision`). Transition rules: RESOLVED when `revision_development.development_detected` starts with "yes" OR `scaffolding_control.cycle_status` ∈ {consolidate_and_return, stop}; SUPPORT CAP = 3 answer/explain exchanges without a substantive revision (counter resets on a substantive revision; unresolved substantive revision returns to teaching). `experience_control` persisted in `_run_reasoning` `$set`. `interact` returns 409 when phase ∈ {reflection, complete}.
- Reflection = exactly 4 sections: (1) The one thing you worked on (locked element), (2) How your writing changed (evidence-only; support-cap case never claims mastery), (3) Why it helps your reader (communicative function), (4) Carry it forward (transfer). `completion_reason` = resolved | support_cap.
- Frontend: new `ExperienceReflection.jsx` (4 sections + read-only `reflection-passage` + sole `reflection-try-another` button). `PublicPreview.jsx` completion now driven ONLY by `experience_control.phase` (turn-count `canBridge` + `PreviewBridge` removed from preview); reflection branch hides all editing controls; "Try another paragraph" begins a COMPLETELY fresh session (never reuses the completed one).
- Verified: unit suite `backend/tests/test_experience_control_unit.py` (7/7: lock-once, support cap, resolved-via-yes, resolved-via-cycle_status, unresolved-stays-active, counter-reset, non-preview-untouched); API suite `test_experience_control_api.py` (3/3 incl. 409 guard); live objective-lock (`test_experience_control_live.py`, objective locked='Thesis'). Testing agent iteration_33: backend 100%, frontend 100% (seed+begin, phase-driven completion, PreviewBridge absent, ExperienceReflection shape verified). Validation report: `/app/EXPERIENCE_COMPASS_SLICE1_VALIDATION.md`.
- Deferred to Slice 2: "Bring your own writing" workflow (PreviewBridge file retained but unlinked from preview), feedback capture, beta identity/privacy notice, session recovery. Note: the 3-support-turn reflection path was validated by unit test rather than a live 3–6 min LLM drive.

## Experience Compass — Pre-Slice 2 Cleanup (2026-07-26, DONE — engine FROZEN, no Slice 2 features)
- Narrow semantic-consistency + dead-code pass over the Slice 1 wrapper. NO engine / instructional-object / canonical-model / evaluator / anti-coauthoring changes; NO experience redesign; NO `experience_compass.py` extraction (deferred).
- Terminal state: removed the phantom `"complete"` phase (never set) from the `interact` guard → `phase == "reflection"` is the SOLE terminal/409 condition; documented `ExperienceControl.phase` as the single terminal authority.
- Support cap reconciled: retired the legacy 6-exchange `preview_analytics.soft_cap_reached`; analytics now records the real Experience Compass cap (`support_cap`=3, `support_count`, `support_cap_reached`). Flow still governed only by `support_cap=3`.
- Dead code removed: `PREVIEW_FIXED_SELECTIONS` / `PREVIEW_FIXED_IO` (+comment); redundant `ExperienceReflection.resolved` (kept `ExperienceControl.resolved` as flow flag + `completion_reason` as metadata).
- Reflection wording: humanized element names + 2–3 deterministic per-session variants (no LLM); prefer engine `communication_change`/`primary_growth`/`transfer_message` when present; support-cap wording still states "explored but not yet revised" (no false mastery).
- PreviewBridge.jsx retained + commented for Slice 2, kept unlinked.
- Verified: unit 7/7, API 3/3 (incl. 409), manual init/409/grep, frontend compile + invariant checks (phase-only completion, no `complete` ref, PreviewBridge absent, 4 sections). Report: `/app/EXPERIENCE_COMPASS_PRESLICE2_CLEANUP.md`. Slice 2 NOT started.

## Experience Compass — Chapter 3 (Welcome) + Chapter 4 (Assignment + interim Writing) (2026, DONE)
- Ch3: WelcomeScreen.jsx before the assignment step; exact copy; header hidden on Welcome; Begin advances; restart skips Welcome. Frozen.
- Ch4: Old SeedScreen split into AssignmentScreen (authentic assignment, display-only help, privacy line, Back→Welcome, validation ≥10 chars) + minimal interim WritingScreen (assignment read-only + response + submit). Session created only on writing submit. Restart → cleared Assignment Screen.
- Backend narrow wrapper change: educator's assignment is authoritative (session.assignment + telos.assignment_context + notes marker "THE ASSIGNMENT THE LEARNER IS RESPONDING TO:"); target-learner framing = "intelligent high school graduate" (Grade-9/essay framing removed); PreviewStart.assignment added; neutral fallback. NO engine/instructional-object/Stage-A/coaching/reflection/experience-control changes.
- Verified: backend curl (assignment authoritative, Grade-9 gone, hs-grad operative, fallback correct); frontend browser assertions (Welcome→Assignment→Writing, validation, help/privacy/back, assignment shown read-only on Writing). Spec synced at /app/memory/EXPERIENCE_COMPASS_SPEC.md.

## Homepage Restructure + Post-Experience Teacher Reflection (2026-06, DONE — presentation/sequencing only; engine FROZEN)
- Conceptual correction per user: Compass is ONE evolving instructional experience, not two products. Teacher Review is no longer a top-level pathway; teachers first EXPERIENCE Compass as a student would, and only AFTERWARD does Compass explain itself. Principle: Experience → Reflection → Understanding.
- Homepage (`Landing.jsx`, full rewrite): single "Experience Compass" panel with staged availability — Current experience: Composition / Coming next: Organizing Ideas / Future: Integrated Compass. One primary CTA "Experience Compass" → `?preview`. Kept "How a single Compass interaction unfolds" section. Removed the two equally-prominent Teacher Review vs Student Experience cards. testids: experience-compass-panel, btn-experience-compass, stage-current/next/future.
- Post-experience reflection: after the experience reaches its natural stopping point (`experience_control.phase === 'reflection'`), `ExperienceReflection.jsx` now offers "Would you like to review this experience as a teacher?" (testids reflection-review-offer, reflection-review-as-teacher). Clicking it renders the new `TeacherReflection.jsx` for the teacher's OWN session — explains what Compass understood, the single instructional focus chosen, why (with what it set aside), and the broader developmental goal. Sample-student explorer (`?review`) is offered ONLY afterward as optional exploration (link inside TeacherReflection); not deleted, not primary.
- Backend: new `GET /api/sessions/{id}/teacher-reflection` (server.py) reuses `_curate_case` on the live session's theory+turns (assignment, first writing turn as response, last AI turn as invitation). No engine/prompt/schema change. Note: preview R3-trimmed output omits differentiation/integration/cultural_resource fields, so `explanation.recognized` is typically empty for preview sessions (UI renders it conditionally); understood/focus/why/set_aside/broader all populate.
- PublicPreview wiring: `reviewingAsTeacher` state toggles between ExperienceReflection and TeacherReflection; restart resets it. api.js: `getTeacherReflection(id)`.
- Verified: homepage screenshot (single panel + 3 stages + CTA); `/api/sessions/{id}/teacher-reflection` curl on a real completed preview session returned full understood/focus/why/set_aside/broader; TeacherReflection UI screenshot (mounted standalone) rendered response + Compass's message + instructional reasoning + explore-samples link; preview flow compiles (welcome screen renders, no console errors). NOT e2e-driven through a full ~90s live cycle (Level 2 change; each surface verified independently).

## Chapter 6 — Compass Thinking & Developmental Response (2026-06, DONE — frozen engine UNCHANGED)
- Additive "Pedagogical Noticing" layer establishes the instructional relationship ("I understand you") BEFORE teaching. Independent of the frozen M1–M14 engine (engine remains authoritative for instruction).
- Backend: `POST /api/sessions/{id}/noticing` (server.py) — fast, single-shot `claude-haiku-4-5-20251001` call (~2s, 8s timeout, graceful None on failure). System message `NOTICING_SYSTEM_MESSAGE` produces ONLY {understanding (required, grounded in actual meaning, no contrast/"rather than"), recognition (optional/null — never manufactured; names a productive MOVE not a content restatement), bridge (optional, purely relational — no quality/impact hints)}. Reads the first student "writing" turn. Hard prohibitions on targets/deficiencies/corrections/advice/diagnoses/scores/implicit-gap-signaling.
- Frontend (`PublicPreview.jsx`, preview-only): on first-draft submit, fires `getNoticing()` in parallel with the frozen engine's background reasoning. Reveals Understanding → (pause) → Recognition (if present) → (pause) → Bridge via gentle opacity fades (subtle timing, not performed animation), in the coach's voice/surface. A subtle `preview-thinking-with` cue shows while the engine finishes; the Developmental Response (coaching card) then appears as a natural continuation BELOW the beats (auto-expanded on the first encounter) — no abrupt screen swap. `isFirstMoment` (reviseCount===0) gates it; revisions keep existing coaching behavior. testids: preview-noticing, preview-noticing-understanding/recognition/bridge, preview-thinking-with.
- Terminology cleanup (Preview + Studio): learner's writing → "Response"/"First Draft"; prompt → "Assignment". Removed learner-facing "passage"/"essay" (kept Landing's "not an essay generator" positioning + TeacherSetup's essay example = authentic teacher assignment). App.js TEST_PRESET updated.
- Validation (Level 3): dedicated harness `backend/tests/chapter6_noticing_eval.py` (16 diverse cases, separate claude-sonnet-4-6 evaluator for instructional authenticity) → 100% pass: understanding grounded 16/16, zero teaching leakage 16/16, recognition authentic-when-present / honestly absent 7/16 (never manufactured), latency avg 1.81s / max 2.38s. Reports: test_reports/chapter6_noticing_{results.json,review.md}.
- Testing agent iteration_34: frontend 100% — homepage single panel; noticing appeared 2.8s (understanding+recognition+bridge, grounded, no teaching); continuous encounter (coaching card at 53.5s, noticing still above = no swap); terminology confirmed. No critical/minor issues. Frozen engine untouched (no 66-case regression needed per approved plan).

## Organizing Thought (OT) — Phase 2 of Stage 1 (2026-06, DONE — frozen engine UNCHANGED)
- A 5-object PREWRITING workflow inserted BEFORE the existing Writing workflow in the preview experience (/?preview): The Assignment → Questions I Need to Answer → My Ideas → My Current Answer → My Plan → hands off to existing Writing. Extension, not replacement; no parallel session model, no second writing engine.
- Backend: new module `organizing_thought.py` (router prefix /api/ot, init(db,llm_key,now_iso) convention like assignment_representation). Operates on the existing `sessions` collection; OT state persisted on Session.`ot` (new Optional[dict] field on the Session model). Endpoints: POST /api/ot/{id}/start (seeds assignment), /object (direct save + material-change flags dependents WITHOUT erasing), /interact (PROCEED|TEACH|ASK|PAUSE reasoning via claude-sonnet-4-6, ~3s), /advance, /handoff; GET /api/ot/curriculum. Curriculum data: `ot_curriculum.json` (22 canonical ideas OT-UA/QA/MI/CA/MP with id, stage, canonical_statement, purpose, blocking_difficulty, required_cognitive_act, sufficiency_criterion, stopping_rule).
- Instructional logic: one blocking difficulty per turn; PROCEED when sufficient (not perfect); protect-cognitive-work rules (never writes the assignment repr/questions/ideas/answer/plan/outline) enforced in _OT_SYSTEM with a copy-test validity rule.
- Frontend: new `OrganizingThought.jsx` (5-stage rail, revisitable; object textarea with autosave-on-blur; "Share with my coach" → coach bubble; Continue enables on proceed/sufficient; amber review flag on flagged dependents). `PublicPreview.jsx`: enterOrganizing() creates the preview session + otStart at Assignment→OT; showOT gate (session exists but no student turn); finishOrganizing() hands off to existing Writing on the SAME session; localStorage 'compass_ot_session' resume effect so refresh/return lands back in OT with objects intact. api.js: otStart/otSaveObject/otInteract/otAdvance/otHandoff.
- Verified: backend curl (start/object/interact TEACH~3.4s & PROCEED / material-change needs_review without erasing / advance / handoff / getSession persists ot). testing_agent iteration_35 frontend 100% (8/8): homepage regression, OT entry, weak→TEACH, strong→PROCEED through all 5 stages, "Start writing" handoff, Writing+Pedagogical Noticing regression, revisit-with-preserved-content, material-change amber flag, resume-on-refresh. No bugs. Note: flagged stages require re-share (status→in_progress) before "Start writing" re-enables (intended). Frozen M1–M14 engine and _build_prompt untouched.

## Home Page — Three Student/Teacher Entry Points (2026-06, DONE — entry/routing only)
- Home page (`Landing.jsx`) now offers three explicit entry cards (title + one-sentence description + Begin), reusing existing card styling; hero, stage cards, and "How Compass teaches" unchanged. Replaced the single "Experience Compass" CTA.
  - Teacher Experience → `?review` (TeacherReview — teacher-facing developmental analysis). testid btn-teacher-experience.
  - Student Writing Experience → `?preview=writing` (existing Writing workflow, SKIPS OT). testid btn-student-writing.
  - Student Organizing Thought Experience → `?preview=ot` (OT → Writing). testid btn-student-ot.
- Routing: `App.js` — `?preview` renders `<PublicPreview mode={preview==='writing'?'writing':'ot'} />`; default `?preview` (no value) stays OT (backward compatible). `?review` unchanged.
- `PublicPreview.jsx` gained a `mode` prop: Assignment→Continue routes to enterOrganizing (ot) or straight to Writing (writing); showAssignment gate tightened (`entered && !session && !writingStarted && !otPhase`); submitResponse creates the session if none exists (writing-direct); OT localStorage resume guarded to `mode==='ot'`. No changes to Teacher, Writing, or OT workflow logic.
- Verified: testing_agent iteration_36 frontend 100% (3 cards present + appearance intact; teacher→review; writing→direct Writing skipping OT; ot→Organizing Thought; default ?preview still OT). No bugs.

## Genuine Student Entries — removed teacher wrapper from student modes (2026-06, DONE — entry/routing only)
- Fixed: ?preview=writing and ?preview=ot previously reused the teacher-simulation wrapper ("Welcome to Experience Compass", "experience as one of your students", create-assignment, "Help me create one"). Now they use a genuine student entry.
- New `StudentEntry.jsx`: OT mode shows a student intro ("Organize Your Thinking" + Begin, testids student-ot-intro/student-ot-intro-begin) then the assignment-entry ("Your Assignment" / "Paste or type the assignment you are working on." / "Include the complete instructions your teacher gave you." / button "Continue"); Writing mode shows the assignment-entry directly with button "Continue to Writing". testids: student-entry, student-assignment-input, student-assignment-continue.
- `PublicPreview.jsx`: added mode separation — isTeacher/isStudent. Teacher mode ('teacher') keeps WelcomeScreen + AssignmentScreen + teacher reflection ("Review this experience as a teacher" shown ONLY when isTeacher). Student modes ('writing'/'ot') use StudentEntry via showStudentEntry gate; enterStudent() creates the session with the student's OWN pasted assignment then enters Writing (writing) or OT→Writing (ot). Resume via localStorage 'compass_student_session' storing {id,mode}, guarded to matching mode (no cross-mode/teacher-session pull). WritingScreen back: ot→OT, writing→student entry.
- `App.js` routing/modes: ?review→Teacher Experience (TeacherReview, unchanged); ?preview=writing→genuine student Composition; ?preview=ot→genuine student OT; ?preview=teacher→retained teacher-simulation wrapper; bare ?preview→default = genuine student OT (NEVER the teacher simulation).
- Verified: testing_agent iteration_37 frontend 100% (7/7): student framing correct (no teacher language), routing per mode, in-mode resume, cross-mode isolation, teacher paths intact (?preview=teacher welcome + ?review sample students), homepage cards distinct. No bugs. OT→Writing handoff unchanged; no second Writing/OT engine.

## OT Persistent Thinking Area (2026-06, DONE — presentation only)
- Keeps the Original Assignment and all OT objects visible throughout OT and at Writing handoff, so students never rely on memory (each OT object builds on earlier ones).
- New `OTThinkingPanel.jsx`: renders "Original Assignment" (ot.seed_assignment, read-only, verbatim) + the 5 objects with exact student labels; "Not started yet" for empty; Revisit buttons (onRevisit→goTo); quiet "Needs review · updated since this was developed" from ot.needs_review; long content scrolls (max-h-32); no JSON/ids/status/AI-reasoning exposed. testids: ot-thinking-panel, ot-panel-original, ot-panel-item-<stage>, ot-panel-revisit-<stage>, ot-panel-flag-<stage>.
- `OrganizingThought.jsx`: two-column grid on lg (left = stage rail + current work; right = sticky panel); stacks on narrow screens. proceed() now captures the final OT state from otHandoff and passes it up via onComplete(handoffOt).
- `PublicPreview.jsx`: main container widens to max-w-5xl for OT / OT-writing; WritingScreen gains an `ot` prop and renders OTThinkingPanel (only Original Assignment, The Assignment, My Current Answer, My Plan) when OT context exists — writing-direct mode unchanged (inline writing-assignment-display, single column). finishOrganizing(finalOt) stores otData; WritingScreen ot={otData || session?.ot}; resume hydrates otData from persisted session.ot (backend Session model already returns `ot`).
- Original Assignment vs The Assignment kept distinct: Original = ot.seed_assignment (teacher's verbatim prompt, read-only); The Assignment = ot.objects.the_assignment (student's representation) — both shown separately.
- Verified: testing_agent iteration_38 (6/8; found handoff panel bug — session.ot not populated client-side) → fixed by threading otData → iteration_39 100% (7/7): handoff panel present, revisit+draft preservation, dependency flags, responsive stacking, resume-mid-writing, writing-direct unchanged, panel on OT stages. No OT engine/curriculum/session or Writing engine changes.

## OT "My Ideas" — Two-Pass Redesign + Global Inquiry + Panel Collapse (2026-06, DONE — frozen engine untouched)
- Redesigned the Organizing Thought "My Ideas" stage from bulk answering into a low-friction, structure-led workflow. Diagnose LOCALLY (one question at a time) but plan inquiry GLOBALLY. Compass teaches the STRUCTURE of knowing and directs inquiry; it NEVER supplies subject-matter answers/definitions/facts. Uses Claude Haiku 4.5 for responsive structural diagnosis. Frozen M1–M14 engine + ot_curriculum.json untouched.
- Backend (`organizing_thought.py`): new phase machine on `ot.ideas.phase` = pass1 → knowledge_map → inquiry_plan → inquiry_paused → pass2 → construct → done. Endpoints: `POST /api/ot/{id}/ideas/{init,interact,advance,map,confirm-map,inquiry-plan,pause,resume,construct-guidance,construct}`. PASS 1 is NON-BLOCKING (default decision=proceed; a weak-but-interpretable attempt lets the student continue; PAUSE only when no attempt is possible without a required source). PASS 2 applies sufficiency (not perfection). Knowledge map recommends can_answer/partial/need_info + structural present/gap; confirm-map stores student-confirmed statuses and returns gentle challenges for clear mismatches (never overrides). ONE integrated inquiry plan (needs + generic source categories only). Construct guidance helps the student see connections/central-vs-supporting; final My Ideas is the student's CONSTRUCTED content, not a raw Q&A list. State persists on `session.ot` (no parallel model).
- Frontend: new `MyIdeasWorkflow.jsx` (all mi-* testids) rendered by `OrganizingThought.jsx` when active stage is my_ideas; auto-redirect to Questions if none saved; OT Continue gated on ot.status.my_ideas==='sufficient'. `OTThinkingPanel.jsx` now collapses non-active objects by default (title + status + short preview; ot-panel-toggle-{key}), keeping Original Assignment + active object expanded; expanding does not change stage; Revisit unchanged.
- Verified: full backend flow curl-tested through every phase. Testing agent iteration_40: 15/16 frontend flows green (redirect, non-blocking pass1, structural coaching w/ NO subject-matter leakage, map present/gap, self-assessment challenge, inquiry plan + source chips, pause+resume, refresh-persistence, pass2 revise, construct→done, continue-to-next-stage, panel collapse, draft preservation, narrow viewport). One HIGH bug (mi-build-button on last Pass-2 question gated on stale persisted state) FIXED — predicate now consults the current index's local answer (toConstruct persists before advancing). NOTE: retest of the Build-button fix was self-reasoned (mirrors the verified mi-next-button pattern) but not re-run through the multi-minute UI flow.


## OT "My Ideas" — Structure-Led Anti-Leakage Correction (2026-06, DONE — prompt/critic only)
- Fixed a content-boundary violation: even when Compass correctly named a structure, its follow-up cued the disciplinary answer (e.g. "what does a person with a fixed mindset believe about their intelligence?"). Compass must teach the STRUCTURE of an answer and point students to their own work/source, never naming/cueing the specific belief/feature/dimension/mechanism/criterion/reason.
- `organizing_thought.py` (prompt + logic only; workflow/stages/storage/engine untouched): hardened `_IDEAS_PASS1_SYSTEM`/`_IDEAS_PASS2_SYSTEM` with (a) an explicit CORE DISTINCTION (name the TYPE of missing element, never its CONTENT), (b) a required 5-step message pattern (name structure → explain general form → identify what the student gave → identify the structural gap w/o content → ask them to inspect/revise or consult their source), (c) per-structure teaching+FORBIDDEN lists (definition/comparison/explanation/evaluation/argument/description), (d) internal reasoning fields incl. `leakage_check_passed`. Because Haiku self-grading was unreliable, added a dedicated adversarial critic `_leakage_sanitize()` (Claude Sonnet-4-6) that reviews and rewrites any teach/ask/pause message that cues content; `_ideas_reason()` calls it on every teach/ask/pause turn (proceed turns skip it, staying fast).
- Verified via curl across all 6 required structures (fixed-mindset definition [the reported example], photosynthesis, democracy/monarchy comparison, erosion explanation, uniforms argument, energy evaluation) — all clean, no leaked content. Testing agent iteration_41: build-button fix + definition-leakage display + full two-pass flow into My Current Answer = 100% green; verbatim Pass-1 definition message confirmed leak-free. Known tradeoff: the Sonnet critic adds ~30-60s to teach turns (loader shown throughout); correctness prioritized over speed.


## Compass Developmental Feedback System (2026-06, DONE)
- Purpose: evaluate BOTH usability AND whether the instructional process genuinely develops student thinking — treated as equally important. Data/presentation only; frozen engine + OT logic untouched.
- Backend `feedback.py` (router registered in server.py): `POST /api/feedback/{id}` (kind end|early_exit|general; stores responses + would_use_again + exit_reason + comments + an auto analytics snapshot in a new `feedback` collection); `POST /api/feedback/{id}/event` (lightweight events appended to `session.feedback_events`); `GET /api/feedback/{id}/analytics`. Auto analytics derived from session state + events: stage_reached, time_per_stage_seconds (from stage_enter deltas), revisions (revise turns), help_requested/count, exited_after_repeated_difficulty, resumed_later.
- Frontend `components/feedback/`: WelcomeIntro (experimental-system intro banner on entry screens), FeedbackButton (persistent bottom-right 'Help Improve Compass'), FeedbackModal (8-question end form: 7 text + would-use-again choice + comments; auto-opens once at reflection), EarlyExitModal ('Before you go…' reasons + comments, with 'Send & leave' and 'Leave anyway'). Wired in PublicPreview: header Exit control during active non-reflection student sessions; analytics events fired for stage_enter (OT stage / writing / reflection), help_requested (explain-more), resume (session restore), and a best-effort beforeunload beacon (early_exit_unload).
- Verified: backend curl (events, end, early_exit, analytics snapshot all ok); testing agent iteration_42 = 100% frontend (intro, persistent button, end form submit→thanks, early-exit submit + leave-anyway, writing + OT regression, no console/overlay issues). NOTE: reflection AUTO-OPEN not e2e-verified (needs full LLM writing completion) — same modal verified via the persistent button. All feedback POSTs never trap the student (dismissible; errors swallowed to a thank-you). all data-testids present.


## Feedback Invitation Reworded — "Can We Ask a Favor?" (2026-06, DONE)
- Reframed the feedback system from a survey to a personal, humble, collaborative request. Copy/structure only; backend + response keys (helped_most/confusing/improve_one) + all data-testids unchanged.
- Persistent button: "Can We Ask a Favor?". End dialog: new heading + 3-paragraph intro, 3 brief questions + would-use-again + optional "Anything else"; buttons "Send Feedback" / "Maybe Later". Early-exit: new warm copy, 6 reasons, optional comments; buttons "Send Feedback" / "Leave Without Feedback". Submitting always optional. Verified via compile + screenshot.

## My Ideas PASS 1 — Elaboration Coaching (2026-06, DONE, backend prompt/logic only)
- Scope: ONLY the My Ideas first pass (`_IDEAS_PASS1_SYSTEM`). Pass 2 and all other Compass coaching unchanged.
- Rewrote Pass-1 coaching to a warm 6-step elaboration sequence: affirm effort -> name the kind of thinking -> briefly explain that structure -> DESCRIBE (not evaluate) where the student thinking is -> INVITE them to extend their OWN thinking with an OPEN question -> reduce performance pressure. Non-blocking as before.
- HARD ban on this screen: no directing to notes/readings/sources, no emphasizing missing knowledge (those move to later stages). Content-leak boundary still enforced (never supply/cue the answer, incl. inside an inviting question e.g. NOT "what does the person believe about their abilities?").
- Enforcement: added `_LEAKAGE_CRITIC_PASS1_SYSTEM` (strips content cues AND source-direction, rewrites toward open elaboration) + `_leakage_sanitize(pass_no)`. Pass-1 runs the reviewer on EVERY turn to guarantee the boundary (Pass 2 keeps the source-directing critic on teach/ask/pause only). Tradeoff: ~+3s per Pass-1 turn (correctness prioritized; deferred layered optimization would reduce it).
- Verified via curl: fixed-mindset (reported example), photosynthesis, erosion -> 6-step tone, no source-direction, no content leak, decision non-blocking.

## My Ideas PASS 1 — Explicit Structure->Action Bridge (2026-06, DONE, Pass-1 prompt/critic only)
- Added step 4 "BRIDGE" to the Pass-1 sequence: affirm -> name the kind of thinking -> explain the structure -> EXPLICITLY state the concrete cognitive action to perform that structure (restating the task using the question concept, e.g. "to write a definition, tell what a fixed mindset IS") -> invite with an OPEN question. Compass no longer assumes students infer the action from the structural explanation.
- Critic (`_LEAKAGE_CRITIC_PASS1_SYSTEM`) updated to REQUIRE + PRESERVE the bridge (restating the task with the question concept is clean) while still stripping actual answer CONTENT and any source-direction.
- Verified via curl: definition (fixed mindset), comparison (democracy/monarchy), explanation (erosion) all show explicit bridge + open invitation, no content leak, no source-direction.

## My Ideas PASS 1 — Component-Based Structural Coaching (2026-06, DONE, Pass-1 prompt/critic only)
- Compass now evaluates STRUCTURAL COMPONENTS, not all-or-none structures. It detects which components the answer already performs, acknowledges that specific progress structurally (echoing the student words), and coaches ONLY the next missing component — never restarting the whole structural explanation.
- Components defined per structure: Definition (kind / defining feature / distinguish); Comparison (things / shared dimension / how each differs); Explanation (outcome / cause / process); Evaluation (criteria / apply / judgment); Argument (claim / reasons / evidence).
- Pass-1 critic updated to PRESERVE progress-acknowledgment (student own words = clean) alongside the bridge, while still stripping answer content + source-direction.
- Verified via curl: partial definition ("a way of thinking" -> acknowledges KIND, coaches distinguishing feature, per the reported example); thin definition (coaches first component); partial comparison (acknowledges two things, coaches shared dimension). No content leak, no source-direction.

## Homepage Redesign — Tell the Story of Compass (2026-06, DONE — frontend only)
- Rebuilt `frontend/src/components/Landing.jsx` from the approved blueprint (`design_guidelines.json`). Removed the old redundant double-panel layout. NO backend/engine change.
- New structure: editorial sticky nav with CGI logo (`/frontend/public/cgi-logo.png`) + "Try Compass" button; left-aligned hero "AI that develops thinking. Not replaces it." with philosophy subtext; bold philosophy contrast section (3 rows, muted "Most AI…" vs terracotta "Compass…"); 3 asymmetric entry cards — dominant terracotta "Try Compass" (->?review, per user, replaces old "Teacher Experience") + "Student Writing Experience" (->?preview=writing) + "Organizing Thought Experience" (->?preview=ot); roadmap timeline (Composition Now -> Organizing Ideas Next -> Integrated Compass Future); 4-step method explainer; dark closing CTA.
- Kept existing site fonts (font-serif-display Cormorant Garamond, font-mono-panel IBM Plex Mono, body IBM Plex Sans) and warm paper-grain + terracotta #8C3A2A aesthetic per user choice.
- Verified: screenshot (all sections render on-brand) + testing agent iteration_43 — frontend 95%: all landing sections + data-testids render, CGI logos load, no console errors; all 3 "Try Compass" variants navigate to ?review; both preview routes render. Only note was a testid mismatch in the test SPEC (referenced non-existent 'preview-seed-screen'; actual seed screens use 'student-entry'/'student-ot-intro') — NOT a redesign bug.

## Homepage IA Refinement — Inspire → Invite → Explain → Welcome (2026-06, DONE — frontend only)
- Reordered `Landing.jsx` so visitors can act within seconds: (1) Hero, (2) Experience Compass entry cards moved up directly under the hero, (3) "What makes Compass different" philosophy, (4) NEW warm human statement "Why we built this" (AI should strengthen learning not replace thinking; experimental; invites feedback), (5) dark Final CTA "See what it feels like to be taught, not corrected."
- Removed the Roadmap ("Where Compass is going") and the 4-step Method ("How a single Compass interaction unfolds") sections from the homepage — content preserved in `/app/memory/homepage_removed_content_for_about_page.md` for a future About/How-Compass-Works page.
- Tightened the philosophy contrasts to slogan form: "Most AI gives answers. / Compass develops thinkers." · "Most AI writes for students. / Compass teaches students to write." · "Most AI removes productive struggle. / Compass turns it into learning."
- Visual style (typography, spacing, colors, editorial aesthetic) unchanged. Verified via screenshots: new order, tightened contrasts, human statement + CTA all render on-brand, no console errors. New data-testid: `human-statement-section`; removed `roadmap-section`/`explainer-section`.

## W-E Dependency-First Instruction — Teach the Missing Ingredient Before Revising the Structure (2026-06, DONE — reasoner architecture, verified)
- New DEFAULT coaching architecture for Guided Composition (server.py reasoner). Canonical principle: Compass must NOT ask a student to "strengthen"/"clarify" a higher-level structure (thesis, topic sentence, intro, argument, conclusion, organization) until the lower-level conceptual/rhetorical DEPENDENCY that structure requires has been identified and developed. Replaces the premature "strengthen your thesis" reflex with "here is the one thing your thesis needs to do its job — we'll build that first."
- Implementation (no engine rewrite; prompt + schema + model only): added ENGINE-REFINEMENTS block **W-E DEPENDENCY-FIRST INSTRUCTION** to SYSTEM_MESSAGE with the mandatory student-facing sequence (Recognize → name focus → explain purpose → identify missing ingredient in terms of purpose → scaffold that ingredient with ONE act → project the return). Added `required_dependency` + `dependency_status` (none|identified|being_taught|addressed) to `InstructionalReasoning` model + JSON output schema; reuses existing `continue_consolidate_release_or_shift=shift_to_prerequisite`. Coordinates with W-A (consolidate the dependency by principle once developed), W-B (do NOT invent a dependency on competent work — release instead), W-C (never supply the student's actual content), one-target/one-invitation rules.
- Verified live (growth-mindset thesis, external URL, 2-turn cycle): T1 draft "growth mindset helps students improve grades / work harder" (states what it DOES, not what it IS) → active=Thesis, required_dependency="Definition of 'growth mindset'", dependency_status=identified, shift_to_prerequisite, coach recognized idea + explained thesis purpose + named the missing definition + asked "how would you explain what a growth mindset actually is?" + "let's build that first". T2 student gives definition → dependency_status=addressed, continue, coach consolidated ("now you have the conceptual anchor your thesis needs") and RETURNED to the thesis (draft a contestable claim using the definition). NOTE: full 66-case regression suite NOT re-run (each run ~45 min of live LLM calls); W-E is additive + guarded by W-B, target scenario + return cycle confirmed.

## W-E Generalized to DEPENDENCY-FIRST INSTRUCTION (core canonical principle) + teacher visibility (2026-06, DONE — verified)
- Renamed/broadened W-E from "thesis coaching" to the general **DEPENDENCY-FIRST INSTRUCTION** principle governing ALL Guided Composition coaching. Prompt (server.py SYSTEM_MESSAGE) now states it as a CORE canonical principle with four worked examples (thesis→concept definition, topic sentence→central idea, argument→evidence/reasoning/clarity, transition→relationship between ideas). Procedure + mandatory student-facing sequence unchanged; still one target/turn, coordinates with W-A/W-B/W-C.
- Added `dependency_rationale` (plain teacher-language "why this comes first") to `InstructionalReasoning` model + JSON schema (alongside existing `required_dependency`, `dependency_status`).
- Teacher visibility: NEW Dev Panel block `dependency-first-block` (DevelopmentPanel.jsx, top of scroll content, amber accent) shown ONLY when a dependency is active. Displays TEACHER language — "Current instructional focus" / "Dependency currently being developed" / "Why this comes first" — and NEVER exposes internal field names (required_dependency/dependency_status). GitBranch icon imported.
- Design docs: /app/memory/CANONICAL_DECISION_LOG.md updated with an ADOPTED entry listing Dependency-First Instruction among Compass's core instructional principles (teach one move at a time; make focus explicit; preserve authorship; strengthen emerging structures not replace; teach the missing dependency before returning to the larger structure).
- Verified live (external URL): growth-mindset thesis draft → active=Central Claim/Thesis, required_dependency + dependency_status=identified + dependency_rationale all populated in teacher language; Dev Panel dependency-first-block renders the 3 teacher lines with no raw field names; coach message uses the dependency-first sequence ("Before we strengthen your thesis, let's build the ingredient it needs..."). Full 66-case regression NOT re-run (~45 min live LLM); change is additive + W-B-guarded.

## Constitutional Update — Integrate the Week's Instructional Principles into the Existing Constitution (2026-06, DONE — verified)
- Did NOT create a new constitution. Integrated into the existing constitutional layer (`memory/COMPASS_GOVERNANCE_ARCHITECTURE.md`, Layer 1) + the reasoner prompt (server.py SYSTEM_MESSAGE).
- Constitution doc changes: (1) NEW "Constitutional precedence (binding)" section — Constitution is highest authority; on conflict the constitutional principle governs; Compass is a teacher-directed developmental system, NOT a discovery-learning facilitator; legacy inside-out phrasings (follow train of thought / open-ended discovery / organize around student interests / discuss ideas before structure / let content obscure the lesson) made SUBORDINATE. (2) NEW "Layer 1 — Constitutional Instructional Principles": the six principles (Begin with emerging competence; Structure leads content; Make the lesson visible; Teach one structural move at a time; Dependency-first instruction; Preserve student authorship) + the 7-step default Guided Composition instructional sequence. (3) NEW "Fast Instructional Triage vs Deep Developmental Analysis" section documenting that the architecture already distinguishes them (Layer-2 rapid triage ~3.8s via reasoning_mode flag; Layer-3 conditional deep analysis) and the perceived-latency goal, gated on 66-case validation, never weakening Layers 1-2. (4) NEW "Teacher Transparency (Layer 5)" guarantee — teacher language only, never internal field/schema names.
- Reasoner prompt (server.py SYSTEM_MESSAGE) changes: prepended a top-of-prompt "=== COMPASS CONSTITUTION ===" preamble (highest authority) with precedence, the teacher-directed-not-discovery clarification, the six principles, and the default 7-step sequence. Reworded the INSIDE-OUTSIDE COORDINATION RULE and the "student thinking leads" clause so student thinking leads the CONTENT while sound pedagogy leads the INSTRUCTIONAL SEQUENCE (structure leads content) — subordinating the legacy discovery-first reading.
- Verified live (external URL, persuasive/thesis turn): coach opened by recognizing emerging competence ("you're already doing the most important thing: taking a position"), named the focus (thesis) + its purpose, applied dependency-first (identified the missing arguable reason, dependency_status=identified, scaffolded it with a frame that preserves authorship). Backend loads clean; reworded inside-out rules did not break coaching. NOTE: full 66-case regression NOT re-run (~45 min live LLM); changes are additive/clarifying and consistent with existing W-A..W-E behavior.

## Unified Structural Reasoning — One Reasoner, Two Entry Points (2026-06, DONE — verified live; full 66-case run in progress)
- Made the Composition Process and the live Teacher Review (?preview=teacher) two ENTRY POINTS into ONE canonical reasoner over ONE nested structural hierarchy (Whole Composition → Major Parts → Paragraph → Structural Elements → Relationships → Sentences → Words). No second system; both submit at different points.
- Backend (server.py): NEW "UNIFIED STRUCTURAL REASONING" governing block in SYSTEM_MESSAGE (canonical hierarchy; structures vs elements vs relationships; entry-point detection composition_process|teacher_review; 9-step reasoning sequence; hierarchical instructional triage — higher-level structural issues constrain lower-level instruction; then the canonical coaching sequence). NEW `StructuralReasoning` model + `theory.structural_reasoning` (entry_point, available_portion, hierarchy_level, structure_identified, elements_present/emerging/absent/unnecessary, element_relationships, developmental_dependencies, hierarchical_triage_rationale); added to JSON output schema, _compact_theory serialization, and the final "respond with JSON" instruction. Coordinates with W-A..W-E, one-target/one-invitation, M5A.
- Frontend (DevelopmentPanel.jsx): NEW teacher-language `structural-reasoning-block` ("Where this fits in the writing": what Compass is reading / already in place / beginning to emerge / not yet present / how the parts relate / why start here) — no internal field or schema names. Layers icon imported.
- Docs: COMPASS_GOVERNANCE_ARCHITECTURE.md gained a "Unified Structural Reasoning (one reasoner, two entry points)" section (Layers 2–3 under the Constitution).
- Verified live (external URL, both entry points): (A) Composition Process top-level start ("I want to write about homework") → entry_point=composition_process, level=structural_element, triage "no controlling idea → teaching hook/transitions premature; thesis is highest-leverage", target=Thesis. (B) Teacher Review pasted body paragraph → entry_point=teacher_review, FIRST located "developing body paragraph", present=evidence/topic, absent=supporting claim/topic sentence/explanation, relationship "evidence present but unconnected to any claim", triage "no supporting claim → all lower-level work premature", target=Supporting Claim. Both used the canonical coaching sequence + preserved authorship. Teacher panel block renders correctly. Full 66-case benchmark (run 751995fc, label "Unified Structural Reasoning — post-implementation") started in background vs baseline fd0dec0c — results viewable in ?tests Compare-Two-Runs.

## Latency Experience + Coaching Execution Contract + Developmental Sufficiency (2026-06, DONE — verified)
### A. Latency experience — delayed, grounded, cumulative interim observations (frontend + backend)
- Replaced the old immediate understanding/recognition/GENERIC-BRIDGE noticing with 1-2 GROUNDED, CUMULATIVE, STRUCTURAL interim observations. NOTICING_SYSTEM_MESSAGE rewritten: Obs1 = emerging central idea (grounded, provisional); Obs2 (optional) = the structural relationship/distinction supporting it. Constraints: grounded in student's words, brief, provisional (no verdict words: wrong/breaks/works against), student-understandable, consistent with final coaching, instructional-not-reassuring, NO process talk ("I'm analyzing"), NO chain-of-thought, max 2. Endpoint `/noticing` now returns {ok, observations:[...], understanding=obs1 for back-compat}; uses the MOST RECENT student writing turn.
- PublicPreview.jsx: `noticing.observations` state; paced reveal — Obs1 held to ~3.5s after submit (submitAtRef), Obs2 ~+3s; NoticingBeats renders observation-1/observation-2 (removed bridge). Never delays full coaching (replaces beats when reasoning completes). VERIFIED LIVE in ?preview=writing UI: Obs1 at ~3.6s, Obs2 at ~7.2s, both grounded/cumulative, then "Thinking with you…" continues.
### B. Coaching Execution Contract (reasoner SYSTEM_MESSAGE, new block)
- ALWAYS NAME THE LESSON BEFORE EXPLAINING: coaching MUST open by naming today's structural focus ("Today we're working on your thesis…") before any topic/content discussion (even a mismatch); 5-step order What/Where/What-does-it-do/What-it-needs/Let's-build. MAKE DEPENDENCIES EXPLICIT (focus+dependency+why-first+will-return). WRITING INSTRUCTION HAS ABSOLUTE PRIORITY OVER CONTENT: never teach subject content just because understanding is incomplete/inaccurate; content clarification only when all 3 conditions hold, using a NEUTRAL/unrelated example, then return to student's topic and have THE LEARNER construct content. SCAFFOLD RATHER THAN SUPPLY. VERIFIED LIVE: coaching now opens "Today we're working on your thesis…", applies dependency-first, teaches writing not the concept, scaffolds without supplying.
### C. Developmental Sufficiency, not local optimization (constitutional principle #7 + contract rule + fields)
- New governing rule: before teaching/refining any structure ask "sufficient for the learner to proceed to the next step?" If SUFFICIENT → release, acknowledge achievement, connect to next step, MOVE ON (a definition need not be perfect). New fields instructional_reasoning.sufficiency_for_next_step (sufficient|not_yet) + next_developmental_step (model + schema). Added to Constitution preamble (principle 7). VERIFIED LIVE: good-enough-but-imperfect definition → sufficiency_for_next_step=sufficient, dependency_status=addressed, coach acknowledged ("That's a solid working definition…") and moved on to sharpening the thesis instead of over-refining.
### Latency (actual) — still open per user item 5
- Interim observations improve PERCEIVED latency; real Stage-B reasoner call still ~40-55s. Levers to pursue next: make fast structural triage complete earlier / stream the coaching once the instructional decision is stable / combine or drop redundant LLM passes / let the canonical structure map constrain the first pass. Triage mode exists behind reasoning_mode flag.

## Canonical Structural Elements — Developmental Exit Criteria v1.0 (2026-06, DONE — verified)
- NEW canonical data file `backend/developmental_exit_criteria.json`: 15 structural elements across the hierarchy (whole composition: communicative_purpose, overall_organization; introduction; body paragraph: paragraph_purpose, controlling_idea, definition, explanation, evidence, example, transition, reader_guidance, synthesis, closure; sentence: sentence_purpose, sentence_relationships). Each has purpose, cognitive_operation, developmental EXIT criterion, dependencies, failure_modes, next_operation. Standard = developmental SUFFICIENCY ("can this element support the next authentic cognitive operation?"), NOT quality.
- Backend (server.py): loader `load_exit_criteria()` + `EXIT_CRITERIA` / `EXIT_CRITERIA_BY_ID` + `_build_exit_criteria_block()` compact one-line-per-element block (EXIT + NEXT + top failure modes + constitutional rule), injected into SYSTEM_MESSAGE via `<<CANONICAL_EXIT_CRITERIA>>` placeholder inside the UNIFIED STRUCTURAL REASONING section. Reasoner instructed to judge sufficiency against the element's EXIT criterion and set next_developmental_step to that element's NEXT operation. NEW field structural_reasoning.active_exit_criterion (model + schema).
- Frontend (DevelopmentPanel.jsx): teacher-language "Ready to move on when" line added to the "Where this fits in the writing" block, with a "✓ achieved — moving on" badge when sufficiency_for_next_step=sufficient. No schema names exposed.
- VERIFIED LIVE: growth-mindset thesis draft → active_exit_criterion = Definition element's exact canonical criterion ("The concept 'growth mindset' can be used in subsequent writing without confusing the reader"), next_developmental_step = use definition to sharpen thesis, sufficiency=not_yet; teacher panel renders "Ready to move on when …". Backend injects all 15 elements (placeholder replaced), no leftover token.

## Stage C — Student Coaching Renderer + Deterministic Validator (2026-06, DONE — verified; default exhaustive composition path)
- ROOT CAUSE (from architecture report): Stage B fused diagnosis + free-form student_facing_invitation in one call; nothing bound the coaching to scaffolding_control.primary_target, so Compass could diagnose "Thesis/Definition/Evidence" correctly yet produce generic content commentary.
- FIX: Added a separate Stage C after Stage B in `_run_engine` (exhaustive path only; triage/governance untouched). New pipeline: Submission → Domain Select → Prompt Assembly → Stage B (diagnosis+plan) → **Stage C (coaching renderer)** → Finalize → Teacher Review.
  - Stage B unchanged (still produces primary_target, required_dependency, sufficiency_for_next_step, next_developmental_step, active_exit_criterion, candidate invitation). student_facing_invitation kept for back-compat but NO LONGER the final student message on this path.
  - Stage C (`_render_coaching`, COACHING_RENDERER_SYSTEM, Claude Sonnet 4.6): receives ONLY the plan (student excerpt, primary_target, required_dependency, sufficiency, next_developmental_step, active_exit_criterion, candidate move, assignment). Renders the final coaching in the mandatory 5-function order: (1) NAME the writing element up front, (2) recognize emerging competence from the excerpt, (3) explain the structural need, (4) invite ONE learner operation, (5) state the dependency return path. Must not re-diagnose/re-target/teach subject matter/supply submittable text. If sufficient → acknowledge + advance to next element.
  - `_finalize_turn` writes Stage C output as turn.content; result["_stage_b_invitation"] retains the Stage-B text.
- DETERMINISTIC VALIDATOR (`_validate_coaching` + COACHING_SYNONYMS map): before saving, confirms (a) coaching names primary_target or an approved synonym; (b) if a dependency is active, coaching names it (synonym OR salient noun — lenient to element mis-inference); (c) ≥1 actionable learner operation; (d) no ready-made submittable sentence (recognition-quotes of the STUDENT'S OWN words are allowed; only suggestion-framed or non-student-origin long quotes flag). On fail → regenerate Stage C ONCE with an explicit correction message; Stage B is NOT rerun. Validator result recorded in _meta.stage_c_validator.
- VERIFIED LIVE (6 cases, external URL, each showing diagnosed target/dependency/exit criterion/next step/final coaching): (1) thesis direct → names thesis; (2) thesis needs definition → dependency-first w/ return path; (3) evidence lacks explanation → names explanation, bridges evidence→claim; (4) ambiguous "transition" draft → correctly triaged to controlling idea (higher leverage) and named it; (5) good-enough definition → sufficiency=sufficient, acknowledged + advanced to thesis; (6) wrong science fact → stayed on the WRITING element (controlling idea), asked student to reconsider WITHOUT supplying the fact. Validator + regenerate-once confirmed in logs; refined validator unit-tested (recognition-quote passes, supplied-sentence flags, generic-content regression caught). Latency note: Stage C adds ~5-14s on top of Stage B ~70-80s (separate from the latency-observation work, as instructed).

## Stage C leak fix + definition-in-service-of-essay refinement (2026-06, DONE — verified)
- BUG: regeneration correction text was concatenated into the Stage-C USER prompt, so the renderer sometimes meta-commented ("I need to flag something about the correction instruction…") and that leaked to students; the prior code also returned the leaking 2nd attempt.
- FIX (server.py): (1) the correction is now a HIDDEN developer note appended to the Stage-C SYSTEM message (never in the user turn); COACHING_RENDERER_SYSTEM gained an explicit OUTPUT DISCIPLINE clause (never mention/acknowledge instructions, corrections, validator, system prompt, own reasoning, or field terminology; begin directly with coaching). (2) Deterministic pre-persistence safeguard `_LEAK_RE` + `_contains_leak` + `_sanitize_coaching`: any student-facing coaching containing implementation/meta language (correction/instruction/validator/system prompt/"I need to…"/"I'm going to…"/"the correction…"/internal field names/"stage a|b|c"/etc.) is treated as a validation failure → regenerate once; if a leak survives, sanitize by dropping offending sentences; if nothing clean remains, fall back to the clean Stage-B invitation. Meta text is never persisted.
- REFINEMENT: COACHING_RENDERER_SYSTEM "WRITING BEFORE CONTENT" clause — for DEFINITION/concept targets keep emphasis on a WORKING definition the essay can USE (invite one-sentence definition in the student's own words for the reader); do NOT quiz the student on the subject-matter theory/psychology ("what does someone with this mindset believe/feel/do").
- VERIFIED: unit test (leak detected + sanitized correctly; clean coaching passes) + live definition turn — no leak, coaching frames the definition as serving the essay/reader and invites a writing move, names element + recognizes + explains + one operation + return path. Backend stable.

## Stage C pedagogy + latency-on-every-turn + Teacher Review rationale (2026-06, DONE — verified)
- TASK 1 (Stage C pedagogy, COACHING_RENDERER_SYSTEM rewrite): coaching now (1) ORIENTS to the larger goal first and briefly TEACHES it, (2) recognizes emerging work, (3) MOTIVATES the dependency before making it the focus + keeps the larger goal visible, (4) TEACHES each element (what it is + why writers use it, does/is contrast), (5) invites ONE WRITING operation (hard ban on topic/psychology quizzes like "what does someone with this mindset believe/feel/do"), (6) states the return path. Verified live: growth-mindset thesis → orients to thesis, motivates+teaches definition, writing-framed operation, return path; all checks pass, no content drift, no leak.
- TASK 2 (latency/noticing bug): root cause — getNoticing only fired on the first writing submit and beats were gated behind isFirstMoment, so revise/answer/explain turns showed NO interim observations. Fix: added fireNoticing() helper called in sendRevision/sendReply/sendExplain (resets pacing, refires); relaxed render gating to show NoticingBeats whenever observations exist (every turn); backend /noticing now uses the MOST RECENT student turn (any kind). Verified: endpoint returns 2 grounded observations post-change. NOTE: user observed this in the DEPLOYED app — these fixes are in the PREVIEW env; PRODUCTION needs a redeploy to pick them up.
- TASK 3 (Teacher Review): new DevelopmentPanel `instructional-rationale-block` at the top of the panel — a concise professional rationale in teacher language with 6 consistent sections: Instructional focus, Evidence in the writing, Developmental judgment, Instructional decision, Exit criterion, Next developmental step (derived from theory fields; no schema names). Verified rendering live.
- FIX: repaired a syntax crash (closing triple-quote dropped from COACHING_RENDERER_SYSTEM during the Task-1 edit) that briefly took the backend down; restored, backend healthy (HTTP 200).

## Stage C item 7 — Recognition-first sequence (2026-06, DONE — verified)
- Reordered COACHING_RENDERER_SYSTEM so every coaching turn now BEGINS with a genuine, evidence-based recognition of an authentic emerging strength (specific, grounded in the student's actual writing, connected to the next instructional move; NO empty praise like "Good job/Nice work/Great start"), THEN orients to the larger goal → explains why the dependency is necessary → briefly teaches the element → invites one learner writing operation → connects back to the larger goal (return path).
- New preferred sequence: (1) recognize emerging strength → (2) explain larger writing goal → (3) explain why dependency is necessary → (4) teach the element → (5) invite one writing operation → (6) connect back to the goal.
- Verified live (growth/fixed mindset draft): coaching opened "You've already done something real here — you named both mindsets and landed on a clear judgment…", tied it to "the seed of a thesis", oriented, explained the "because" dependency, invited one writing operation, and stated the return path; no empty praise. Backend healthy (HTTP 200).

## Stage C — Eliminate disciplinary overreach (2026-06, DONE — verified)
- New constitutional rule in COACHING_RENDERER_SYSTEM (WRITING BEFORE CONTENT — NO DISCIPLINARY OVERREACH) + reinforced in the Stage B Coaching Execution Contract: Compass must NEVER introduce disciplinary content, interpretations, or conceptual claims the student has not already expressed, unless the assignment/teacher explicitly supplied them. It must not name/teach the academic concepts underneath the topic (e.g. "the belief/attitude underneath the behavior", "the idea they hold that makes them stop", motivation, Dweck's theory). It teaches the WRITING operation and derives examples/questions from distinctions ALREADY PRESENT in the student's own writing (definition = does/is writing distinction).
- Verified live (growth/fixed-mindset draft): coaching contained NONE of the overreach phrases; it recognized the student's own contrast, taught thesis-as-contestable-claim, and derived the next writing operation directly from the student's own words ("you said a growth mindset means you keep trying — what does that lead to, in a way someone could dispute?"). No new disciplinary concepts introduced. Backend healthy (HTTP 200).
- Also (prior msg): first interim observation delay changed 3.5s → ~6.5s (5–8s window) in PublicPreview.

## Stage C — Canonical structural explanations (2026-06, DONE — verified)
- Added a stable "canonical_explanations" map (16 elements) to the knowledge base `backend/developmental_exit_criteria.json` (thesis, definition, evidence, explanation, transition, synthesis, controlling_idea, example, paragraph_purpose, communicative_purpose, overall_organization, introduction, reader_guidance, closure, sentence_purpose, sentence_relationships) using the user's exact phrasings where provided.
- Loaded as `CANONICAL_EXPLANATIONS`; `_build_coaching_plan` now looks up the canonical explanation for the target element and (if active) the dependency element via `_element_key_for`, and passes them into the Stage C prompt. COACHING_RENDERER_SYSTEM instructs the renderer to RETRIEVE AND ADAPT the canonical explanation (simplify to grade level, preserve instructional meaning) rather than invent a new theoretical description each turn — for consistent instruction and a coherent architecture of writing.
- Verified live: coaching adapted the canonical thesis explanation ("the main idea you want them to walk away believing") and the canonical definition explanation ("what something IS, not just what it does") while keeping recognition-first, no disciplinary overreach, the writing operation, and the return path. Backend healthy (HTTP 200).

## Stage C — Instructional-object organization + transferable strategy (2026-06, DONE — verified)
- Reworked COACHING_RENDERER_SYSTEM so every turn is organized around ONE named structural element with the sequence: (1) recognize authentic strength → (2) EXPLICITLY ANNOUNCE today's instructional object ("Today we're working on your definition") → (3) present the CANONICAL explanation → (4) teach ONE TRANSFERABLE WRITING STRATEGY (a general move writers use, transfers beyond this essay; never disciplinary content) → (5) APPLY it to the student's own writing (one learner operation) → (6) explain how mastering the element supports the LARGER ARCHITECTURE (+ return path when a dependency is active).
- Added constitutional clause: every interaction must leave the student knowing WHAT element, WHY it matters, at least ONE transferable strategy to construct it, and HOW it fits the essay's architecture — "Compass is building writers, not simply improving essays."
- Verified live: coaching opened with grounded recognition, announced "Today we're working on your definition", gave the canonical does/is explanation, taught a transferable strategy ("ask yourself: if my reader has never heard this term, what would they need to understand first?"), applied it to the student's own sentence, and connected to the architecture + return path. No overreach. Backend healthy (HTTP 200).

## DECISION / ROADMAP — Learner representation deferred until Writing Knowledge Base is complete (2026-06)
- REJECTED: a "Strategies you've learned" checklist of isolated tips. Goal is an ORGANIZED understanding of how essays are constructed, not accumulated techniques.
- FUTURE (deferred): after a session, represent three linked things per element practiced: (1) the STRUCTURAL ELEMENT (e.g., Thesis); (2) the TRANSFERABLE STRATEGY practiced (e.g., "Commit to one central claim"); (3) HOW that element relates to the LARGER ARCHITECTURE (e.g., "A thesis organizes every body paragraph"). Over time this should grow into a coherent representation of the architecture of writing, NOT a checklist.
- PREREQUISITE (do first): complete the WRITING KNOWLEDGE BASE with four first-class parts — (a) structural elements [DONE: developmental_exit_criteria.json], (b) structural RELATIONS [TODO], (c) instructional STRATEGIES [TODO — currently emergent in the Stage C prompt, not stored], (d) canonical EXPLANATIONS [DONE]. The completed KB must DRIVE whatever learner-facing representation we build. Do NOT implement any cumulative learner record before the KB is complete.

## P0 Latency Fix — Decouple Save from Evaluate in "My Ideas" (2026-06-29, DONE — verified)
- Fixed Issue 1 (interaction chaining latency). Root cause: `MyIdeasWorkflow.jsx` `persistIfDirty()` called `otIdeasInteract` (full Stage-B `_ideas_reason` LLM cycle, ~30s) just to save the typed answer before navigating — so Next / Previous / See my understanding / Build my ideas each paid a full LLM roundtrip.
- Established the instructional lifecycle (per user): **draft** (typed, auto-saved, never evaluated) → **shared** (student clicked "Share with my coach"; Stage B/C ran) → **revised** (edited after coaching; can be shared again).
- Backend (`organizing_thought.py`): added `state` to `_blank_response()` (default "draft"); new lightweight `POST /api/ot/{id}/ideas/save` (persist pass1/pass2 text only, NO LLM; sets state revised-if-previously-shared else draft); `ideas/interact` now marks state="shared". `IdeasSave` model added.
- Frontend: `otIdeasSave` added to `api.js`; `MyIdeasWorkflow.jsx` `persistIfDirty` repointed from `otIdeasInteract` → `otIdeasSave`. "Share with my coach" (the `share` fn) is now the ONLY action that invokes coaching.
- Verified via curl on preview URL: cheap save 0.12s (was ~30s), state="draft"; Share → coaching message + state="shared"; edit-after-share cheap save → state="revised"; all persisted via GET. Frontend compiles + OT preview renders cleanly. Frozen engine + coaching quality unchanged.


## Instructional-Experience Refinement — Wait-state, Coaching Orientation, Teacher Review mirror, Latency profile (2026-06-29, DONE — verified; engine FROZEN)
- Focus: refine the STUDENT COMPOSITION experience + TEACHER REVIEW together from the same Stage B plan (no Stage 2 / Chapters 7-9 yet).
- (1) IMMEDIATE ACKNOWLEDGEMENT preserved — the grounded `/noticing` interim observations (Haiku, ~1.9s) still lead every turn.
- (2) WAIT PERIOD is now INSTRUCTIONAL, not silent. New canonical library `frontend/src/lib/writerMetacognition.js` — ~24 writer-metacognition reflections (how experienced writers think, all about WRITING operations) + `FOCUS_TRANSITIONS`. `PublicPreview.jsx` `MetacognitionCue` replaces the old generic "Thinking with you…" dots: rotates general writer-thinking lines (~4.8s each) then bridges to Compass's instructional focus ("Now I'm looking for the one structural element that will help you make the greatest progress as a writer."). Progression = writer thinking → instructional focus; NEVER narrates Compass's internal reasoning/decision.
- (3) COACHING ORIENTATION strengthened (`COACHING_RENDERER_SYSTEM`, Stage C): step 2 now frames "an essay is built from a few structural elements that work together" + names where today's element sits; step 6 ALWAYS closes with WHERE THE WRITER GOES NEXT (not only when sufficient). Constitutional principle updated to the four takeaways: WHAT / WHY / WHERE IT FITS / WHERE NEXT (+ one transferable strategy). Verified live: coaching opened with strength → architecture framing → named object (Explanation) → why → transferable strategy → application to the student's own cost reason → next step.
- (3b) TEACHER REVIEW is now the professional mirror of the student coaching, from the SAME Stage B plan. `_curate_case` (server.py) adds `explanation.architecture` (element_relationships/dependencies; falls back to instructional_reasoning element_communicative_purpose + canonical_performance_structure for preview's trimmed output) and `explanation.next_step` (next_developmental_step / future_opportunity). `TeacherReview.jsx` + `TeacherReflection.jsx` render two new Q-blocks: "Where does this element fit within the architecture of the essay?" (`review-architecture`/`reflection-architecture`) and "What should the writer learn next…?" (`review-next-step`/`reflection-next-step`).
- (4) WRITING-not-content boundary reaffirmed (unchanged enforcement; metacognition lines + orientation stay on writing operations).
- (5) LATENCY PROFILE (`test_reports/coaching_pipeline_latency.md`) from a live turn — per-stage: submission→placeholder 0.13s, acknowledgement(noticing) 1.9s (now instrumented, parallel), Stage A selector 3.92s, prompt build 0.001s, Stage B reasoner 46.83s (~70%), Teacher Review gen 0.14s (off critical path), Stage C renderer 16.65s (~25%), DB read/write 0.001s, poll lag ≤2.5s; TOTAL ≈67s. Stage B dominates (frozen).
- Verified: backend via curl (coaching on-spec, latency logs, teacher-reflection architecture+next_step populated); frontend testing_agent iteration_44 = 100% (metacognition cue writer-thinking not AI-narration, acknowledgement preserved, Teacher Review 4-question mirror on students A/B/C). No engine change. Minor note: final coaching container has no data-testid (not blocking); Teacher Reflection ?preview=teacher path not e2e-clicked (needs a full ~60s turn; backend data pre-verified).


## Phase II (Performance) — Steps 1 & 2: KB instructional-completeness audit + deterministic plan hydrator (2026-06-29, DONE — verified; Stage B FROZEN)
- Context: profiling showed the wait is a Stage B (~47s, ~70%) + Stage C (~8–17s) problem. Stage C investigation (`test_reports/stage_c_investigation.md`): ONE Sonnet call (~8–10s) that doubles to ~17s ONLY when the deterministic validator forces ONE regeneration (~33% of turns, tripping the "ready-made sentence" rule); validator=0ms; no hidden retries; Teacher Review independent (0.14s, off-path). Full Phase II plan + deterministic/judgment classification: `memory/PHASE_II_PERFORMANCE_PLAN.md`.
- STEP 1 — KB instructional-completeness audit (`backend/tests/kb_audit.py` → `test_reports/kb_instructional_completeness_audit.md`). Checks all 8 dimensions per canonical element (canonical explanation, purpose, structural relationships, dependency relationships, developmental difficulties, instructional strategies, observable indicators, exit criteria). Initial: 11/16. Gaps CLOSED (`backend/tests/kb_close_gaps.py`): added a `Synthesis` instructional object (was missing) + a whole-essay `thesis` exit criterion; remapped `sentence_relationships`→Coherence IO; fixed audit false-positives on root elements (empty dependencies = complete). Result: **16/16 instructionally complete, 0 gaps.**
- STEP 2 — Deterministic PLAN HYDRATOR (`hydrate_element(element_key)` + `KB_ELEMENT_MAP` in server.py). Given the element the LLM CHOSE, assembles ALL deterministic scaffolding straight from the KB (explanation, purpose, performance_structure, relationships, dependencies, difficulties, strategies, indicators, exit_criterion, next_operation) — the LLM no longer needs to re-type it. Wired into (a) `_build_coaching_plan` + `_coaching_plan_prompt` — Stage C now teaches from canonical KB purpose/performance-structure/strategy/exit-criterion (prefers KB, falls back to LLM); (b) `_curate_case` — Teacher Review architecture/exit/purpose are KB-hydrated keyed on the focus element. Stage B UNTOUCHED (behaviorally identical this step).
- Verified live: Stage C coaching equivalent/IMPROVED — now uses the canonical thesis purpose near-verbatim ("the single main idea your whole essay exists to establish"), on-spec sequence, no leaks. Teacher Review IMPROVED — architecture now `[Purpose, Central Claim, Introduction, Audience Awareness]` + canonical thesis exit criterion (previously empty in preview + fixtures; all 6 fixtures now populate architecture/next/exit). Backend clean startup; JSON valid.
- DEFERRED (per user, gated): Step 3 = slim Stage B OUTPUT schema (the ~47s win) — only after KB substantially complete + hydrator stable; must pass full 66-case benchmark Compare-Two-Runs vs frozen baseline, roll back on any regression. Step 4 = Stage C speed (trial Haiku 4.5 for the render + reduce regeneration rate) — gated on validator pass + no quality/leak/target regression. Fine-grained Stage C timers (gen1/validate/gen2/calls) added and retained.


## Phase II Step 4a — Stage C validator refinement ("ready-made sentence" rule) (2026-06-29, DONE — verified)
- Problem (from Step-4 investigation): Stage C doubled to ~17s only on validator-forced regeneration; the "ready-made" rule flagged ANY non-student long quote → false positives on legitimate scaffolds.
- Refined `_readymade_violation` + `_quote_is_scaffold` (server.py). A quote is an ACCEPTABLE scaffold (not flagged) when it is: the student's own text (contiguous substring), a revision question (ends `?`), a sentence frame/placeholder (`___`, `[…]`, `your reason/claim…`), attributed to a reader/other (attribution cue precedes it), or an off-topic structural example (no shared salient noun with the student's writing). Otherwise an on-topic, unattributed, non-question, non-frame polished sentence IS flagged. Constitutional rule preserved.
- Fixed the recognition test: use contiguous SUBSTRING match, not word-overlap (topic vocabulary was inflating overlap and hiding a genuine on-topic answer).
- Results — labeled set (`tests/stage_c_validator_cases.py`, 10 cases incl. adversarial): FP 4/6→**0/6**, FN **0**, TP **4/4**. Live (6 identical gen1 drafts, old-vs-new logged): ready-made regeneration **83%→17%**; avg Stage C latency **~14.7s→~9.3s** (~5.4s/turn saved). Manually verified the 4 now-accepted drafts contain only questions/frames/reader-quotes/student's-own-words — no submittable answers.
- Retained: `_readymade_violation_legacy` + `[stage_c_validator] readymade_old/new` monitoring log; fine-grained Stage C timers. Report: `test_reports/stage_c_validator_refinement.md`.
- KB corrigibility (per user): "16/16 complete" = structurally populated, NOT permanently frozen. KB JSON files remain versioned/editable (Synthesis IO carries `enrichment_version`) and `tests/kb_audit.py` is re-runnable; relationships/strategies/difficulties/indicators/exit-criteria stay revisable as real cases are evaluated.
- NEXT (Step 4b, GATED): trial Claude Haiku 4.5 for the Stage C render vs Sonnet on IDENTICAL hydrated plans + IDENTICAL refined validator, judged on the full acceptance-criteria list; adopt only if quality holds and latency drops meaningfully.


## Phase II Step 4b — Stage C render model A/B: Sonnet 4.6 vs Haiku 4.5 (2026-06-29, DONE — verdict: RETAIN SONNET)
- Trial held Stage B / hydrated plan / refined validator / test set CONSTANT; only the render model varied. Harness `tests/stage_c_model_ab.py` runs Stage A+B ONCE per case, builds the plan once, renders with BOTH models from the identical plan + identical COACHING_RENDERER_SYSTEM + identical validator (with production single-regeneration). 5 representative drafts.
- Aggregate: avg render latency Sonnet 8.13s vs Haiku 3.28s (**Haiku 2.5× faster**); regeneration 0/5 both; validator pass 5/5 both; no leak/target-drift/ready-made either; recognition-first + WHAT/WHERE/NEXT + one-operation 5/5 both; avg length Sonnet 244w vs Haiku 188w (−23%).
- Instructional fidelity EQUIVALENT by construction (object/strategy/canonical-explanation/architecture/exit/next come from the shared plan+KB; every case = Thesis, same explanation/exit/next for both).
- DECISIVE REGRESSION: the explicit essay-architecture FRAMING in the orientation ("an essay is built from a claim, the reasons that support it, and how they connect") appears in **Sonnet 4/5 but Haiku 0/5** — Haiku jumps from recognition straight to the object. This is the user-flagged "most instructionally distinctive" part; its systematic loss (5/5) is a pedagogical regression. Haiku's shorter length = part economy, part substance loss (the framing + less specific recognition).
- Decision rule → equivalent quality? NO (orientation). Latency better? yes. Validator unchanged? yes. Recurring quality reduction? yes → **RETAIN SONNET 4.6**. Production render model UNCHANGED (still Sonnet at server.py `_generate`). Report: `test_reports/stage_c_model_ab_report.md`.
- Follow-ups (not adopted): (1) add a deterministic validator check that the orientation contains the architecture framing — would harden Sonnet AND enable a fair NEW Haiku trial (framing-enforced); (2) Step 3 (slim Stage B) remains the biggest gated win.


## Phase II Step 4c — Deterministic Orientation Plan + six-function orientation validator (2026-06-29, DONE — verified)
- Moves the orientation STRUCTURE out of the model and into Compass. `_build_orientation_plan()` + `_orientation_block()` (server.py) assemble an ordered six-slot Orientation Plan from the hydrated plan/KB and inject it into the Stage C render prompt; the renderer now chooses WORDING, not instructional architecture. Slots: (1) recognition [student-specific, renderer supplies], (2) instructional object [KB], (3) canonical explanation [KB], (4) architecture fit [KB relationships/purpose + "essays are built from interacting elements" framing], (5) transferable strategy [KB], (6) transition into ONE learner operation.
- `_orientation_functions()` deterministically checks all SIX functions are present in the rendered coaching; `_validate_coaching` now appends a "missing orientation function(s)" issue → triggers the single regeneration if any is absent. Lenient detectors CALIBRATED against the 10 Step-4b A/B outputs: Sonnet 5/5 and Haiku 5/5 pass (no new regeneration on good drafts), and negative tests confirm it flags genuinely missing recognition/architecture/strategy.
- Live verification (3 turns): 3/3 pass with calls=1 (no regeneration spike), all six functions present, latency unchanged (gen1 5.8–7.3s). Coaching quality high — recognition → object → canonical explanation → architecture ("everything else grows from and points back to") → strategy → single operation.
- Architectural significance (per user): pedagogy now resides in Compass — Knowledge Base + deterministic hydrator + Orientation Plan + validator + rendering constraints — while the LLM is an expert instructional INTERPRETER, not the repository of instructional knowledge.
- PHASE II COMPLETE except Step 3 (gated). Before Step 3: FREEZE current hydrated build as canonical baseline, TAG the version, preserve benchmark outputs; then slim Stage B and prove equivalence of instructional JUDGMENTS (object, bottleneck, evidence, strategy, dependency, sequence, exit, next) + student coaching + Teacher Review against the frozen baseline via the 66-case benchmark. Adopt only if judgment stays equivalent.


## Step 3 (Stage B slim) — Phases A/B done; Group 1 attempted → NOT CERTIFIED → ROLLED BACK (2026-06-29)
- PHASE A (Freeze): version manifest with prompt/KB sha256 hashes (`memory/phase2_baseline/MANIFEST.md`); baseline+comparator harness `tests/stage_b_baseline.py`; three-layer canonical spec `memory/COMPASS_THREE_LAYER_ARCHITECTURE.md`; full-66 pre-edit baseline capture launched (`stage_b_baseline_full66.json`). Migration ledger `memory/phase2_baseline/STEP3_MIGRATION_LEDGER.md`.
- PHASE B (Analyze): field-by-field Judgment/Deterministic/Mixed inventory `memory/stage_b_field_inventory.md`. Group 1 = element_communicative_purpose + canonical_performance_structure; Group 2 = element_relationships + developmental_dependencies + active_exit_criterion.
- PHASE C (Group 1 attempt): removed the two fields from the Stage B OUTPUT (reasoning guidance kept; hydrator supplies them). Certified with a NOISE-FLOOR-controlled 12-case smoke (A1 pre-edit vs B1 post-edit vs B2 2nd post-edit run).
- RESULT: dependency (50%), sequence (83%), sufficiency (75%), and free-text fields matched the noise floor exactly → the engine has large inherent run-to-run nondeterminism (absolute thresholds like ≥98% dependency are unreachable with ZERO code change; certification MUST be noise-floor-relative). BUT **object** = 100% noise floor vs 83% migration → 2 cases (TC49, TC66) shifted `thesis→communicative_purpose` (same direction, not random). object is the foundational judgment required at 100% → **Group 1 FAILED certification.**
- ACTION: rolled back (SYSTEM_MESSAGE hash restored to frozen `7ecc6056f891af30`, verified). App back to certified baseline.
- KEY FINDING: element_communicative_purpose + canonical_performance_structure are NOT pure deterministic output — they are REASONING SCAFFOLD; making the model articulate them stabilizes object selection. Reclassified as **Mixed (reasoning-critical), KEEP in Stage B** (still ALSO hydrated downstream for consistency). Comparator upgraded to judge categorical judgment (exact) vs free-text (Jaccard similarity), not textual identity.
- NEXT: Group 2 fields (relationships/dependencies/exit) are more plausibly pure output (cross-references/criteria, unlikely to scaffold object selection) — test ONE field at a time against the noise floor; certify only if object stays at its ~100% noise floor. Full-66 baseline capture completing in background as the permanent rollback record.


## Phase II · Step 3 · Group 2 · Field 1 — `element_relationships` (2026-07-29, NOT CERTIFIED → ROLLED BACK)
- Re-ran the G2F1 ablation with CLEAN, provenance-stamped controls after discovering the prior session's runs were ambiguous (both "baseline" and "candidate" had imported `server.py` while the candidate diff was on disk → identical code). Killed those runs, discarded their outputs.
- Permanent enhancement (per owner): `tests/stage_b_baseline.py` now stamps every capture with git commit + `server.py` sha256 + SYSTEM_MESSAGE hash; `compare` prints both files' provenance and auto-flags same-code (noise-floor) comparisons. Every benchmark result is now tied to the exact code that produced it.
- Controls: FROZEN `d5686a0` (server.py `2cc4bdcb…`, sys-msg `1c485e2c13d7b8ff`) vs CANDIDATE `e0fa10b` (server.py `68ae56c7…`, sys-msg `3c72345527a89173`). 5 runs each; files `stage_b_g2f1_{baseline_d5686a0,candidate_e0fa10b}_run{1..5}.json`.
- FINDING: frozen `object` noise floor is **83% (10/12), NOT 100%** (baseline-noisy cases TC55/TC61). Certification rule corrected to PER-CASE. Decisive result: **TC37** & **TC49** are frozen-stable `thesis` 5/5 but collapse to 2/5 under the candidate (divergent directions) → genuine destabilization above noise. TC66 unchanged (4/5 both) = correct control.
- VERDICT: `element_relationships` is **Mixed (reasoning-active)** — articulating element relationships stabilizes object selection (same lesson as Group 1). ROLLED BACK to frozen; backend health 200. KEEP in Stage B output + keep hydrating downstream.
- Implication: the structural-relation family (relationships/dependencies) is likely all reasoning-active; `developmental_dependencies` / `active_exit_criterion` not started, expect the same. Full detail: `memory/phase2_baseline/MIGRATION_LOG.md` (ATTEMPT #2) + `STEP3_MIGRATION_LEDGER.md` (Group 2).

## SPRINT 1 — Instructional State, Evidence & Audit Foundation (2026-07-30, CERTIFIED/FROZEN)
- Built an ADDITIVE, isolated persistence + audit layer so every future instructional decision traces to
  student EVIDENCE + a selected INSTRUCTIONAL OBJECT + a SUPPORT LEVEL + an EXIT CRITERION + a REQUIREMENT
  ID. Frozen Stage B/C engine and all existing features untouched (SYSTEM_MESSAGE still 1c485e2c13d7b8ff).
- New module `backend/compass_foundation.py`: 3 collections (`instructional_states`, `evidence_records`,
  append-only `audit_events`); models distinguish OBSERVED / HYPOTHESIZED / UNKNOWN and never store a
  hypothesis as fact; 6 machine-checkable guards (VA-05, DS-01, DS-02, TC-01, VA-06, VA-07); read-only
  diagnostic trace + audit endpoints gated to teacher/admin; read-only migration of legacy sessions.
- Frontend `DiagnosticTrace.jsx` (`?trace=<state_id>&role=teacher`) — minimal read-only panel; students 403.
- Tests A–G + DS-02 + auth + happy-path + append-only correction = 11/11 PASS. Migration: created 50,
  limitations logged 150, zero legacy records modified.
- Endpoints: POST `/api/instructional-state`, GET `/api/instructional-state/{id}`, POST `.../revision`,
  POST `.../evidence`, POST `.../override`, POST `.../advance`, GET `.../trace`, GET `.../audit`,
  POST `.../audit/correct`, POST `/api/admin/foundation/migrate`. Details: SPRINT1_CLOSURE_REPORT.md.
- FREEZE: foundation certified; later sprints consume its interfaces but must not rewrite it without an
  explicit failed requirement.
- NOTE: Stage B optimization thread (F1 reword) was benchmarked + ROLLED BACK before this sprint; F1
  destabilized borderline TC37/TC49 (same cases as G2F1). Open methodology question (perturbation noise
  floor vs sampling noise floor) is PARKED pending owner direction — see MIGRATION_LOG.md ATTEMPT #3.

## SPRINT 2 — Engine Bridge (2026-07-30, VERIFIED)
- The live coaching engine is now both CONSUMER and PRODUCER of the Sprint-1 persistent instructional
  state: every turn READS state at start (audit `turn_started`) and WRITES the full structured decision +
  evidence + audit at end, BEFORE the learner-visible turn is persisted. No new instructional logic;
  frozen Stage B/C untouched (SYSTEM_MESSAGE still 1c485e2c13d7b8ff).
- New architectural rule implemented: "No instructional decision may exist only in generated text; it must
  be in persistent structured state before presentation." Enforced by ordering the producer write before
  the session DB write; audit `instructional_turn.generated_response` ties the invitation to the decision.
- Bridge in `compass_foundation.py`: `begin_instructional_turn` (read), `record_instructional_turn`
  (write, maps theory→state/evidence/audit), `get_or_create_state_for_session`, plus
  GET `/api/instructional-state-by-session/{session_id}`. Added optional backward-compatible state fields
  (reason_for_selection, prerequisite_status, current_learner_task, last_learner_response,
  last_revision_produced, exit_criterion_description, turns_recorded).
- server.py: two best-effort hooks in `_run_reasoning` (consumer read + producer write); never blocks a
  live turn. All 18 required per-turn fields persisted; evidence kept OBSERVED/HYPOTHESIZED/UNKNOWN;
  requirement IDs VA-05/DS-01/DS-02/VA-06/VA-07 (+TC-01) recorded per turn.
- Tests: sprint2_bridge_test.py 12/12 on a REAL live turn; Sprint-1 suite still 11/11. Trace renders real
  bridged data. Details: SPRINT2_CLOSURE_REPORT.md.
- DEFERRED per owner: Wire-Trace-into-nav; full spec reconciliation (until Canonical Spec v2.1 frozen);
  Stage B perturbation methodology (research). ONLY Sprint 2 was authorized.

## FUTURE ARCHITECTURAL REQUIREMENT — BACKLOG ONLY (DO NOT IMPLEMENT)
Recorded 2026-07-30 (owner note; NOT authorized for implementation).
- Sprint 2 is ACCEPTED and FROZEN. The persistence-before-presentation rule is implemented on the
  production (`exhaustive`) path exactly as intended.
- **Streaming/triage invariant (future):** If any future streaming or triage mode becomes part of the
  PRODUCTION system, the architectural rule still holds: *No learner-visible instructional decision may
  appear before a corresponding structured instructional state has been created.* If streaming requires
  earlier rendering, the system must FIRST create a PROVISIONAL instructional state, then UPDATE it when
  the final decision is reached. Persistent instructional state remains the authoritative representation
  of Compass at all times.
- This is a future architectural requirement only. It requires NO modification to the certified Sprint 2
  implementation. (Relates to the Sprint-2 known-nuance: today the opt-in `triage_experimental` path can
  render a partial invitation before the finalize-time structured write; that path is non-production.)
- No further implementation sprints are authorized until Compass Canonical Specification v2.1 is completed
  and frozen. Effort returns to architecture, not implementation.

## SPRINT 3 — Instructional Decision Engine (2026-07-30, VERIFIED; recommend ACCEPT/FREEZE)
- New isolated `backend/compass_decision_engine.py`: each live turn runs an explicit evidence-based A–F
  selection over the frozen engine's structural reasoning, selects exactly ONE target (or explicit
  blocked/NO_TARGET_SUFFICIENT), records strengths/candidates/prereqs/priority-rationale/deferred/
  confidence/uncertainty, and writes it to persistent state BEFORE the learner response (one additive call
  in server.py after the Sprint-2 bridge). No new dialogue/scaffolding; CIO KB untouched; Stage B frozen
  (1c485e2c13d7b8ff).
- Guards DE-01 (one active target or explicit blocked), DE-02 (OBSERVED support), DE-03 (no HYP/UNK as
  fact), DE-04 (block not invent), DE-05 (no prohibited attribution influences selection), TC-02 (teacher
  override honored + engine recommendation preserved separately + audited).
- Additive optional state fields (demonstrated_strengths, strength_status, candidate_instructional_objects,
  selected_instructional_object, selected_object_definition, structural/conceptual_prerequisite_status,
  observed_selection_evidence, priority_rationale, deferred_targets, decision_status, decision_confidence,
  decision_uncertainty, engine_recommendation, decision_requirement_ids, decision_timestamp). Trace panel
  extended (teacher/admin-only, no scores). New endpoint POST /api/instructional-state/{id}/target-override.
- Tests: sprint3_decision_tests.py 16/16 (T1–T12 + guards + TC-02 live). Regression Sprint1 11/11,
  Sprint2 12/12. Details: SPRINT3_CLOSURE_REPORT.md. Sprint 4 NOT begun.

## CANONICAL SPEC v2.1 — RECONCILIATION ITEM (RECORDED 2026-07-30; NOT YET IMPLEMENTED)
Owner architectural clarification. Sprint 3 accepted & frozen; this does NOT reopen Sprint 3.
- **Problem:** the interim `NO_TARGET_SUFFICIENT` value mixes decision VALIDITY with instructional
  NECESSITY. It exposed a specification gap, not a software defect.
- **Resolution (to implement during v2.1 reconciliation):** make the state model orthogonal by separating
  two concepts:
  - `decision_status` ∈ { READY, BLOCKED_INSUFFICIENT_EVIDENCE, BLOCKED_CONTRADICTORY_EVIDENCE,
    BLOCKED_PREREQUISITE_UNKNOWN, TEACHER_OVERRIDE }  ← REMOVE `NO_TARGET_SUFFICIENT`.
  - `instructional_need` ∈ { NEEDS_INSTRUCTION, NO_CURRENT_INSTRUCTIONAL_TARGET }  ← NEW field.
- **Mapping:** the engine may reach `decision_status = READY` with
  `instructional_need = NO_CURRENT_INSTRUCTIONAL_TARGET` (strong writing / no invented weakness). Today's
  `NO_TARGET_SUFFICIENT` maps to exactly that pair. Current BLOCKED_* stay as-is with
  `instructional_need = NEEDS_INSTRUCTION` (or left unset while blocked).
- **Guards affected later:** DE-01 must accept (READY + NO_CURRENT_INSTRUCTIONAL_TARGET + 0 active targets)
  as valid, alongside "exactly one active target" and blocked states.
- **Status:** RECORDED for Canonical Specification v2.1. No further implementation work is authorized
  until this clarification is incorporated into v2.1.

## Canonical Migration & Legacy Pruning Plan (2026-06, DONE — read-only, NO production code changed)
- Produced `/app/memory/CANONICAL_MIGRATION_AND_PRUNING_PLAN.md` (732 lines): the authoritative engineering roadmap to move the FROZEN Decision Engine V2 off its legacy/canonical hybrid onto the Canonical Paragraph Curriculum as the sole source of truth. NO edits to `compass_structure_engine.py` or any engine code.
- Contents: strategic overview (zero-knowledge), final target architecture, design principles, a phase DEPENDENCY GRAPH (Curriculum→Selection→Sufficiency→Dialogue→Teacher Review→Legacy Pruning) with per-edge causal justification, and 5 deliverables — A) legacy-disposition table (11 MINIMAL_OBJECTS + selection/dialogue machinery tagged KEEP/MIGRATE/PRUNE/RECONCILE), B) file-by-file pruning map w/ exact symbols + line refs, C) staged plan (P0-a interim → P0-b authoritative → P1 → P2 → P3 → P4 primaries → P5 subordinates), D) missing canonical authoring gaps (Gap-1 subordinate models, Gap-2 Explanation placement, Gap-3 sentence-level scope, Gap-4 cross-structure leverage procedure, Gap-5 Paragraph-Main-Point→Thesis unity), E) validation corpora (C-SELECT/SUFFICIENCY/DIALOGUE/TEACHER/ALL/SUBORDINATE) enforcing parity-before-pruning.
- Key findings: (1) selection is 100% legacy today and is the ROOT of the hybrid contradiction (priority inversion — legacy ranks Evidence>Elaboration, canonical makes Elaboration principal; plus the forbidden "must be contestable" rule re-entering via `_SEL_SYS`); (2) P0-a (canonical selection, interim leverage), P1, P2, P3, and the primary-facing part of P4 are executable now behind flags with `COMPASS_REASONING_MODE=exhaustive` as global rollback; (3) P0-b and all of P5 are BLOCKED on curriculum-authority authoring (Gaps 1–5) and must not be invented by the execution agent.

## Instructional Decision Making model stored + FIRST-TURN acknowledgment correction (2026-06, DONE)
- PART 1 (store verbatim, wired to nothing): stored the supplied Canonical Model **Instructional Decision Making v1.0** at `backend/canonical_models/instructional_decision_making.json` (provenance=canonical_supplied); added read-only loader + accessors to `compass_curriculum.py` (`get_decision_model`, `get_decision_section`, `decision_foundational_principles`, `decision_order`, `canonical_decision_questions`, `is_decision_model_ready`, `decision_model_report`, `validate_decision_model`). Validation OK — all 9 sections present/non-empty (7 principles, 6 order steps, 9 decision questions). NO change to select_structure/priority/sufficiency/dialogue-selection/Teacher Review/UI/DB/audit/OT. This RESOLVES migration-plan Gap-4 (cross-structure leverage). P0-b marked technically UNBLOCKED but NOT authorized. One implementation ambiguity reported (Step-6 four-level taxonomy Discovery/Scaffold/Rescue/Closure vs engine's DISCOVERY/RESCUE binary + action list) — deferred to P0-b, not resolved.
- PART 2 (narrow instructional-voice fix): rewrote ONLY the FIRST-TURN accomplishment step (both mirrors: `_DLG_SYS` step 1 + `generate_dialogue` first-turn `_mode_block`) into a three-beat STRENGTH-OF-ACHIEVEMENT ACKNOWLEDGMENT — (a) state completed achievement affirmatively, (b) explain its intellectual value, (c) show it as the foundation for the next structure — with an explicit AVOID list (no "you've already/beginning to/something to build from/work with/on the right track/good start but/raw-material/progress" language) and a no-overstatement guard. No change to target selection, canonical content, the rest of the first-turn sequence, continuation mode, sufficiency, Discovery/Rescue, DB, audit, API, or UI.
- Validation: generated live first-turn responses (Sonnet, real engine) for all 5 canonical structures before & after. All 6 checks pass — genuine completed achievement + intellectual value + foundation-for-next-structure; zero banned phrases (grep=0); not overstated; remainder of the canonical teaching sequence unchanged ("next task is to develop…" present in all 5). Backend healthy, no import errors.

## Phase P0-b — Canonical Selection migration (behind CANONICAL_SELECTION flag) + Step-6 taxonomy (2026-06, DONE, flagged/off-by-default)
- STEP-6 TAXONOMY: recorded the authorized interpretation as `implementation_notes` in `instructional_decision_making.json` (supplied canonical text untouched): Support Mode = DISCOVERY(default)/RESCUE(after difficulty or explicit request); Scaffolding = adjustable support WITHIN a mode (not a peer mode); Exit Status = CONTINUE vs CLOSURE/ADVANCE (closure is an exit decision, not a teaching mode). Accessor `decision_implementation_notes()` added.
- CANONICAL SELECTION (P0-b): the single consolidated `select_structure()` now branches to `_select_structure_canonical()` when env `CANONICAL_SELECTION` is truthy (default OFF → learner path unchanged, legacy selector retained for rollback/comparison; no parallel public selector). Canonical path ranks ONLY the five primaries (Opening/Thesis/Elaboration/Evidence-Example/Conclusion) using canonical definitions/functions/`structural_dependencies`/`structural_requirements`/`developmental_variations`/`observable_decision_questions`/`developmental_sufficiency` + the Instructional Decision Making model (One-Thing/Highest-Leverage/Dependency/Sufficiency/Recursive) + a strict RESTRAINT/When-Not-To-Teach clause. Legacy rules removed from the canonical path (contestability, mandatory hook, Evidence>Elaboration priority, conclusion-as-restatement, Paragraph-Main-Point/Central-Claim-as-separate, fixed surface order). Explanation/Definition/Transition/Sentence/Qualification/Comparison/Analogy explicitly OUT OF SCOPE and non-selectable; Explanation does NOT map to Elaboration.
- PROVISIONAL JUDGMENT: canonical selection emits selected + developmental_variation + OBSERVED evidence / HYPOTHESIZED interpretation / UNKNOWN, plausible alternative, confidence (high/med/low, no numeric probabilities), what-next-response-reveals, invitation_intent (advance/clarify/both), integration_signal (ok/weak/repeatedly_failing, evidence only — NO OT routing). Recorded via existing state fields + `instructional_analysis.provisional_judgment` (NO schema/DB/audit/UI/dialogue/Teacher-Review changes). `resolve_structure`/`retrieve_object` made canonical-aware so downstream converges without a second retrieval path.
- Files/symbols: `compass_curriculum.py` (+`decision_implementation_notes`); `compass_structure_engine.py` (`_canonical_selection_enabled`, `_CANON_ALIAS`, `_canonical_or_none`, `_canonical_primary_digest`, `_decision_model_digest`, `_CANON_SEL_SYS`, `_select_structure_canonical`, branch in `select_structure`, canonical-aware `resolve_structure`/`retrieve_object`, provisional merge in `run()`); `canonical_models/instructional_decision_making.json` (+implementation_notes).
- VALIDATION (C-SELECT, 13 cases, `backend/test_cases/c_select_cases.json`; runner + artifacts `test_reports/c_select_report_p0b.txt` + `c_select_results_p0b.json`): under the canonical-validity standard, 13/13 canonically valid — 5 SUPERIOR (canonical fixes legacy misreads: topic-as-thesis, diffuse points, thin-elaboration, direct-thesis-no-opening, multi-weakness), 5 AMBIGUOUS-OK (incl. strong paragraph → CLOSURE with sufficiency=reached after the restraint clause; no invented weakness), 3 where canonical chose a defensible Elaboration/Thesis over a stricter expected key (CS04/05/10) — these demonstrate the intended inversion (Elaboration is PRINCIPAL; Evidence/Conclusion are not selected while elaboration is the limiting structure). No case canonically worse than legacy.
- CALIBRATION WATCH (not a blocker): canonical selector has a strong, by-design bias toward Thesis→Elaboration and is conservative about advancing to Evidence/Conclusion; confirm with a broader corpus that it advances when elaboration is genuinely sufficient (CS13 closure is reassuring evidence it does).
- SAFETY: flag OFF by default; legacy knowledge NOT pruned; dialogue/Teacher-Review/interim-cards/UI/DB/audit/OT unchanged; `COMPASS_REASONING_MODE=exhaustive` global rollback intact. NOT yet default learner path (awaits broader validation + explicit authorization).

## Canonical Manual Test Path — isolated end-to-end preview (2026-06, DONE, TEST-ONLY, not default)
- Enabled canonical selection for a clearly identified TEST SESSION only (not the default learner path). Activation is per-session via the existing `reasoning_mode` field set to new value `canonical_v2` (added to RP5_MODES so it still routes to the structure engine). No global env flip; `CANONICAL_SELECTION` env still works for corpus runs.
- URL: `<preview>/?preview=writing&canon=1` (Composition entry with canonical selection). Without `&canon=1` the preview stays on the legacy selector (isolation verified: default preview → reasoning_mode consolidated_v2, canonical_active false).
- Files: `server.py` (RP5_MODES +canonical_v2; `PreviewStart.canonical`; `create_preview_session` sets reasoning_mode; NEW read-only `GET /api/sessions/{id}/canonical-trace`); `compass_structure_engine.py` (`select_structure(..., canonical=None)` per-call override; `run()` derives canonical from session reasoning_mode/env; developmental_variation added to per-turn instructional_analysis); `PublicPreview.jsx` (one non-visual query-param passthrough `CANON_TEST` → startPreview `canonical`).
- Diagnostics: `GET /api/sessions/{id}/canonical-trace` returns per-turn selected_structure/status/developmental_variation/developmental_sufficiency/confidence/next_objective + full provisional_judgment + current_state. Conversation export via existing `/sessions/{id}/export?format=json|markdown`.
- Smoke-verified end-to-end on the external URL: weak paragraph → T1 Thesis (Underdeveloped, continue, next=Elaboration) → after integration T2 Elaboration (Minimal, next=Evidence/Example); strong paragraph reaches closure (sufficiency=reached, no invented weakness). Only the 5 primaries selectable; subordinate/legacy objects cannot override. Preview page renders correctly. NO change to dialogue/Teacher-Review/interim-cards/UI visuals/DB schema/audit architecture/OT; legacy retained; `COMPASS_REASONING_MODE=exhaustive` rollback intact.

## Episode 1 calibration — Instructional Continuity Principle (2026-06, DONE, continuity scope only)
- FINDING: canonical selector re-diagnosed from scratch each turn (no memory of prior constraint) → fragmented trajectory (jumped to downstream weaknesses before the current structure was sufficient).
- FIX (scoped to CONTINUATION turns only): `run()` now passes the prior turn's principal constraint (`prior_target` + `prior_variation`) into `select_structure`/`_select_structure_canonical`. When a prior constraint exists, a prompt-level CONTINUITY block instructs: keep regulating the same structure until it reaches developmental sufficiency (can SUPPORT its dependents) OR a genuine higher-leverage shift occurs; never abandon an unresolved diagnosis just because a dependent downstream weakness appeared; acknowledge progress made before naming remaining work; and avoid the opposite error (do not strand the learner on an already-sufficient structure). FIRST TURNS get an EMPTY block → prompt byte-identical to the validated P0-b baseline (zero first-turn footprint). Continuity guidance was deliberately kept OUT of the always-on system message (an early attempt to put it there regressed single-turn selection).
- Deterministic continuity label from the developmental chain (Opening<Thesis<Elaboration<Evidence/Example<Conclusion): first_turn / held_same_structure / advanced_after_sufficiency (moved forward) / reprioritized_higher_leverage (recursion upstream). Surfaced per-turn in `/canonical-trace` (+ prior_constraint, prior_constraint_reached_sufficiency, progress_since_last_turn inside provisional_judgment).
- VALIDATION (canonical_v2 preview, multi-turn): weak paragraph → T1 Thesis → T2 Thesis HELD (vague thesis not yet sufficient) → T3 ADVANCED to Elaboration (advanced_after_sufficiency, thesis now integrated) → T4 Elaboration HELD. Coherent, no oscillation. Student-facing held turns explicitly acknowledge progress ("You've moved from … — that shift is real progress" / "Your revision has moved meaningfully"). Selector-level check confirms advance path fires when a thesis+elaboration is present (Thesis→Elaboration, advanced_after_sufficiency).
- SCOPE/SAFETY: canonical knowledge, dialogue philosophy, and instructional objects unchanged; no new objects. Legacy learner path unaffected (continuity code runs only in the canonical branch). Flag OFF by default. C-SELECT single-turn corpus unchanged by continuity (first-turn prompt restored to baseline); residual REVIEW cases (CS04/05/10 canonically-valid Elaboration/Thesis; CS08/CS13 borderline) are Haiku variance in the same band as the P0-b baseline, NOT a continuity regression.
- RESIDUAL (P1, not touched here per directive): the model reads the canonical Thesis developmental-sufficiency bar strictly, so a thesis edited in isolation can be held longer than ideal; it advances as soon as any elaboration appears. Belongs to sufficiency-threshold calibration (Phase P1). CS13 strong-paragraph closure is variance-sensitive (sometimes Elaboration, sometimes closure).

## Episode 1 — Sufficiency Calibration (P1) — DONE (selector operational interpretation only)
- REFRAMED the selector's OPERATIVE sufficiency test (in _CANON_SEL_SYS, applies to first-turn + continuation) from "sufficient = fully developed" to GENERATIVE/DEVELOPMENTAL sufficiency: a structure is sufficient when it has enough internal organization to support PRODUCTIVE WORK ON ITS IMMEDIATE DEPENDENTS. For each candidate: "can this structure, as it currently stands, now generate meaningful work on its dependents?" YES -> advance (do not polish); NO -> remain. Added the concrete thesis->elaboration / elaboration->evidence / ->conclusion / ->closure ladder + "if the writing already contains dependent work, the upstream was sufficient — move to the frontier." Schema now asks developmental_sufficiency (reached only under generative sufficiency) + dependent_work_possible (yes/no), surfaced in /canonical-trace. NO change to canonical knowledge or dialogue philosophy.
- VALIDATION — selector probes (fixed prior): A vague-thesis HOLD Thesis ✓; B clear-thesis-alone ADVANCE to Elaboration (no polishing) ✓; C minimal-elaboration HOLD ✓; F strong-paragraph CLOSURE (reached) ✓. Residual conservatism: D (developed elaboration, no evidence) and E (evidence already present) still hold/return to Elaboration rather than advancing to Evidence/Conclusion — Elaboration read strictly; borderline/variance.
- VALIDATION — multi-turn Episodes 1-3 (canonical_v2 preview): EP1 weak->Thesis->Thesis(held, vague)->ADVANCE Elaboration(integrated)->Elaboration(held): progressive ✓. EP2 adequate thesis -> Thesis -> ADVANCE Elaboration after one turn (no endless polishing) ✓. EP3 strong paragraph -> straight to Conclusion (final structure, not stranded upstream; closes next turn) ✓. Trajectory feels like progressive construction, not serial correction.
- ROBUSTNESS FIX (found during validation): the longer canonical schema occasionally produced malformed JSON that crashed selection. Hardened `_extract_json` (trailing-comma repair) and wrapped `_select_structure_canonical` with a strict-JSON retry + conservative fallback (hold prior diagnosis / default Thesis, low confidence, `parse_fallback` flagged in trace) so a canonical learner turn never crashes.
- SCOPE/SAFETY: only the selector's operational interpretation changed; legacy learner path untouched; flag OFF by default. Single-turn C-SELECT remains in the P0-b noise band (CS04/05/10 canonically-valid Elaboration/Thesis; CS03/CS08 borderline; Haiku variance).
- RESIDUAL (future micro-calibration): Elaboration is read strictly, so Elaboration->Evidence/Conclusion advancement can lag by a turn on borderline snapshots (converges in real multi-turn use); strong paragraphs teach Conclusion once before declaring closure.

## Episode 2 — Thesis over-holding fix (2026-06, DONE, selector operational interpretation only)
- ROOT CAUSE: canonical selector, given prior=Thesis and a generatively-sufficient thesis, over-held Thesis (reproduced: focus→learning draft held 3/5; full school-purpose chain held 3/3) — it treated "could be deeper" as insufficiency, applied a moving criterion (asked for "what's fundamentally at stake" after the integrated message was supplied), and re-asked the same Thesis question. The earlier P1 generative principle was too generic to override Thesis-specific strictness; multi-turn runs only advanced by landing the ~40% variance.
- FIX (in `_CANON_SEL_SYS` + `_select_structure_canonical`, canonical path only): (1) explicit THESIS generative-sufficiency clause — a thesis is sufficient once it has a recognizable main point + an INTEGRATED relation + enough organization to begin elaboration; do NOT require philosophical depth / ultimate significance / another "why does that matter" / deeper abstraction / ideal thesis; a cause→consequence→purpose chain is integrated; "could be deeper" is never grounds to hold; treating a stated consequence as "a condition not a meaning" after the learner linked it to learning/school purpose is a MISREAD. (2) ANTI-REPETITION / NO-MOVING-CRITERION rule using the prior draft (now passed as `prior_student_text`): if the learner constructed the requested relation and it can organize elaboration, ADVANCE; do not re-ask in new words or raise the bar; hold only on a NEW concrete structural failure. `run()` captures `prior_student_text` from `revision_history`; threaded through `select_structure`/`_select_structure_canonical`.
- VALIDATION: selector probes went from 2/5 & 0/3 advancing → 8/8 advancing to Elaboration (focus→learning + full chain). No over-correction: vague thesis still HOLDS 3/3; first-turn weak/topic drafts still pick Thesis; first-turn integrated thesis gets one establishing Thesis turn then advances on continuation. End-to-end Episode 2 (canonical_v2): T1 Thesis (establish) → T2 ADVANCED to Elaboration (advanced_after_sufficiency) with a response that names the achievement, explains it now functions as a thesis, and moves to Elaboration → T3 Elaboration held with progress acknowledgment. No "why does focus matter"/deeper-meaning/repetition/guessing.
- THINKING-STAGE BOUNDARY: reviewed `NOTICING_SYSTEM_MESSAGE`; already compliant (explicitly no correction, no naming deficiencies, provisional, identifies emerging central idea + structural relationship only). No change made.
- PRESERVED: continuation "you've moved…" progress language; canonical Thesis-as-integrated-message; instructional continuity; generative sufficiency. No change to other canonical objects, Teacher Review, UI, DB, audit, OT, or the legacy path. Flag OFF by default.
- REMAINING BOUNDARY CASES: (a) first-turn integrated thesis gets one establishing Thesis turn before advancing (by design, not stuck); (b) Elaboration→Evidence/Conclusion can still lag a turn on borderline snapshots (from P1, unchanged); (c) the vague↔integrated boundary is model-judged and can vary on borderline theses, mitigated by the anti-repetition rule.


## Episode 3 — Thesis Recognition + Achievement Anchoring (2026-06, DONE, canonical path only; flag OFF by default)
- SCOPE: only Episode 3 Thesis Recognition + Achievement Anchoring. No change to other instructional objects, Teacher Review, OT, UI, DB, audit, or the legacy learner path. Canonical selection stays behind `reasoning_mode == "canonical_v2"` (`?preview=writing&canon=1`); NO default flip, NO legacy pruning.
- PART 1 — THESIS RECOGNITION (`_CANON_SEL_SYS` in compass_structure_engine.py): (a) three-concept distinction added — ASSIGNMENT TOPIC (what the assignment asks about) vs SUBJECT MATTER (the event/text/experience chosen) vs THESIS (the single integrated understanding drawn from it); do NOT classify a genuine thesis as "merely the topic" because a more profound formulation is imaginable. (b) CRITICAL ANTI-OVER-HOLD RULE — forbids holding Thesis on the grounds that it "lacks internal differentiation/organization to guide elaboration", "the relationship must be differentiated BEFORE elaboration", "advancing would risk unsupported/abstract assertion", or "the connection is stated but not yet explained": in each the missing material is EXACTLY what Elaboration produces, so its absence is the reason to ADVANCE, not hold. A bare one-sentence integrated thesis with no body yet is the normal starting condition for Elaboration. (c) `selected`-field semantics clarified: it names the structure THIS TURN TEACHES NEXT (the frontier), not the structure just judged sufficient; a rationale that says "advance to Elaboration" while selected='Thesis' is a prohibited self-contradiction.
- PART 2 — ACHIEVEMENT ANCHORING (dialogue, `_DLG_SYS` + `generate_dialogue`): FIRST-TURN three-beat acknowledgment (state completed achievement affirmatively → explain its intellectual value → show it provides a foundation for the next structure), banning weak/incompleteness phrasing ("you have some ideas", "beginning to", "on the right track", "good start but"). CONTINUATION turn rewritten to a LIGHTER 6-function shape (re-anchor → what CHANGED since prior draft → NAME the developmental OPERATION performed, not the content added → what is now SUFFICIENT → HOLDING vs ADVANCING + why → one next invitation), using developmental language ("not yet", "still developing", "the next step is"), never deficit language ("lacks", "fails to"). `generate_dialogue` now receives `prior_student_text` (threaded from `run()` → call site) so continuation turns can compare current vs previous draft.
- VALIDATION (transient `/tmp/test_ep3.py`, canonical selector + continuation dialogue, no DB writes): Cases A–F, 3 consecutive full-suite runs 7/7 each. A narrative-with-lesson → thesis recognized, advances to Elaboration; B narrative-only-events → holds Thesis (scaffold discovery, "Topic substituted for thesis"); C simple valid thesis → RECOGNIZED as "Integrated thesis capable of supporting elaboration" (no longer misclassified as topic — the actual Case-C fix); D multiple disconnected lessons → holds Thesis ("Diffuse thesis, no integrated message"); E continuation acknowledgment → names the developmental operation ("you've performed an act of integration…"), states advancing, zero banned generic phrases; F Episode 2 cell-phone integrated thesis → advances (Episode 2 fix intact, no regression).
- KNOWN RESIDUAL: on truly bare single-sentence theses (C1 "patience makes anything easier", C3 "being on time shows respect") the advance-vs-hold decision still wobbles run-to-run (Haiku variance) — but recognition is stable (status "present", variation "integrated…capable of supporting elaboration"); a status-based deterministic override was rejected because diffuse Case D also returns status "present" (would wrongly advance D). This is the same mild "Generative Sufficiency Boundary Lag" already logged.

## Episode 3 — Final Calibration: positive evaluation + topic/thesis recognition (2026-06, DONE, canonical path only; flag OFF)
- SCOPE: Episode 3 only; no other objects/UI/DB/audit/OT/Teacher-Review/legacy path changes.
- ISSUE 1 — Achievement Anchoring now leads with an EXPLICIT POSITIVE EVALUATION. First-turn acknowledgment (`_DLG_SYS` fn 1 + `generate_dialogue` first-turn `_mode_block`) is now a FOUR-beat pattern: (a) genuine, SPECIFIC positive evaluation FIRST ("You've made a strong start…", "You've identified a clear central experience…", "That's an important shift…") so the learner immediately feels "Compass recognized something valuable I actually accomplished"; (b) name the completed achievement with a definitive verb; (c) explain why it matters; (d) show it provides the foundation for the next step. Generic praise ("good job") and overstatement forbidden; incompleteness/repair phrasing still banned, but an affirmative standalone "you've made a good start" is now explicitly ALLOWED (only the "good start, BUT…" pivot into criticism is forbidden).
- ISSUE 2 — Topic vs Thesis recognition strengthened (`_CANON_SEL_SYS`): added the TOPIC-vs-THESIS DIAGNOSTIC — "my thesis is HOW I was transformed", "…THE CAUSES of WWII", "…WHY uniforms are good" are TOPICS (name the aboutness) not theses; a thesis says something ABOUT the topic. Added a BINDING RECOGNITION RULE: a sentence expressing a single integrated understanding that CAN organize the paragraph — even if simple — SHALL be recognized as a thesis (status not 'missing') and must NOT be downgraded to 'topic'/'gesture toward meaning' merely because it could be more elegant/deep/philosophical.
- VALIDATION (`/tmp/test_ep3_calib.py`): 7/7 — topic-stated-as-thesis G1/G2/G3 (transformed / WWII causes / uniforms) all correctly HELD on Thesis, status 'missing', variation "Topic substituted for thesis"; real simple theses R1/R2 recognized (status 'present', "Integrated thesis capable of supporting elaboration"); first-turn acknowledgment opens with explicit positive evaluation and carries no banned phrases. Regression: Cases A–F re-run twice, 7/7 each. Backend restarted, HTTP 200.


## Episode 3 — Thesis-to-Elaboration Handoff + Explanation Leak Fix (2026-06, DONE, canonical path only; flag OFF)
- SCOPE: canonical composition path (`reasoning_mode == "canonical_v2"`) only; no default flip, no legacy pruning, no Explanation-home decision, no OT/Teacher-Review/UI/DB/audit changes.
- ROOT CAUSE of the "Explanation" appearance: NOT routing. Canonical Elaboration (`elaboration.json`) is ready and `_resolve_teaching_source('Elaboration')` correctly returns display_name "Elaboration" (canonical=True) with no "explanation" text; `_LEGACY_TO_CANONICAL` does NOT map Explanation→Elaboration. The leak was Sonnet dialogue-output VARIANCE — the model occasionally named the operation "Explanation" and framed it as claim→evidence→explanation (its writing-instruction prior).
- FIX (compass_structure_engine.py `generate_dialogue`, canonical dialogue only): added a per-turn `_elab_block` for Elaboration turns that (a) forbids the word "Explanation" as the structure/task name, (b) forbids the claim→evidence→explanation framing, (c) defines canonical Elaboration as developing the SUBSTANCE of the thesis (evidence subordinate/optional), and (d) on first turn injects the THESIS-TO-ELABORATION HANDOFF (positive eval → state thesis in learner's words → identify as their thesis → why it's a thesis → sufficient to organize → advance to elaboration OF that thesis → one unpack invitation). Also added the immediate positive-evaluation lead to the first Thinking response (`NOTICING_SYSTEM_MESSAGE`).
- VALIDATION: first-turn Elaboration repro ×5 — 0 "Explanation", thesis explicitly named + handoff performed every run; noticing leads with positive eval; A–F regression 7/7 (twice).

## Post-Episode 3 — Thesis Orientation + Relational Operations (2026-06, DONE, canonical path only; flag OFF)
- Generalized the Episode-3 handoff into THREE governing principles across ALL FIVE canonical primaries (compass_structure_engine.py `generate_dialogue`, `_elab_block` now built for any `src['canonical']` structure; server.py `NOTICING_SYSTEM_MESSAGE`).
- (1) THESIS ORIENTATION: every canonical dialogue turn first locates the learner's THESIS STATE (A topic-only / B emerging / C established), states/paraphrases the thesis in the learner's own words, identifies it explicitly as their thesis, says what it says ABOUT the topic, THEN introduces the operation — so the learner always knows "what idea am I developing?" before "what operation?". Thesis stays the visible organizing center even once sufficient (but not held when sufficient).
- (2) RELATIONAL OPERATIONS: each structure is framed relationally via a per-structure map — Elaboration = "elaboration OF the thesis", Evidence/Example = "evidence FOR / example OF a specific point derived from the thesis (never floats free)", Conclusion = "completion/integration OF the line of reasoning from the thesis", Opening = "orientation toward the thesis". Never taught as isolated nouns.
- (3) IMMEDIATE ACHIEVEMENT RECOGNITION: first Thinking response must begin with a specific EVALUATIVE WORD (good/strong/clear/thoughtful/meaningful/promising/insightful/effective/solid), accurate/proportionate/grounded; generic exclamations banned ("Great job!", "This is amazing!", "You have some ideas", "I can see what you are trying to do"). Thinking-stage acknowledgment does NOT replace full Achievement Anchoring in the formal response — both occur.
- Explanation prohibition preserved; legacy Explanation still NOT mapped to Elaboration; canonical Elaboration remains sole teaching source for Elaboration turns.
- VALIDATION (Section-8 scenario + A–E turn tests): selector/noticing 6/6 (MAIN advances Thesis→Elaboration; noticing leads "You have a strong insight…"; topic-only "how I was changed by failing" → Thesis/missing/"Topic substituted for thesis"; thesis present → recognized). Dialogue 11/11 — Elaboration/Evidence/Conclusion each open with positive eval, name the thesis, frame the operation relationally, 0 "Explanation", 0 claim-evidence framing. A–F regression 7/7. Backend restarted, HTTP 200.
- REMAINING BOUNDARY CASES: orientation adds length to every canonical turn (still within 2–6 sentence spirit but denser); on truly bare single-sentence theses the advance/hold call still wobbles (pre-existing Haiku variance); Explanation's canonical home remains intentionally unresolved.


## Episode 3 — Thesis Presence vs Thesis–Assignment Fit (2026-06, DONE, canonical dialogue only; flag OFF)
- PROBLEM: on a thesis that is PRESENT but does not yet fully satisfy the assignment's specific demand, the canonical Thesis-turn dialogue still said "Your next task is to develop a thesis" (implying absence) — conflating Thesis Presence with Thesis–Assignment Fit.
- FIX (compass_structure_engine.py `generate_dialogue`, `_elab_block`, dialogue-only): new branch `elif _dispn == "Thesis" and status != "missing"` — when a thesis is present, Compass must NEVER say "develop a thesis" / "you need a thesis" / "this points toward a thesis"; it must (1) AFFIRM the thesis is present and name it in the learner's words, then (2) frame the work as REFINING / SHARPENING it to more fully answer the assignment's specific question (e.g. what THIS experience taught about oneself). Explicitly distinguishes THESIS PRESENCE (has one integrated understanding → has a thesis) from THESIS–ASSIGNMENT FIT (does it fully satisfy this assignment). Overrides the earlier "develop a [structure]" phrasing for this turn. Never erase a genuine achievement to name remaining work. Topic-only (status "missing") is unaffected — discovery framing preserved. Selector NOT changed (holding Thesis for a real fit gap remains legitimate).
- VALIDATION (`/tmp/val_fit.py`): Thesis turn on a general-lesson thesis under a self-understanding assignment, status partial ×2 + present ×2 = 4/4 — 0 absent-thesis language, affirms thesis present ("you have stated a genuine thesis"), uses refinement framing. A–F regression 7/7 (Case E = Thesis continuation dialogue intact). Backend restarted, HTTP 200.


## Episode 3 — Explanation is not an Instructional Object/Stage (2026-06, DONE, canonical path only; flag OFF)
- ARCHITECTURAL CLARIFICATION: the canonical curriculum rejects the legacy Thesis → Evidence → Explanation sequence (which treats a stated thesis as already complete/transparent and reduces the remaining work to proving it and explaining how evidence supports the claim). A thesis is a COMPRESSED integrated understanding; the essay UNFOLDS it. The recursive instructional question is always "what does the naive reader need to understand NEXT to understand this thesis?" — never "how does this evidence support the claim?". Explanation is only a subordinate reasoning operation, never an instructional object, target, or next stage.
- ROOT CAUSE of residual "Explanation" references: `next_objective` in the canonical path could still surface legacy objects — (1) `resolve_structure()` (legacy alias map) resolved a model-emitted "Explanation"→"Explanation"; (2) the deterministic `next_objective` fallback iterated `PRIORITY_ORDER` (which contains Explanation, Transition, Definition, Sentence Construction).
- FIX (compass_structure_engine.py, canonical path only):
  - `run()` next_objective: when `_canonical`, normalize via `_canonical_or_none()` (legacy objects → None) instead of `resolve_structure()`; and the deterministic fallback now iterates `CC.PRIMARY_STRUCTURES` (canonical-only order) instead of `PRIORITY_ORDER`. next_objective is therefore always one of the five primaries or empty — never Explanation/Transition/etc.
  - `_CANON_SEL_SYS`: added the "EXPLANATION IS NOT AN INSTRUCTIONAL OBJECT OR A STAGE" principle (reject the legacy sequence; thesis is compressed; recursive naive-reader question; next_objective must be a canonical primary or null).
  - `generate_dialogue` orientation block (all canonical structures): added the RECURSIVE PROCEDURE ("what does the naive reader need to understand next about the thesis?") and an explicit ban on naming "Explanation" as a structure/task/next step on ANY canonical turn.
- VALIDATION: `_canonical_or_none` returns None for Explanation/Transition/Definition/Sentence Construction/Reasoning/Warrant and passes the 5 primaries; canonical fallback order = [Opening, Thesis, Elaboration, Evidence / Example, Conclusion]. Dialogue suite 11/11 (Elaboration/Evidence/Conclusion, 0 "Explanation", thesis-centered), A–F 7/7. Backend restarted, HTTP 200.


## Episode 3 — Thesis-as-Compressed-Understanding + legacy Explanation model removed (2026-06, DONE, canonical path only; flag OFF)
- CONFIRMED root cause: the residual "explanation … how and why your evidence leads to your claim" text was DIALOGUE-GENERATION variance (Sonnet's conventional Thesis→Evidence→Explanation prior), NOT selection/routing/source-resolution. In canonical mode the selector cannot pick Explanation (`_canonical_or_none('Explanation')=None`), the target is never remapped to a legacy object, and `_resolve_teaching_source('Elaboration')` returns the canonical model (ready) — the legacy Explanation CIO is unreachable as a teaching source. next_objective leakage was already closed in the prior pass (canonical-only normalization + `CC.PRIMARY_STRUCTURES` fallback).
- CHANGES (compass_structure_engine.py `generate_dialogue`, canonical Elaboration branch): the thesis is now explicitly taught as a COMPRESSED integrated understanding whose meaning the paragraph must UNFOLD (not transparent once stated); Elaboration listed as the progressive development of UNDERSTANDING (define term / distinguish ideas / differentiate parts / trace relation / unpack implication / examine tension / qualify / compare / show how understanding changed) — not addition of information; explicit "do NOT shift to evidence yet". Handoff rewritten to: celebrate → state thesis (learner's words) → identify as thesis AND compressed understanding → SELECT ONE part still compressed for the NAÏVE READER → introduce Elaboration OF that part → one unfold invitation, no answer supplied, no evidence shift. Thesis-Presence branch nudged to prefer "refine/sharpen it" over bare "develop your thesis".
- VALIDATION (`/tmp/val_uof.py`, exact "user of failure" revision, ×2): 9/9 each — selector advances Thesis→Elaboration (established Thesis; next_objective=Conclusion, canonical); noticing leads with eval word; dialogue names the thesis, frames it as compressed ("seed/packed/compressed form"), selects one part for the naïve reader, elaboration OF the thesis, 0 "Explanation", 0 claim-evidence framing, ownership preserved. Regression: A–F 7/7; Thesis-Presence-vs-Fit no-absent-language 4/4 + refine 4/4. Backend restarted, HTTP 200.
- Explanation remains a subordinate reasoning operation with no canonical home decided (per scope); Evidence/Example remains a later subordinate support; no legacy Explanation→Elaboration mapping introduced.


## Episode 3 — Developmental Sufficiency vs Rhetorical Optimization (2026-06, DONE, canonical path only; flag OFF)
- PRINCIPLE: instructional-object COMPLETION is determined by DEVELOPMENTAL SUFFICIENCY (the learner successfully constructed the object — an integrated understanding, responsive to the assignment, capable of organizing the writing), NOT by maximal RHETORICAL QUALITY (could be more elegant/sharper/sophisticated). Once a thesis is developmentally sufficient the Thesis object is COMPLETE → celebrate + advance to Elaboration; never keep assigning Thesis work merely because it could be improved.
- OBSERVED ERROR (3rd recurrence of the same pattern): the selector marked integrated theses `status=present` yet still `selected=Thesis`, with rationales like "provides sufficient conceptual organization to support elaboration. However… holding Thesis briefly here will sharpen the organizing principle" — textbook sufficiency/optimization conflation. Prompt reinforcement had failed on this exact contradiction three times.
- FIX (compass_structure_engine.py):
  - `_CANON_SEL_SYS`: added the explicit DEVELOPMENTAL SUFFICIENCY vs RHETORICAL OPTIMIZATION distinction + completion criterion (advance once integrated+responsive+organizing; assignment-fit hold only for genuine non-responsiveness, never for imaginable better wording).
  - `_select_structure_canonical`: NEW deterministic DEVELOPMENTAL-SUFFICIENCY GUARD — if `sel=="Thesis"` AND `status=="present"` AND variation contains "integrated" AND NOT (diffuse / topic substituted / competing / contradict / fragment / absent), the Thesis object is complete → advance to the next unestablished canonical primary (Elaboration→Evidence/Example→Conclusion), mark Thesis established, rewrite rationale, and set `_provisional.sufficiency_guard_advanced=True`. Keyed on "integrated" so topic-only (status missing) and diffuse (Case D) still correctly hold.
- VALIDATION (`/tmp/val_suff.py`): T1 simple-responsive, T2 improvable, T3 identity — all now ADVANCE to Elaboration (were held before). Regression: A–F 7/7 (Case B topic-only holds; Case D diffuse holds), topic/thesis calibration 7/7 (G1/G2/G3 topic-stated-as-thesis hold; R recognized). Backend restarted, HTTP 200.
- NOTE: a non-responsive general lesson under a specific assignment now also advances (guard fires on integrated+present); per this instruction's priority (sufficiency over optimization) assignment-fit is handled recursively during Elaboration (Thesis↔Elaboration recursion), and the dialogue's Thesis-Presence-vs-Fit framing still applies whenever a Thesis turn does occur.


## Episode 3 — Completion, Cumulative Thinking, Focus of Work + Content Neutrality (2026-06, DONE, canonical path only; flag OFF)
- SCOPE: canonical composition path only; no default flip, no legacy pruning, no OT/Teacher-Review/DB-schema/audit changes; no dashboard/stepper/gamification.
- (A) DEVELOPMENTAL COMPLETION > OPTIMIZATION: reinforced by the sufficiency guard (prior pass) + selector rules; the "persevere/quits" complete thesis now advances (Focus→Elaboration) instead of being told it's "close".
- (B) CONTENT NEUTRALITY & STRUCTURAL ADEQUACY (`_CANON_SEL_SYS`): added STRUCTURAL JUDGMENT (the only authoritative test — one integrated understanding, says something about the topic, assignment-responsive, organizes the material, generates Elaboration) vs CONTENT PREFERENCE (deeper/more personal/identity/surprising — never authoritative); learner holds meaning authority; MULTIPLE valid theses for one event; completion-language ban ("close", "what did this reveal about who you really are", etc.). Dialogue orientation block: ANTI-LEADING rule + prefer structurally-open questions; never steer toward identity transformation unless learner-introduced or assignment-required.
- (C) CUMULATIVE THINKING (`NOTICING_SYSTEM_MESSAGE` + `_pedagogical_noticing` + `/noticing` endpoint): the noticing call now receives the PRIOR student version + PRIOR observations (persisted per session as `last_noticing_observations`) and names only what is genuinely NEW/changed each turn; deterministic NO-CHANGE short-circuit (no LLM) when the submission is unchanged. First observation still leads with an evaluative word — but on the NEW achievement.
- (D) FOCUS OF WORK DISPLAY: `Turn` model gains `focus_of_work` + `focus_description`; `_finalize_structure_v5` sets them from the canonical target via `_FOCUS_DESCRIPTIONS` (Opening/Thesis/Elaboration/Evidence-Example/Conclusion one-liners); frontend `PublicPreview.jsx` renders a small non-editable "FOCUS OF WORK" panel atop the coaching card (`data-testid=preview-focus-of-work`). Semantics: shows the CURRENT student-facing operation (Elaboration once Thesis complete), never a legacy/internal label.
- VALIDATION: `/tmp/val_cum.py` — 3-revision cumulative Thinking names new each turn (string similarity ~0.10, no repetition), no-change short-circuits (PASS), "persevere" thesis advances to Elaboration. End-to-end API (localhost): canonical session → coaching turn carries `focus_of_work="Elaboration"` + description. Live UI screenshot: FOCUS OF WORK → Elaboration renders; coaching marks thesis complete, no identity-leading question. Regression: A–F 7/7; topic/thesis calibration 7/7 (G topic-as-thesis holds even when model labels status=present, because the guard excludes "topic substituted" variations); multiple-thesis advance confirmed. Backend restarted, HTTP 200; frontend compiled.


## Episode 3 — Live-path mismatch diagnosed + fixed (2026-06, canonical path only; flag OFF)
- SYMPTOM: live preview showed "develop an Explanation" + legacy claim→evidence→explanation + NO Focus of Work panel, contradicting local validation.
- DIAGNOSIS (DB inspection of recent preview sessions): the user's live session `fc063057` (updated 18:27, after the restart) was `reasoning_mode=consolidated_v2` — the DEFAULT/LEGACY path — with explanation=True, focus_set=False. My two post-fix canonical sessions (`f5fc9240`, `3b7b09cc`) were `canonical_v2` with explanation=False, focus_set=True. So the live path simply was NOT canonical: the legacy consolidated_v2 selector can pick "Explanation", and `_finalize_structure_v5` only sets `focus_of_work` for `canonical_v2` → no panel.
- ROOT CAUSE: `PublicPreview.jsx` resume effect restored the `localStorage` session matched only by `mode`, IGNORING the `?canon=1` flag and the session's `reasoning_mode`. A returning learner with a prior consolidated_v2 session had it silently resumed under `?canon=1`, bypassing the canonical path. (Backend/prompt were correct; this was a client-side activation bug.)
- FIX: resume effect now compares `CANON_TEST` against the fetched session's `reasoning_mode === "canonical_v2"`; on mismatch it drops the stale session and starts fresh with the correct mode. `reasoning_mode` is present in the GET /sessions/{id} response.
- VERIFIED LIVE (fresh `?canon=1` session, post-fix screenshot): FOCUS OF WORK → Elaboration renders; coaching explicitly names the learner's thesis and marks it complete; "develop an elaboration of that thesis" (no "Explanation"); COACHING_HAS_EXPLANATION=False. Frontend recompiled; backend HTTP 200.


## Canonical v2 PROMOTED TO DEFAULT for all preview routes (2026-06, DONE; legacy retained as rollback)
- DECISION (user, option a): canonical_v2 is now the authoritative instructional model; the legacy Claim→Evidence→Explanation (consolidated_v2) model is no longer an active design alternative and must not appear in ordinary testing. Legacy code retained behind a rollback flag.
- BACKEND (server.py): `PreviewStart.canonical` default None; `create_preview_session` now computes `_want_canonical = True if payload.canonical is None else bool(payload.canonical)` → `reasoning_mode = canonical_v2` by default, `consolidated_v2` only when `canonical=false` explicitly. Verified: default→canonical_v2, canonical:true→canonical_v2, canonical:false→consolidated_v2.
- FRONTEND (PublicPreview.jsx): replaced `CANON_TEST = has("canon")` with `WANT_CANONICAL = !has("legacy")` — canonical is default for every preview route (writing, teacher, ot); append `?legacy=1` to roll back to legacy. Used in both `startPreview` calls and the resume guard. Added an unobtrusive DEV BADGE next to the Compass logo showing the active reasoning mode ("Canonical v2" / "Legacy v2", `data-testid=preview-reasoning-mode-badge`).
- VERIFIED LIVE: `?preview=teacher` (previously legacy) now runs canonical — badge "CANONICAL V2", FOCUS OF WORK: Elaboration, coaching names the thesis explicitly ("That's your thesis…"), 0 Explanation, cumulative Thinking present. Backend HTTP 200; frontend compiled.
- ROLLBACK: `?legacy=1` (or API `canonical:false`) → consolidated_v2 legacy path, untouched.


## Canonical UI — Current Instructional Focus + Vertical Writing Structure (2026-06, DONE, canonical preview only)
- COMPONENT (`PublicPreview.jsx` `CanonicalOrientation`, rendered immediately above the coach response on the active card): (1) CURRENT INSTRUCTIONAL FOCUS panel = structure name (uppercase, accent) + one-line description; (2) VERTICAL Writing Structure listing the 5 canonical primaries with Thesis ⇅ Elaboration reciprocal marker, Evidence / Example indented (└──) and subordinate BENEATH Elaboration, Conclusion at the base; legend ✓ Established / ● Current / ○ Not in focus. No Step X, no percentages, no bars, no gamification.
- SOURCE OF TRUTH: focus = AI turn `focus_of_work` (from `result.decision.selected_instructional_object`); description = `focus_description` (`_FOCUS_DESCRIPTIONS`); established = AI turn `established_structures`. Backend authoritative; frontend derives nothing independently.
- BACKEND: `run()` decision now returns `established_structures`; `_finalize_structure_v5` sets `t.established_structures = _canonical_established(...)` which normalizes the selector's (possibly descriptive) established entries to canonical primary names AND includes every primary strictly before the current focus (guarantees Thesis shows Established once focus is Elaboration). Only set for `canonical_v2`.
- DYNAMIC: Thesis sufficient → focus flips to Elaboration + Thesis marked Established (validated). Focus can return to Thesis if the thesis is later weakened (coach explains). While coach is thinking, the last authoritative focus stays displayed (it's on the persisted AI turn).
- ACCESSIBILITY: two indicators minimum (symbol ✓/●/○ + font weight + border enclosure on focus panel), `aria-current="step"` on the focus row, `aria-label` on each marker, legend text; not color-alone.
- VALIDATION: 4-turn canonical API demo — T1 focus=Thesis; T2 focus=Elaboration, established=[Opening,Thesis]; T3 focus=Elaboration; T4 focus=Elaboration (selector judged elaboration not yet sufficient to advance — authoritative, UI mirrors it); 0 Explanation all turns. Desktop (1920) + narrow (390) screenshots confirm correct vertical render, Thesis⇅Elaboration, Evidence subordinate, focus/map agree with coaching. Backend HTTP 200; frontend compiled.
- BOUNDARY: Opening is marked Established from turn 1 (before-focus rule) though single paragraphs rarely have a separate opening — cosmetically harmless, kept to guarantee the Thesis-established requirement without a frontend theory.


## Episode 3 Polish — Thesis Highlight + Coaching Prose Refinement (2026-06, DONE)
- User polishing Episode 3 (Thesis/Elaboration) before advancing. Five requests implemented:
- (1) THESIS HIGHLIGHT (frontend): the sentence(s) Compass currently recognizes as the thesis are now tinted IN PLACE inside the learner's editable draft, always corresponding to the YOUR THESIS panel. New `/app/frontend/src/lib/thesisMatch.js` `findThesisRanges(draft, thesis)` (direct case-insensitive substring, else sentence-level token-overlap ≥50%, guard: overlap fallback needs ≥2 content tokens to avoid over-matching short theses). `PublicPreview.jsx`: module fn `renderThesisSegments` wraps ranges in `<mark data-testid=thesis-highlight bg-[#f3e2c0] text-transparent>`; rendered in a transparent-text overlay div (`data-testid=preview-document-highlight`, `absolute inset-0`, identical font/padding/whitespace-pre-wrap, scrollTop-synced) BEHIND the editable textarea (`preview-document`, bg-transparent) so the tint sits under the learner's own words. Highlight appears only once `activeCoaching.current_thesis` is present (matches the panel).
- (2-5) COACHING PROSE (backend `_DLG_SYS` + Elaboration block in `compass_structure_engine.py`): shortened GLOBAL STYLE target 80-140/max~160 → 50-100/max~120 words + "2 to 6" → "2 to 5" sentences, strengthened "never re-teach; feel like progressing not rereading a lesson"; jargon ban expanded to include "instructional structure"; added ELABORATION DISCOVERY STANCE ("you do NOT possess a predetermined elaboration… governing question is 'what does the naive reader still not understand about this thesis?' NEVER 'what elaboration do I want the learner to produce?'; ask genuinely OPEN questions"); reinforced CONTENT NEUTRALITY ("assess only STRUCTURAL adequacy… never whether you'd prefer a different conception of identity, growth, resilience, or meaning").
- Verified: direct engine calls — Elaboration continuation 96 words / Thesis-present continuation 90 words, 0 jargon hits, discovery-framed open questions, content-neutral. Testing agent iteration_45 — frontend 100%: thesis-highlight renders, pixel-perfect overlay alignment (bbox == textarea), matches YOUR THESIS panel, survives editing, no doubled/offset text. Engine selector/decision logic FROZEN (no changes). ~90s/turn reasoning unchanged.


## Canonical V2 — Developmental Operations Update (2026-06, DONE)
- Shift from wording polish to TEACHING the intellectual OPERATIONS of revision. No UI change; selector/decision logic FROZEN; all changes in the dialogue layer + a new internal library + philosophy docs.
- (1) TEACH THE OPERATION, NOT THE PROBLEM — new highest-priority "DEVELOPMENTAL OPERATION SHAPE" in `_DLG_SYS` governs every turn: A. name what the learner accomplished; B. why the current organization is incomplete IN TERMS OF READER UNDERSTANDING + structure (never "needs more elaboration"); C. teach the THINKING OPERATION that carries this draft to the next (not "explain more").
- (2) DEVELOPMENTAL OPERATIONS LIBRARY — new `/app/backend/developmental_operations.py` (11 operations: Story→Meaning, Observation→Thesis, Example→Principle, Illustration→Elaboration, Description→Interpretation, Claim→Justification, Evidence→Explanation, Experience→Generalization, Generalization→Personal Meaning, Abstract→Concrete, Parallel ideas→Integrated explanation). Each has from/to form, reader-understanding gap, and a coaching strategy. `render_operations_library()` + `operation_hints(focus)` injected into the dialogue prompt (`_ops_block`); the model picks the ONE operation the turn requires and teaches it (never names the label to the learner).
- (3) INTEGRATION is itself an operation — when a new idea runs beside the thesis, teach "how does this grow FROM my thesis?" not accumulation.
- (4) READER UNDERSTANDING tied to STRUCTURE — "what would a reader still not understand, and which idea must be developed?" not "explain more".
- (5) PROGRESS VISIBLE — name the new intellectual achievement ("you have introduced a conceptual distinction").
- (6) CONTINUITY — continuation turns mark Last→This→Next trajectory when a prior draft exists.
- (7) EVALUATION vs INSTRUCTION — banned bare quality verdicts ("Good.", "Excellent.", "Great job"); prefer "your next intellectual task is…".
- (8) CONTENT NEUTRALITY preserved (teaches how ideas are organized, never what to think).
- (11) STORY vs ELABORATION distinction + prompt-compliance rescue added to the Elaboration block: recognize when a narrative functions as ILLUSTRATION rather than elaboration; teach the different intellectual FUNCTIONS (Story="what happened?"; Elaboration="what does it mean?"; + Evidence/Explanation/Interpretation/Conclusion function menu); reinterpret (never discard) the narrative — its role becomes lived experience FROM WHICH understanding is developed.
- (9,10) Philosophy/teacher documentation: new `/app/memory/CANONICAL_MODE_PHILOSOPHY.md` — Canonical Mode teaches culturally established forms as intellectual tools; prompt-compliance ≠ writing quality (prompt supplies content, canonical structures organize it); long-term direction = a tutor in developmental intellectual operations ("what transformation must occur for this learner to reach the next organization of thought?").
- Word budget relaxed 50-100/max120 → 70-130/max150 to fit A/B/C teaching; "3 to 6 sentences".
- Verified: direct engine calls — story-as-illustration turn (140w) and parallel-ideas/integration turn (119w) both produce the A/B/C shape, reader-understanding gap ("they can hold both ideas but don't yet understand how one becomes the other"), teach-the-transformation questions, Story/Elaboration distinction, content-neutral; 0 jargon, 0 bare quality verdicts. Backend healthy (/api/tests/cases 200). Selector FROZEN, no API/UI/data-model change.


## Canonical V2 — Post-Validation Refinement (2026-06, DONE)
- Narrow pass after the live multi-turn test. Selector architecture, UI, structure map, operations library, and progression logic preserved; only calibrations added.
- (1) DENSITY: GLOBAL STYLE hard cap 90-140 / never >~150 words or 3 short paragraphs, even first turn; first-turn COMPRESSION directive added (one acknowledgment / one idea / one gap / one invitation). Live result: first turn 188→169w; continuation turns ~131w.
- (2) ANTI-MENU: forbade offering candidate meanings/causes/mechanisms as a list OR a series of rhetorical questions ("Was it X? A moment where Y? The fact that Z?"); ask ONE structurally open question. Fixed a triple-question menu that appeared in stage C.
- (3) THESIS LABEL: deterministic `_thesis_is_verbatim(thesis, student_text)` in engine (normalized substring, ≥3 words) → `decision.thesis_is_verbatim` → `Turn.thesis_is_verbatim` (server) → frontend panel labels "YOUR THESIS" (exact learner quote, shown in quotation marks) vs "Compass's current reading of your thesis" (paraphrase/synthesis, no quotation marks). Never presents a synthesis as an exact quote.
- (4) FUNCTIONAL ADVANCEMENT (Elaboration→Conclusion): selector prompt now requires (a) ≥1 relation in the thesis unfolded, (b) connected to the story/experience, (c) naive-reader-understandable, (d) integrated (not parallel) BEFORE advancing; must NOT advance on length/abstraction/reflective-sentence count alone.
- (5) FUNCTIONAL REINTERPRETATION named ("Your story now does a new job: it shows the change your thesis is describing").
- (6) CUMULATIVE CONTINUITY: each turn distinguishes what changed / what the learner did / what remains; no repeated praise or thesis summary across turns.
- (7) VALIDATION via real engine pipeline (3-turn session): A story-first (169w) teaches story-vs-elaboration, thesis verbatim=True; B disconnected reflection (131w) names parallel ideas + teaches integration, no menu; C connected reflection (131w) RECOGNIZES integration, marks Last→This→Next, and correctly HOLDS on Elaboration (did not advance to Conclusion — the mechanism is still compressed for a naive reader), no menu. Detector unit-tested (exact→True, paraphrase→False). Backend + frontend compile clean.
- Remaining boundary cases (watch): first-turn length can still run ~160-170w on concept-heavy openings (3 short paragraphs, acceptable); thesis label relies on substring match — a lightly reordered verbatim thesis may read as paraphrase (conservative, acceptable); Conclusion functional-gate not yet exercised on a fully-sufficient Elaboration (advance path unverified live). Evidence/Conclusion calibration still deferred per prior instruction.


## Canonical V2 — Instructional Tone Refinement (2026-06, DONE)
- Tone-only pass on the dialogue prompt (`_DLG_SYS`). No selector/progression/stage-gate/structure-map/operations-library/UI change.
- (1) SUCCESS → NEW OPPORTUNITY (never Success → Deficiency): after a just-completed developmental move, acknowledge and LET IT STAND; forbid the deflating pivot ("but…", "however…", "you haven't yet…", "it still doesn't…", "it is not yet…"); start a NEW affirmative sentence for the next step; describe the next layer as something to ADD, not something missing.
- (2) Transition into the next task affirmatively ("The next step is…", "Now your task is…").
- (3) Reinforced TEACH FUNCTIONS NOT LABELS with the requested examples (thesis = one main idea to hold onto; elaboration = unfolds what the main idea packed inside; evidence = why the idea is believable).
- (4) PLAIN LANGUAGE (Grade-9) substitution list added: "main idea you want your reader to understand" (not "integrated message"); "central idea"/"main idea that holds the paragraph together" (not "organizing principle"); "main understanding"/"central insight" (not "integrated understanding"); banned "compressed"/"organizing center" jargon to the learner.
- (5) Inquiry preserved — end on ONE open question that asks the learner to perform the next operation.
- Verified via real engine calls: first Elaboration turn 149-167w, integration turn 125w; zero academic jargon; strong standalone acknowledgment + affirmative next-task framing + single open question. RESIDUAL: the word "but" still appears ~once per turn as cushioned reader-curiosity framing ("a reader might still wonder"), not deficiency framing — judged acceptable (full removal would make prose stilted); first-turn length can still touch ~167w on concept-heavy openings.


## Compass 3.0 — Sprint 1: Function-Centered Teacher Review (2026-06, DONE — engine isolated behind flag, now LIVE)
- Shifted the internal unit of analysis in the experimental `functional_v3` engine from structural labels to COMMUNICATIVE FUNCTIONS (orient/focus/develop/support/consolidate) + two organizing relations (functional_organization, local_organization). `canonical_v2` (`compass_structure_engine.py`) is byte-frozen; only `functional_v3.py` + a trace addition in `server.py` changed. Rollback = set `COMPASS_ENGINE="canonical_v2"` in `/app/backend/.env` + `sudo supervisorctl restart backend`.
- New selector `_select_functions()` (functional_v3.py ~L1630) with `_FUNCTION_SEL_SYS`: reads the paragraph as functions for a naive reader, emits the FULL internal decision JSON schema the user specified (communicative_task, topic, current_focus, focus_status, focus_reasoning, functions{orient/focus/develop/support/consolidate:{status,evidence}}, functional_organization{status,reader_path,limiting_relation}, naive_reader_need, selected_function, student_facing_term, selected_operation, local_target{quoted_text,purpose,remaining_reader_need}, developmental_sufficiency, progress_since_last_turn, continuity_decision, confidence). Topic-vs-focus recognition + retrospective composition + generative sufficiency (never hold Focus/Thesis for polish); Support subordinate to Develop; RESTRAINT/closure when coherent.
- Mapping `_FUNCTION_TO_TERM` → student-facing terms (Opening/Thesis/Elaboration/Evidence / Example/Conclusion/Organization/Sentence Construction) drives the PROVEN `generate_dialogue`/`generate_closure` (teaches one concept, invites one learner-owned revision, never rewrites). Added an `Organization` teaching object to MINIMAL_OBJECTS for `functional_organization`.
- Turn 2+ REVISION COMPARISON: `run()` now treats any follow-up turn with a prior draft as a continuation, so the dialogue names the operation the learner performed and states hold/advance/recurse/complete; the selector emits `continuity_decision` + `progress_since_last_turn`. Full functional decision surfaced in `decision.functional_decision`, `instructional_analysis`, and the `functional_v3_trace.log` (engine/selected_function/focus_status/continuity_decision/naive_reader_need).
- FIXED-MINDSET ACCEPTANCE (spec case) — verified live + offline: T1 topic='the fixed mindset', focus recognized SUFFICIENT (not held for polish), selected_function='develop' → student term 'Elaboration', invitation teaches Elaboration as unfolding the thesis tied to the naive reader's next need + one learner-owned revision, no rewrite. T2 (organized revision) → continuity_decision='complete', names the operation performed, offers optional extension without inventing a weakness.
- Verified: offline harness `backend/tests/test_functional_v3_sprint1.py` 11/11; testing agent iteration_1 (live e2e, backend 100%, no critical/minor issues): durable processing (interact <2s placeholder, 409 on concurrent, no dup turns), poll-to-complete for writing+revise, full 14-key schema present, canonical_v2 import seam healthy. App is now flipped to `functional_v3` for live Teacher Review trials.


## Compass 3.0 — Live Calibration Prep: trace retrieval + completion-logic hardening (2026-06, DONE)
- Per user directive: Teacher Review stays a STUDENT experience — no function grid / naive-reader analysis / selected operation / internal JSON exposed in the public UI; no teacher dashboard or side-by-side engine compare built. App stays LIVE on `functional_v3` (rollback via `COMPASS_ENGINE`).
- DEV-ONLY trace retrieval (minimum change, no UI): `GET /api/dev/functional-v3-trace/{session_id}` (server.py, after GET /sessions/{id}) parses the append-only `functional_v3_trace.log` (strips the logger timestamp before each JSON record) and returns all trace records for that session oldest-first. Not linked from any interface; for the dev team during calibration. Verified live (count/records correct).
- COMPLETION-LOGIC FIX (the key calibration correctness issue): completion is now a WHOLE-PARAGRAPH judgment, not per-function. Strengthened `_FUNCTION_SEL_SYS` to separate three levels (requested operation completed → selected function sufficient → paragraph as a whole may still need another function/organization) and to set `continuity_decision='complete'` ONLY when ALL hold: Focus sufficient; every necessary function sufficiently fulfilled; functional_organization coherent; no remaining functional/organizational need of substantial developmental value. Added a deterministic WHOLE-PARAGRAPH COMPLETION GUARD in `_select_functions` that overrides a premature model 'complete' (when focus not sufficient, develop/support missing|partial, or org weak|partial) to 'advance'/'recurse' toward the limiting function (records `_completion_guard`).
- Verified: acceptance harness `tests/test_functional_v3_sprint1.py` still 11/11 (genuinely coherent revision still legitimately completes); new `tests/test_fv3_completion_guard.py` — a revision that develops one function but stays a disorganized list is NOT marked complete (model→'advance', org='partial'); invitation asks the writer to make the connections visible. canonical_v2 untouched. NEXT: await live calibration results before Sprint 2.


## Compass 3.0 — Thesis Decomposition + Elaboration-Target Selection (2026-06, DONE — functional_v3 only)
- Added hidden thesis-decomposition reasoning to the Develop path. When a Focus/thesis is present the selector internally decomposes it into its principal COMMUNICATIVE MEANINGS and the RELATIONSHIPS among them (organized meaning, NOT vocabulary targets) — e.g. "The fixed mindset is the belief that one's abilities are fixed and cannot change." → [a fixed mindset is a belief; the belief concerns one's abilities; those abilities are understood as fixed; 'fixed' means they cannot change]. Recorded in new schema field `thesis_decomposition` (list). Hidden reasoning objects; never shown to the learner.
- Elaboration-target selection: the engine picks the ONE decomposed meaning a NAIVE reader is LEAST likely to fully understand as the current elaboration target (`elaboration_target` = {meaning_relation, why_least_understood, what_would_help}), and frames `naive_reader_need` around it. The instructional question shifts from "what additional idea comes next?" to "what meaning expressed by THIS thesis does the reader still not fully understand?" → then "what would help the reader understand this more completely?". Must be a meaning the thesis already expresses (no new ideas invented).
- Threading: `_select_functions` builds a private `_elaboration_context` (only when selected_function=='develop') that `run()` passes into `generate_dialogue` (new optional `elaboration_context` param) as INTERNAL guidance — the invitation targets that specific compressed meaning; the block is never quoted/named to the learner. Prompt section THESIS DECOMPOSITION + SELECTING AN ELABORATION TARGET added to `_FUNCTION_SEL_SYS`; schema extended in the selector prompt. All isolated to functional_v3.py; canonical_v2 untouched; app stays on functional_v3.
- Verified live (Fixed Mindset): decomposition matched the spec example; elaboration_target named the under-understood meaning (why abilities are seen as fixed / lived manifestation); invitation asked the writer to unfold that meaning for a naive reader (not to add a new idea). Sprint-1 acceptance harness schema check is a subset test (unaffected by the added keys).


## Compass 3.0 — Sprint 1.2: Content-Neutral Scaffolding (2026-06, DONE — functional_v3 only)
- Fixed over-scaffolding on the Develop path: the prior version supplied conceptual content (candidate explanations/causal chains/examples) via the elaboration context and leading questions. Compass now separates three acts and does only (1)+(2): (1) TEACH the communicative function; (2) LOCATE it in the learner's own writing; (3) SUPPLY the conceptual content — FORBIDDEN. Rule: teach the operation, locate it, ask the learner to perform it; never propose the conceptual bridge, candidate explanations, causal chains, mechanisms, examples, definitions, or interpretations unless the learner already produced them.
- Changes (all in functional_v3.py): (a) `_DLG_SYS` gained a top-priority CONTENT-NEUTRAL SCAFFOLDING block with the copy-test self-check ("if the learner could copy my conceptual explanation into their paragraph and thereby satisfy the target, I scaffolded too much → revise"), plus a ban on leading questions that presuppose the answer; (b) `_elaboration_context` rewritten to pass ONLY which thesis meaning to direct attention to (meaning_relation) + naive-reader framing — it no longer feeds `why_least_understood`/`what_would_help` candidate content into the dialogue, and carries the same copy-test; (c) selector `elaboration_target.what_would_help` reframed to name only the KIND of operation (define/distinguish/trace/unpack), never the actual content; content-neutrality note added to `_FUNCTION_SEL_SYS`.
- Verified live (Fixed Mindset): internal `what_would_help` still lists candidate causes (genes/brain) but is NOT voiced; the invitation teaches elaboration, points to the learner's own IQ/genes/brain sentences, and asks an OPEN "why do they believe abilities are inborn… what leads them to that conclusion?" — the learner supplies all content; copy-test passes (no pasteable conceptual content in the response). canonical_v2 untouched; app stays on functional_v3.


## Compass 3.0 — Sprint 1.3: Thinking Loop Redefined (2026-06, DONE — functional_v3 live experience)
- The "thinking loop" is the Pedagogical Noticing layer (`POST /api/sessions/{id}/noticing`, consumed by the live student UI PublicPreview as interim "thinking" observations). It was drifting into instructional evaluation (the old prompt asked observations to "begin to trace the line of instruction" + name a structural distinction). Redefined its PURPOSE: reassure the learner that Compass has UNDERSTOOD what they wrote before selecting an instructional focus ("I see what you're trying to communicate") — recognition and understanding ONLY; NOT instruction/diagnosis/critique/coaching.
- New `NOTICING_SYSTEM_MESSAGE_V3` (server.py): MAY identify apparent central idea, recognize genuine accomplishments, summarize apparent organization of thinking, reflect back current understanding, show curiosity. MUST NOT identify weaknesses/deficiencies, compare/rank ideas, recommend changes/reorganizations, introduce instructional concepts, or hint at the instructional target. Tone bans contrastive words (but/however/although/right now/still/yet, incl. additive "not just X but Y" → "both X and Y") and deficiency/evaluative words (depends on/needs/missing/doesn't yet/not fully/lacks/could be); prefers "I can see…/You seem to be developing…/Your paragraph is exploring…/You're connecting…/So far I'm understanding…". Strict separation: Thinking Loop = understand; Instructional Response = teach.
- GATED on the engine: `_pedagogical_noticing` uses V3 only when `COMPASS_ENGINE=='functional_v3'`; canonical_v2 keeps the original noticing prompt (byte-identical rollback). Output JSON schema unchanged (observations[1-2]) so the frontend is untouched. No engine/UI redesign.
- Verified live (Fixed Mindset, both turns): T1 two observations reflect the central idea + apparent organization; T2 reflects what the learner developed and how they organized it — all recognition, zero criticism/recommendations/deficiencies/target-hints; banned-word scan = NONE. Elaboration is still introduced only in the instructional response after hidden reasoning.


## Compass 3.0 — Sprint 1.4: Make Communicative Functions Visible (2026-06, DONE — pedagogical/UI, engine unchanged)
- Part 1 (UI, PublicPreview.jsx `CanonicalOrientation`): each structural element now explicitly displays the communicative FUNCTION it serves, keeping the familiar structural language. Added `FUNCTION_QUESTIONS` map: Opening="What does the reader need before they can understand my focus?"; Thesis="What central understanding do I want my reader to construct?"; Elaboration="What does my reader still need to understand about this focus?"; Evidence / Example="What would convince my reader that this understanding is warranted?"; Conclusion="What integrated understanding should my reader leave with?". Shown as a "Function" line under the Current Instructional Focus name AND under every row in the Writing Structure map (new `RowWithFunction`, testids `preview-focus-function`, `writing-structure-function-*`). Goal: teach that structures exist because they perform communicative functions (not to replace structural language). Frontend build compiles clean.
- Part 2 (server.py NOTICING_SYSTEM_MESSAGE_V3): the FIRST thinking response now must EXPLICITLY recognize a SPECIFIC genuine accomplishment, naming it concretely (e.g. "You've established a clear central idea." / "You've given your reader one clear idea to build from.") — no generic praise, no instruction/evaluation/target-hint. Verified live: OBS1 → "You've established a clear definition right at the start…". Compatible with Sprint 1.3 (recognition names the accomplishment; still no deficiency/contrastive language).
- Part 3 (forward-looking organizing principle): "writing develops as learners organize communicative functions into coherent communication." Added a FUNCTIONAL ORGANIZATION DIAGNOSTIC to the functional_v3 selector's existing FO reasoning (no decision-schema change): for messy/accumulative paragraphs the hidden reasoning asks (1) what function is each idea serving? (2) which functions are missing? (3) which are present but disconnected? (4) how can their relationships become more coherent? — informs functional_organization.reader_path/limiting_relation and the DEVELOP vs FUNCTIONAL_ORGANIZATION choice. Principle recorded in `/app/memory/FUNCTIONAL_ORGANIZATION_PRINCIPLE.md` to guide future FO + CM development.
- No engine redesign, no schema change. canonical_v2 untouched (noticing V3 gated on COMPASS_ENGINE). App stays on functional_v3.


## Compass 3.0 — Terminology "Thesis" + Communicative Neutrality + Recursive Elaboration (2026-06, DONE — functional_v3)
- TERMINOLOGY STANDARDIZATION: use "thesis" for the principal organizing understanding; never substitute "central idea/main idea/key idea/primary idea" (a brief gloss allowed only when first explaining the concept to a beginner). Updated the active dialogue prompt `_DLG_SYS` TERMINOLOGY rule + examples, the functional_v3 selector, the V3 thinking-loop (server.py NOTICING_SYSTEM_MESSAGE_V3), and student-facing UI text (PublicPreview "expressed your thesis"). canonical_v2 / legacy server prompts left for the "eventually throughout Compass" phase.
- WRITING STRUCTURE DIAGRAM (PublicPreview): the communicative FUNCTION is now the primary instructional unit, shown inline as `Structure → Function` (arrow), not as secondary text beneath. Updated wording: Opening→"What does the reader need before they can understand my thesis?"; Thesis→"What thesis do I want my reader to construct?"; Elaboration→"What about this thesis does my reader still need to understand?"; Evidence→"What would convince my reader that this thesis is warranted?"; Conclusion→"What integrated understanding should my reader leave with?". Applied the same arrow format to the Current Instructional Focus box. Frontend build compiles.
- CONTENT NEUTRALITY (dialogue): Compass must never ask discipline/domain questions (e.g. "Why do people with a fixed mindset believe abilities are fixed?" is a psychology question — forbidden) and never determine the conceptual direction of the next sentence. Added a hard ban + approved communicative prompt shapes + the PREDICTABILITY TEST ("another AI must not be able to predict the student's next sentence") to `_DLG_SYS` and to `_elaboration_context`.
- RECURSIVE ELABORATION (CONSTITUTIONAL RULE, functional_v3): elaboration proceeds by recursively UNPACKING meanings the WRITER HAS ALREADY INTRODUCED (thesis + existing elaborations); never redirect to a new conceptual path if an existing writer-introduced meaning can still be developed. Reframed the selector's MEANING DECOMPOSITION (now covers whole-paragraph, writer-generated meanings incl. their own metaphors) and RECURSIVE ELABORATION target selection; `elaboration_target.meaning_relation` must quote/paraphrase the writer's OWN words; `what_would_help` names only the KIND of communicative operation. The dialogue anchors prompts in the writer's own expression ("You've introduced the idea that <their words>. What might a reader still need to understand about that?").
- Verified live (Fixed Mindset): decomposition captured writer-introduced meanings incl. "'carved in stone' (permanence metaphor introduced by the writer)"; target quoted a writer-expressed meaning (physical cause / brain & genes); invitation named the writer's own idea and asked "What might a reader still need to understand about what that actually means?" — recursive, content-neutral, passes the development/predictability test. canonical_v2 untouched; app stays on functional_v3.


## Compass 3.0 — Visible Interpretation + Negotiated Understanding, then MVP scope-down (2026-06, DONE — functional_v3)
- CONSTITUTIONAL FEATURE (backend): before evaluating, the selector emits `visible_interpretation` (verbatim learner-text spans for thesis/elaboration/evidence/opening/conclusion + focus_region + focus_portion) and `reconsideration`. `run()` now distinguishes DRAFT kinds (writing/revise/continue) from MESSAGE kinds (answer/explain) so a learner CHALLENGE never overwrites the draft; the challenge is passed to the selector + dialogue. On challenge the engine rereads the ENTIRE disputed function and does exactly one of revise/explain/narrow (never repeats), recorded in reconsideration{outcome,reread,...}. Turn model gained function_spans/focus_region/focus_portion; server persists them for functional_v3. Verified live (iteration_2: backend 3/3, frontend 1/1, no issues) — challenge "I already elaborated" → outcome=explained, reread whole elaboration, acknowledged learner, pointed inside their own writing ("brain and genes"), no repeat.
- MVP SCOPE-DOWN (per "build the smallest possible version"): the visible layer is now intentionally minimal. (1) Highlight only Thesis and Elaboration regions (region highlight gated to focus_of_work ∈ {Thesis, Elaboration}); (2) distinguish the local focus portion within the region (dashed region vs solid-underline portion, non-color-only); (3) ONE simple relation card `THESIS │ ▼ ELABORATION` (testid preview-interpretation-mvp), shown only when focus is Elaboration; (4) ONE communicative question attached: "How does this elaboration help the reader understand the thesis more completely?"; (5) nothing else — no trees/graphs/animation/analytics/drag-drop. Replaced the busier "What Compass is reading" legend with this minimal card + a 2-item highlight key. Optimized for changeability. Verified live via screenshot: highlights render on the draft, focus=Elaboration, MVP relation card present.
- Backend spans + reconsideration remain full (dev team inspects via GET /api/dev/functional-v3-trace/{session_id}); only the learner-facing VISUAL was minimized. canonical_v2 untouched; app on functional_v3.


## Compass 3.0 — Visible Interpretation DIAGRAM frozen + hidden (2026-06, DONE)
- Product decision: the separate learner-facing communicative diagram (THESIS ▼ ELABORATION + question) added cognitive load without equivalent value; the highlighting + instructional dialogue carry the pedagogy. FROZEN and HIDDEN behind a dev flag: `SHOW_COMMUNICATIVE_DIAGRAM` in PublicPreview.jsx (default false; re-enable with `?diagram` in the URL). No further layout/styling/arrow work.
- KEPT (infrastructure for later): backend visible_interpretation spans + reconsideration; the highlighting system (region/portion/thesis overlays on the draft); the relation/question architecture (FUNCTION_QUESTIONS + focus_region/focus_portion). Verified live: diagram gone by default (preview-interpretation-mvp count=0), highlighting still renders (vi-region/portion/thesis all present).
- DESIGN PRINCIPLE recorded: never show a visualization just because we can; every visual must reduce cognitive load. For now the highlighted writing + instructional dialogue are the primary workspace. Next phase: keep refining reasoning, highlighting, negotiated dialogue, and communicative questions; design a permanent visualization only when real use reveals a recurring need.

## Compass 3.0 — Highlighting Accessibility + Visual Simplification (2026-06, DONE, testing_agent-verified iteration_3)
- Fixed the confusing / color-dependent highlighting with a stable, grayscale-safe visual grammar (MVP: Thesis + Elaboration only): SOLID outline + "THESIS" label = Thesis region; DASHED outline + "ELABORATION" label = Elaboration region; UNDERLINE = only the local passage in current instructional focus; no marking = outside. No color carries meaning; no competing indicators; regions are boxed (not underlined) so underline has one meaning.
- Impl: PublicPreview.jsx renderInterpretationSegments renders each region as ONE <mark> (vi-thesis-region / vi-elab-region) with the focus portion wrapped in inner <span class=vi-focus-underline>. CSS in index.css uses `outline` (not border) so the transparent overlay stays character-aligned with the textarea; labels via ::before pseudo-elements (no layout shift); underline via text-decoration-color independent of the transparent text. Neutral outline color (#44403c) for both — solid vs dashed + labels are the distinction.
- Verified by testing_agent (iteration_3, 7/7 PASS, no issues): thesis solid+THESIS, elaboration dashed+ELABORATION, underline only on the local focus, grayscale-distinguishable, no double meaning, uncluttered; frozen communicative diagram stays hidden (preview-interpretation-mvp=0). Scope limited to Thesis/Elaboration (not extended to Opening/Evidence/Conclusion). canonical_v2 untouched; app on functional_v3.

## Compass 3.0 — Visible Interpretation MVP revision: three nested levels (2026-06, DONE)
- Revised the highlighting to mirror Compass reasoning at 3 nested levels: L1 Function label = the boxed THESIS/ELABORATION identifier (::before); L2 Whole communicative unit = SUBTLE neutral shading across the entire function span (removed the heavy outline box so it never looks selected/edited); L3 Current instructional focus = ONE lightweight marker (thin rounded outline + slightly darker tint) on the specific passage under discussion — replaced the underline (which competed with text conventions).
- Impl: index.css .vi-thesis-region/.vi-elab-region now use background shading (no box) + box-decoration-break clone + ::before labels; new .vi-focus-marker (outline 1px + darker tint, rounded). Renderer span testid renamed vi-focus-underline -> vi-focus-marker. Uses background+outline (no border/padding) so the transparent overlay stays aligned with the textarea. Grayscale-safe (shade intensity + labels, no hue meaning).
- Verified via live screenshot: THESIS + ELABORATION labels with subtle shaded units, focus passage marked by rounded outline+tint, no underline, clean/uncluttered; instructional dialogue intact; frozen diagram hidden (preview-interpretation-mvp=0). Scope still Thesis+Elaboration only. canonical_v2 untouched; app on functional_v3.

## Compass 3.0 — Constitutional Rule: Development Through Emerging Communicative Constraints (2026-06, DONE)
- New constitutional rule (functional_v3 ONLY; canonical_v2 untouched as frozen rollback baseline): every revision is a change to the draft's COMMUNICATIVE SYSTEM, not an isolated correction. After a revision Compass rereads the draft as an integrated whole and determines (a) which relationships got STRONGER, (b) which now need attention, (c) what NEW constraint the revision created, (d) WHY the focus shifted — then explains the transition to the writer so they understand not just WHAT the next task is but WHY it became the next task.
- Scope (per user): applies primarily on true revision turns (a prior draft exists to compare against); the internal reasoning also supports explaining a novel principle a student isn't grasping.
- Impl (prompt + one internal context thread + one observability field; NO schema/engine/UI redesign): (1) added the constitutional block to `_FUNCTION_SEL_SYS` (functional selector) after REVISION COMPARISON; (2) added `emerging_constraints` {revision_performed, relations_strengthened, new_constraint, why_focus_shifted} to the selector JSON schema (empty on first turn); (3) `_select_functions` builds `_emerging_constraints_context` on revision turns and returns it; the full block also surfaces in the functional_v3 trace via `functional_decision.emerging_constraints`; (4) `run()` threads `emerging_constraints_context` into `generate_dialogue`; (5) continuation-mode dialogue step (5) rewritten to NARRATE THE TRANSITION (name the relationship the revision strengthened, then the new challenge that follows) instead of stating a bare hold/advance.
- Verified live (localhost, 2-turn revise session, `backend/tests/test_emerging_constraints.py`): turn 1 emerging_constraints empty (first_turn); turn 2 continuity=advance with richly populated emerging_constraints ("The revision itself created this constraint…"), and the student-facing invitation narrated the transition ("Because you introduced the idea that lateness tells someone their time is worth less, your reader is now ready to go one level deeper"). git diff: only functional_v3.py changed (57 insertions). NOT yet run through the testing_agent / frontend flow — awaiting user verification.


## Compass 3.0 — Visible Interpretation revised grammar: continuous paragraph + dotted-underline focus (2026-06, DONE)
- Constitutional principle enforced: NEVER separate the student's paragraph into visual sections — Compass interprets, it does not reconstruct. The paragraph stays one continuous piece; structural info is overlaid on top.
- Change (frontend-only, CSS + comments; no logic/data-testid/scope change): Level 3 (current instructional focus) switched from the rounded OUTLINE BOX to a DOTTED UNDERLINE (+ a slightly stronger tint within the already-shaded unit). Removed `outline`/`outline-offset`/`border-radius` from `.vi-focus-marker`; now `border-bottom: 2px dotted #57534e` + `background rgba(120,113,108,0.20)`, with box-decoration-break: clone so the underline wraps across line breaks. The boxed ::before label (THESIS/ELABORATION) remains the ONLY boxed element. Level 2 keeps subtle continuous inline shading over the whole unit (no box, no whitespace, no panels). `.vi-thesis-region` border-radius removed for a flat continuous tint.
- Verified via a faithful static CSS replica screenshot (same classes/structure the live overlay uses): boxed labels above first word; entire Thesis + entire Elaboration units softly shaded as one continuous paragraph; focus passage dotted-underlined and wrapping cleanly across the line; no box, nothing else underlined. Scope still Thesis + Elaboration (MVP). canonical_v2 untouched; app on functional_v3. Live in-app overlay uses identical classes → renders identically; user experiential verification in the writing preview still welcome.


## Compass 3.0 — Visible Interpretation "Fix for Image 2": colour-coded, sentence-aligned grammar (2026-06, DONE)
- Applied the user's precise attachment spec. User chose option (b): keep the writing canvas editable in place; labels float just above the first word of each unit (true inline label boxes are impossible over a live textarea without misalignment). This intentionally OVERRIDES the earlier grayscale-safe accessibility rule — colour is now used (redundant label text/box remains).
- Grammar: L1 boxed label above first word — blue outline+text "THESIS", purple outline+text "ELABORATION" (the only boxed elements). L2 whole-unit shade colour-coded — light BLUE thesis (rgba(37,99,235,.11)), light PURPLE elaboration (rgba(147,51,234,.11)), snapped to FULL sentence boundaries so shading never splits a sentence; elaboration is clipped so it never overlaps the thesis span; text after the elaboration's last sentence (the concluding sentence) is left unshaded. L3 focus = dotted PURPLE underline (#6b21a8) + slightly stronger tint on the one passage under discussion; nothing else underlined. Paragraph stays one continuous piece (no line breaks/spacing/section splits; text never moved).
- Impl: `lib/thesisMatch.js` added `snapRangesToSentences(draft, ranges)` (expands each range out to sentence terminators .!? + closing quotes, trims whitespace) and `subtractRanges(ranges, blockers)` (clips overlap). `PublicPreview.jsx` thesis/elab ranges now snapped, elaboration subtracted against thesis. `index.css` `.vi-thesis-region`/`.vi-elab-region`/labels recoloured blue/purple; `.vi-focus-marker` dotted purple underline. No backend/data-testid change (vi-thesis-region / vi-elab-region / vi-focus-marker preserved).
- Verified: node unit test of the helpers (thesis + elaboration each snap to full sentences ending cleanly on punctuation, elaboration clipped off the thesis, no split sentences); static CSS-replica screenshot of the attachment's fixed-mindset example matched the "Final Correct Visual" (blue THESIS + blue shade to "(Dweck, 2006, p.x).", purple ELABORATION + purple shade, dotted underline on "deep-seated traits.", concluding sentence unshaded, continuous paragraph). Frontend compiles clean. Scope still Thesis + Elaboration (MVP). Live in-app overlay uses identical classes/logic → renders identically; user experiential verification in the writing preview welcome.


## Compass 3.0 — Visible Interpretation live-fix: unit boxes, full elaboration, clean focus underline (2026-06, DONE)
- Fixed 5 issues found in a live screenshot (functional_v3, ?preview): (1) thesis had no box → added a thin BOX around each unit via inset box-shadow (blue thesis, purple elaboration) — used box-shadow not border so it adds NO layout width and the transparent overlay stays char-aligned with the textarea; (2)+(3) the elaboration unit was unlabeled and only a fragment shaded → elaboration is now ONE contiguous span (union of function_spans["Elaboration"] + focus_region → [min start, max end] → snap to full sentences → clip off the thesis), so the FULL elaboration is shaded and the ELABORATION label always renders; (4) dotted underline ran over blank space → the renderer now never underlines whitespace (splits focus runs, leaves space sub-runs unmarked) and focus ranges are whitespace-trimmed + clipped to the active unit; (5) focus underline now colours to match its unit (blue in thesis, purple in elaboration) via vi-focus-thesis / vi-focus-elab modifiers.
- Also: L3 focus falls back to focus_region when the engine leaves focus_portion empty (whole region is the focus), so the passage under discussion is always marked. Diagnosed against real trace data: function_spans["Elaboration"] holds the full verbatim elaboration; focus_region was one sentence, focus_portion "".
- Impl: `lib/thesisMatch.js` (snapRangesToSentences + subtractRanges from prior step reused); `PublicPreview.jsx` new elabSignals/elabRanges union memo, portionText fallback + trim + unit-clip, focusClass, renderInterpretationSegments splits whitespace out of underline + takes focusClass; functionSpans memoized. `index.css` unit box-shadow boxes + vi-focus-thesis/vi-focus-elab colours; label z-index. No backend/data-testid change.
- Verified: node logic test on the real draft+signals (thesis=full sentence; elaboration=one contiguous span covering the whole unit ending cleanly on punctuation; focus=the focus_region sentence clipped to elaboration, whitespace-trimmed) + static CSS-replica screenshot matching the target (blue boxed THESIS, purple boxed+labeled FULL elaboration, purple dotted underline on the focus sentence, no blank-space underline, continuous paragraph). Frontend compiles clean (0 warnings). NOTE: bordering wrapped inline text draws the box per line fragment (a stacked-box look) — inherent tradeoff of keeping one continuous paragraph; acceptable. User experiential verification in the live writing preview still welcome.


## Compass 3.0 — Visible Interpretation: uniform unit shading (2026-06, DONE)
- Fix: the elaboration (secondary unit) looked non-uniform because the L3 focus passage carried a stronger background tint, creating a darker patch inside the otherwise-even shade. Removed the focus background tint entirely — L3 is now marked ONLY by the unit-coloured dotted underline (blue in thesis, purple in elaboration). Each unit therefore keeps ONE uniform shade (light blue thesis / light purple elaboration, distinct colours). CSS-only: `.vi-focus-thesis`/`.vi-focus-elab` now set border-bottom-color only (no background). Verified via static CSS-replica screenshot — elaboration evenly shaded, focus sentence underlined only, thesis a distinct uniform blue. Frontend compiles clean.


## Compass 3.0 — Visible Interpretation: colour-blind-safe unit colours (2026-06, DONE)
- Changed the elaboration unit from purple to ORANGE/amber so the two units are distinguishable by colour-blind users (blue thesis vs purple was unsafe — purple reads as blue under red-green deficiency). Blue (#1d4ed8) + orange (#c2410c) is the canonical colour-blind-safe pair and also differs in luminance (separable in grayscale); labels/boxes/underline give redundant non-colour cues. CSS-only: `.vi-elab-region` shade rgba(234,88,12,0.15)+orange box, `.vi-elab-region::before` + `.vi-focus-elab` + base `.vi-focus-marker` recoloured orange. Verified via static replica screenshot; frontend compiles clean.

## Compass 3.0 — Writing canvas auto-grows to show the full draft (2026-06, DONE)
- Fix: the student writing canvas had a fixed height (min-h-[34vh]) with an inner scrollbar, so long drafts were cut off. Added an auto-grow effect in PublicPreview.jsx (`fitDoc`: set height=auto then height=scrollHeight, overflowY hidden) that runs on draft change, on entering the writing screen, and on window resize. The textarea now expands to fit its entire content (min-h-[34vh] kept as a floor), so the whole paragraph is always visible with no inner scroll; the page scrolls if needed. Because the highlight overlay is absolute inset-0, growing the textarea grows the container and keeps overlay + text aligned. Verified via static replica screenshot (full multi-sentence draft visible, no scrollbar). Frontend compiles clean.

## Compass 3.0 — Persistent question banner + header cleanup (2026-06, DONE)
- Removed the header engine badge (data-testid preview-reasoning-mode-badge / "Canonical v2" / "Legacy v2") — no engine toggling anymore; header now shows only the Compass logo + name.
- Added a sticky "Assignment" banner (data-testid assignment-banner / assignment-banner-text) pinned to the top of <main> (sticky top-0, z-20) so the question the learner is answering stays visible THROUGHOUT the process (writing, organizing, and revising). Shown whenever an assignment exists and not on welcome/assignment-entry/reflection screens. questionText = assignment || session.assignment. Removed the now-redundant inline 'Your assignment' box (writing-assignment-display) from WritingScreen and dropped its unused assignment prop.
- Verified by testing_agent (iteration_4.json, frontend-only, 100% 7/7): banner pinned on both writing and revision screens (persists across the LLM turn and on scroll); preview-document auto-grow confirmed (clientHeight==scrollHeight=460, overflowY hidden, no inner scrollbar on an ~809-char draft); no engine badge in header; old inline assignment box confirmed removed.

## Compass 3.0 — Constitutional refinement: explain constraints, not content directions (2026-06, DONE)
- Problem: the emerging-constraints narration was drifting into determining the CONCEPTUAL direction of the writing ("your reader's next question naturally becomes…", "the next step is…"). New constitutional principle: Compass identifies the communicative RELATIONSHIP/constraint that now needs attention and explains WHY the focus shifted; it NEVER decides what the writer should think/say next. The STUDENT determines how to satisfy the constraint, and there are always MANY valid paths (non-deterministic language required).
- Impl (functional_v3 prompt-only, canonical_v2 untouched): (1) added an "EXPLAIN CONSTRAINTS, NOT CONTENT DIRECTIONS" clause to the selector constitutional block; (2) rewrote continuation-dialogue step (5) — removed the "reader's next question becomes Z → the next step is" example; now forbids content-direction + single-path phrasing ("the next step is", "the paragraph now needs", "the next question is", naming the specific point) and mandates preferred non-deterministic relationship forms ("One communicative relationship that now deserves attention is…", "One productive direction for revision would be…", "there are many ways… the current focus is one likely to help most"); the (6) invitation is now offered as one productive option, not the only move; (3) reframed the internal _emerging_constraints_context the same way.
- Verified live (2-turn revise, tests/test_emerging_constraints.py): turn-2 coach reply used "One communicative relationship that now deserves attention is between your thesis and that phrase…" and an open question inviting the student to decide the content — no forbidden deterministic/content-direction phrasing. emerging_constraints still populated. Backend restarted; syntax OK.

## Compass 3.0 — Fix: working unit stops being shaded as Compass iterates (2026-06, DONE)
- BUG (user, real usage): across revision turns the Visible Interpretation stopped shading the unit Compass was working on.
- Two root causes fixed (frontend only): (1) the overlay only rendered Thesis + Elaboration, so once focus advanced to Evidence/Conclusion/Opening the worked-on unit was never shaded; (2) the range locator relied on exact substring matching, so as the working unit grew/re-wrapped across revisions (focus_region seen growing 88->863 chars in real sessions) the long verbatim span no longer matched and shading dropped.
- Fix: (a) generalized the overlay to shade the THESIS (blue) + the CURRENT WORKING UNIT (orange) whatever structure it is (Elaboration/Evidence/Conclusion/Opening), with a dynamic label via CSS content: attr(data-label); refactored classes to .vi-region + .vi-region-thesis/.vi-region-active and testids vi-thesis-region/vi-active-region; focus underline .vi-focus-thesis/.vi-focus-active. (b) lib/thesisMatch.js added locateSpanLoose (whitespace-tolerant match, maps back to original indices) and locateUnit (loose full match -> first+last sentence anchor -> token fallback) so long, revision-drifted units keep matching; PublicPreview thesis/active-unit/portion ranges now use locateUnit.
- Verified by testing_agent iteration_5.json (frontend, 100% 4/4 turns): working unit (ELABORATION) shaded on every turn across draft + 3 revisions, region growing with the draft (h 237->309->381->381), dynamic uppercase label correct. Also node-tested locateUnit against whitespace drift + middle-sentence drift (still bounds the full unit). Frontend compiles clean.
- Note: in that session vi-thesis-region was null (focus stayed on Elaboration and current_thesis paraphrase didn't locate) — thesis shading depends on current_thesis matching the draft; the WORKING-unit shading (the reported bug) is reliably fixed.

## Compass 4.0 Sprint 1 — hidden Developmental Cognition Object (DCO) (2026-06, DONE)
- Goal: make Compass's developmental reasoning VISIBLE for calibration — no UI redesign, no coaching-prompt improvement, no new instructional features, and NO coaching behavior depends on it.
- Impl (functional_v3 selector, single existing LLM call — no extra round-trip; canonical_v2 untouched): added a `developmental_cognition` object to the selector JSON schema + a system-message directive marking it INTERNAL/DEVELOPER-ONLY that must NOT influence coaching/selected_function/invitation. Fields: orientation_target_interpretation, orientation_target_confirmed, conceptual_organization, communicative_organization, coordinative_capacity (Kurt Fischer dynamic-skill terms: single representations / representational mappings / representational systems / single abstractions / abstract mappings / abstract systems), instructional_horizon, reachable_next_move, beyond_horizon, and a per-estimate confidence {field: high|medium|low}.
- Plumbing: _select_functions returns _developmental_cognition (from fd); run() adds decision.developmental_cognition; server _log_functional_v3_trace writes a top-level `developmental_cognition` into the trace record. Exposed via existing GET /api/dev/functional-v3-trace/{session_id} (developer-only; not linked from UI).
- Verified live: created a session, submitted a draft, read the dev endpoint — developmental_cognition present and fully populated (e.g. coordinative_capacity: 'coordinating representational mappings…'), with confidences. Coaching path unchanged. Backend syntax OK / restarted.

## Compass 4.0 Sprint 2 — Developmental Cognition calibration tooling (2026-06, DONE)
- One-click developer access: gated behind URL param ?dco. A fixed 'View Developmental Cognition' button (data-testid view-developmental-cognition-button, bottom-left) appears once a session exists; clicking opens DevCognitionPanel (data-testid dev-cognition-panel). No API calls needed by the developer. Absent without ?dco (verified).
- Evidence per estimate: backend developmental_cognition schema now carries a parallel evidence{} object (1-4 short observations grounded in the student's actual writing) alongside confidence{} for all 8 fields. Panel shows estimate + confidence + evidence bullets per field, and a raw-JSON toggle (dev-cognition-raw -> dco-raw) that renders the COMPLETE object verbatim (~4.7KB, includes confidence + evidence). Auto-refreshes on each new AI turn (turnKey) + manual refresh (dev-cognition-refresh).
- New API: getFunctionalTrace(id) -> GET /api/dev/functional-v3-trace/{id} (dev-only, not linked from normal UI).
- Coaching behavior, coaching prompts, and student UI UNCHANGED; DCO still influences nothing. Teacher Review remains only a harness for generating authentic writing.
- Verified: backend curl (evidence + confidence + Fischer terms populated) and testing_agent iteration_6.json (frontend 100%): all 7 fields render with estimate/confidence/evidence, raw JSON complete, gating correct.
- ACCESS: open the student flow with &dco, e.g. {FRONTEND}/?preview=writing&dco -> enter assignment -> writing screen shows the button -> after each interaction press the panel's ↻ to load the latest developmental diagnosis.
